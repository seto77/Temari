# Temari — 計画書

*2026-08-04 起草。正本はこのファイル。英語の対外向け説明は `README.md`、
層構造の詳細は `docs/architecture.md`。*

---

## 1. これは何か

**孤立原子に対する散乱・励起の各種因子を、外部依存ゼロで第一原理から計算する
汎用エンジン。**

出発点は ReciPro の STEM-EDX 用イオン化形状因子 F(s, E₀) を作るコード
(`tools/IonizationGen/handout/ionization.jl`) だが、そのコードを実測で分解すると
性格がはっきりする。

| 層 | 行数 | 割合 |
|---|---:|---:|
| 第 2〜5 章 (中心力場の動径エンジン) | 835 | 34 % |
| 自前数値ライブラリ (特殊関数・スプライン・求積) | 562 | 23 % |
| 第 6 章 (MDFF・対称 Ewald 運動学 = **電子衝撃専用**) | 388 | 16 % |
| 第 7 章 (Bote–Salvat 表の参照。物理計算ゼロ) | 49 | 2 % |
| その他 (入出力・診断・自己検証) | 約 600 | 25 % |

つまり **「イオン化形状因子を計算するコード」ではなく、その 16 % の薄皮を剥がせば
中心力場の動径ソルバが露出する**。イオン化は一出口にすぎない。Temari はこの
エンジンを主役に据え直し、出口を増やす。

## 2. 名前について

手毬 (てまり) は球面を幾何学的に分割し、糸を何本も巻き重ねて模様を作る日本の
伝統工芸。このコードの骨格である「球対称な原子場の上に部分波 (球面調和関数) を
何十本も重ねる」構造そのものの比喩になっている。部分波展開という骨格は、GOS でも
弾性位相差 δ_l でも Compton の S(q) でも変わらないので、扱う量が増えるほど比喩の
適合が良くなる。

命名は 4 観点 (物理・分野慣習・ブランド・日本語由来) で 24 案を出し、3 基準
(名前の衝突・将来の射程・実運用の使いやすさ) で独立採点して選んだ。Temari は
唯一どの基準でも弱点がなかった (最低点 76/100)。次点は CentralField (射程 91 だが
GitHub 検索で CRM/DB 系のノイズを拾う)、CFATOM (衝突ゼロだが口頭伝達に難)。

この分野の定番 (FEFF, GRASP, PENELOPE, ELSEPA, µSTEM) はいずれも不透明名なので、
説明的である必要はない。ただし **サブタイトルで中身を宣言する**:

> Temari — atomic scattering and excitation factors from first principles

## 3. 設計原則 (妥協しない 6 点)

1. **依存ゼロ**。Julia 標準ライブラリのみ。GRASP・WIEN2k・BLAS すら要らない。
   同梱データは Bote–Salvat 係数 (パブリックドメイン) だけ
2. **スタンドアロン**。1 ファイル `include` で動く状態を保つ。パッケージ化しても
   「1 ファイルに落として渡せる」性質は捨てない (引継ぎ・査読・再現性のため)
3. **MIT**。参照実装として誰でも読めて使えること。実装は AI 支援だが、処方の
   選択と検証は人間が責任を持つ
4. **速い**。年 1 回のバッチではなく対話的に使える速度を狙う。ただし
   **速度より再現性が上位** (§6)
5. **物理が読める**。コード中のコメントが処方の正本。論文を読まなくても
   何を計算しているか追えること
6. **GUI とエンジンの界面は CLI 契約に固定する**。最低限の GUI を持たせる場合も、
   GUI はサブコマンド + JSON/CSV ファイル出力だけを叩く別プロセスの薄いシェルとし、
   in-process 結合 (embed・直リンク) は行わない。GUI がどう育っても成長は境界の
   GUI 側で起こし、エンジンの言語・構造の判断に逆流させない (2026-08-05 決定)

## 4. 出口の全体像 (何を計算できるか)

4 観点で実コードを精査した結果。工数は現行コードからの差分。

### 4.1 すでに計算して捨てている量 (工数 = 出口を付けるだけ)

- **EELS コアロス微分断面積 dσ/dΔE** — `compute_NK` が `diag.dNde`
  (ε ノード × K ノード行列) を持っている。その K=0 列に 4γ²a₀² を掛ければ
  平行照射 EELS の dσ/dε そのもの。吸収端形状が実質ゼロ工数で出る
- **弾性散乱の部分波位相シフト δ_l** — `ContinuumSet` は漸近域を
  u ≈ a·F_l + b·G_l に最小二乗フィットしている。δ_l = atan2(b, a) が既に手元にある
- **内殻寄与の阻止能** — `diag.dNde` と `diag.w` の 1 行の縮約

### 4.2 小〜中工数

| 量 | 工数 | 要点 |
|---|---|---|
| **一般化振動子強度 (GOS) / Bethe 面** | 中 | **E₀ に依存しない** (連続状態も R テーブルも k_i/k_f を物理的に参照しない) ので E₀ 次元が丸ごと消え、1 チャネル 1 回で済む。Egerton の Hartree–Slater GOS 表は 1980 年代のもので、**公開された近代的・相対論的 GOS は事実上存在しない** — 最大の空白地帯 |
| **二重微分断面積 d²σ/dΩdΔE** | 小 | `angular_integral` の K=0 分岐が既に θ 格子上で S/Q⁴ を評価済み。しかもその θ 格子は前方 1/Q⁴ ピークを均す変換で張ってあり、EELS の mrad 級収集角に自動的にノードが密集する |
| **収集角・エネルギー窓つき部分断面積 σ(β, Δ)** | 中 | EELS 定量の k-factor そのもの。最も裾野が広い。実質の作業は ε 窓専用ノードの設計 |
| **X 線原子散乱因子 f_x(s) → Mott–Bethe → 電子散乱因子 f_e(s)** | 小 | SCF 電荷密度から直接。任意価数のイオンも第一原理で出る (Peng/Doyle–Turner のパラメータ表が不要になる) |
| **Mott 型弾性散乱 dσ/dΩ・σ_el・σ_tr** | 中 | δ_l から。中性の漸近条件 (z_asym=0) と部分波上限の付け替えが要る |
| **副殻光イオン化断面積 σ_nl(ω) と非対称パラメータ β_nl** | 中 | 高速電子の遷移演算子を光子の双極子演算子に差し替えるだけ。エネルギー規格化は光電離が要求するものと同一で追加の状態密度因子が要らない |
| **M 殻 (M1–M5)** | 小 | `bote_salvat.json` は Z≳50 で 9 副殻を既に収録済み。`CHANNELS` に 5 行足せば通る (l_init=2 用の [3j]² 表は追加推奨) |
| **ΔSCF 束縛エネルギー・緩和エネルギー** | 中 | 中性と緩和イオンの SCF を既に両方解いてキャッシュしている |
| **Compton 散乱関数 S(q)** | 中 | 束縛軌道対の多重極行列要素は R テーブルと同じ積分 |
| **TDS 吸収形状因子 f′(g,h;B)** | 中 | 2 つの前方ピークを持つ被積分関数という点で角度求積の設計が同型 |

### 4.3 大工数 (将来)

- STEM-EELS 用 非局在イオン化形状因子 F(s; β, Δ) — 円形アパーチャが
  分離型 Gauss–Legendre 求積を壊す (θ を k̂₊ 基準で測っているため)
- 異常分散 f′, f″ (Cromer–Liberman 級)
- 完全 Dirac 連続状態 (κ 分解位相シフト、スピン偏極)
- EXAFS 用の中央原子位相と後方散乱振幅
- 束縛-束縛遷移 (白線)

### 4.4 やらない

- 蛍光収率・オージェ・Coster–Kronig — 多電子遷移確率は別問題。文献値
  (Krause 表) を使うべき領域
- 白線の**定量** — 孤立原子・平均場では多重項と固体 DOS が原理的に出ない。
  GOS の部分和則 (∫df/dΔE dΔE → 占有数) の低 Q 側の不足量が白線の欠落量
  そのものなので、まず GOS で不足量を測ってから判断する

## 5. 層構造 (詳細は `docs/architecture.md`)

```
L0 数値基盤    特殊関数 (球ベッセル/Coulomb)、スプライン、求積、ODE 積分器
L1 原子構造    SCF (HFS/DHFS)、束縛状態 (Schrödinger/Dirac)、ポテンシャル
L2 連続状態    歪曲波、エネルギー規格化、漸近マッチ、直交化、スカラー相対論
L3 動径行列要素 ∫u_a · j_λ(Qr) · u_b dr (多重極)
L4 角運動量代数 3j、Legendre、MDFF 組み立て
L5 出口        電子衝撃 / 弾性 / 光子 / 輸送
```

**変わる軸は 2 本だけ**: (a) 始状態と終状態を結ぶ**演算子** (高速電子の
遮蔽 Coulomb / 光子の双極子 / 弾性のポテンシャル散乱)、(b) 何で積分して
何を出すかという**出口**。L0–L4 は全出口で共有される。現行コードの第 6 章は
「電子衝撃という 1 つの演算子 + F(s) という 1 つの出口」を直に書き下したもので、
これを演算子と出口に分離するのが最初のリファクタリング。

## 6. 再現性の規律 (速度より上位)

年 1 回のテーブル生成が科学的な出荷物である以上、**同じ入力から同じビットが出る**
ことが速度より重要。2026-08-04 の v3 生成で確立した運用:

- 総和順序を変える最適化 (`@simd` 等) は、**ビット同一性を失うと明示的に宣言し、
  テーブル全体を再生成する場合にのみ**採用する。順序を保つ最適化
  (キャッシュブロッキング等) を優先する
- **Julia 処理系のバージョンはデータセット世代の一部**。世代ごとに MANIFEST へ
  ピン留めし (v3 = Julia 1.11.9)、処理系の更新はコード生成・数学関数実装の変化で
  ビット同一性を壊しうるため、**テーブル全再生成と同格の宣言事項**として扱う
  (実態は既にこの運用。掟を実態に合わせる明文化。2026-08-05)
- **決定論バグの修正はビット互換に優先する** (2026-08-06 作者決定)。計算結果が
  実行環境 (負荷・スレッド分割) に依存して揺れる箇所は、それ自体が再現性のバグで
  あり、その出力ビットを参照絶対値として保護しない。修正 (index-order 固定化など)
  は「テーブル全再生成と同格の宣言事項」として実施し、以後の参照ビットは決定論版で
  再定義する。バグ込みで再現性の低いビットを基準に据えるのはナンセンス、が原則。
  (発端 = E8。2026-08-06 に計装待ち伏せで捕獲・解剖済み: **エンジン側の決定機構は
  全てシロ** — 縮約 dgemv・LAPACK stev・整列依存 peeling を法医学 (468 サイドカー
  突合 + 単一ノード再現 240 試行) で棄却。残る整合的説明はランタイムの過渡擾乱
  (concurrent sweep GC 系 = 既知 Windows GC クラッシュの「静かな親戚」。発生率
  ~1e-2/行 @t4 既定 GC、振幅は中間値 1 個の低位ビット級 = 物理許容の 6 桁下)。
  したがって**コード修正対象なし・index-order 書き換えは中止** (観測フリップを
  1 件も防がずビット互換だけ失うため)。ビット同一規律の適用範囲は**単発実行の
  決定論**と明文化し、フリート実行間のバイト一致は保証対象外 (QC ゲートで監視)。
  休眠サイドカー計装 (E8_SIDECAR 環境変数で覚醒) を配備コードに常設し、以後の
  フリート実走で自然捕獲を継続。根治はランタイム側 = 上流 #62162 系修正の
  リリース到達時に再評価 + 次回長時間実行で WSL2 レーンのフリップ率も比較。
  原則自体は維持 — 今回は「コードのバグではなかった」という決着)
- 最適化の可否は必ず**実測**で決める。今回の実測例:
  - 内側ループのキャッシュブロッキング: 2.4 倍 (ビット同一)、全体 1.13 倍
  - 転置 + `@simd`: 8.3 倍だがビット非同一 → 全体 1.27 倍にしかならず不採用
  - 除算の逆数化: 1.03 倍 (依存連鎖でレイテンシ律速。効かない)
  - `--heap-size-hint`: GC 回数 151 → 151 で**効果ゼロ**
  - BigInt 階乗の表引き化: 割り当て量 0.777 → 0.775 GB (仮説は外れたが害はない)
- 検証は 3 段:
  1. 解析解に対するテスト梯子 (selftest T0–T8。c→∞ 極限で非相対論に 8.5e-15 で還元)
  2. 独立実装との照合 (Julia ↔ Python で max|ΔF| ~ 9e-8)
  3. 外部参照 (K 殻は Oxley–Allen 2000 / µSTEM と 1 % 以内)
- **新しく追加すべき検証**: 振動子強度の部分和則。∫df/dΔE dΔE が占有数
  (K で 2、L3 で 4) に収束するかは、外部参照が無い量にも効く強い自己検査

## 7. 性能方針

2026-08-04 の実測で分かったこと。

- **Julia はスカラー数値カーネルでは C++ とほぼ同速**。GC 時間は全体の 4.3 % しか
  なく、C#/C++ への素直な移植では 1.0〜1.2 倍しか得られない。**移植はしない**
- プロファイル: 球ベッセル漸化 58 %、R 積分の内側ループ 24 %。この 2 つで 8 割
- **プロセス並列 > スレッド並列**。4 プロセス × 8 スレッドから 8 プロセス × 4 スレッドに
  変えるだけで **2.26 倍** (CPU 使用率 68 % → 94 %)。ε ノードの並列幅と同期
  オーバーヘッドの関係で、1 プロセスに 8 スレッドを与えても使い切れない
- 残る最適化候補: **SIMD 化はロードマップ P1 (最優先) へ昇格** (§8.1)。以下は詳細:
  - 球ベッセル漸化の**動径点方向 SIMD** (λ 方向は逐次だが動径点は独立。AVX-512 で
    4〜8 レーン)。58 % の部分に 3〜4 倍が見込め、全体 1.5 倍以上。ただし
    Miller 下方漸化と上方漸化の分岐、途中リスケールのレーン別処理が要る
  - スレッド別ワークスペースの事前確保 (割り当て 0.78 GB/ケースの大半は
    連続状態と R テーブルの大配列。GC 露出そのものを減らせる)
  - FFTLog による球ベッセル変換 (対数格子なので原理的に適用可。O(N²) → O(N log N)
    だが精度の作り込みが重い)
- ⚠ **この計算はメモリ帯域律速の側面が強い** (2026-08-05 実測)。8 プロセス →
  16 プロセスに増やすと CPU 使用率は 65 % → 100 % になるのに、スループットは
  **1.16 倍にしかならなかった**。増えた CPU 時間の大半が帯域待ちで消えている。
  → コア数やプロセス数を増やす方向は既に頭打ち。**SIMD 化 (1 命令で 8 要素を
  処理し、同じデータ移動でより多くの仕事をする) の方が筋が良い**という判断の
  裏づけになっている。R 積分の配列レイアウト改善 (§8.1-4) も同じ理由で効く
- **GPU は使わない** (§8.1 末尾)。作者機 RTX 4060 の FP64 は 0.24 TFLOPS で、
  Ryzen 9950X の AVX-512 (約 2.2 TFLOPS) に 9 倍負ける。ただし上の帯域律速を
  踏まえると、**FP64 が速いカードでの優位は演算性能より帯域**にある
  (A100 で 2 TB/s、MI300X で 5.3 TB/s に対し DDR5 は 80–90 GB/s)。将来
  検討するなら「FLOPS が何倍か」ではなく「帯域が何倍か」で見積もること
- **Windows の Julia は高割り当て・多スレッドで GC がクラッシュする**。1.12 は
  `gc_mark_objarray`、1.11 は `sweep_malloced_memory` で EXCEPTION_ACCESS_VIOLATION。
  自コードに unsafe 操作は皆無なのでランタイム側の問題。運用は
  「ログ停滞 15 分で kill → 自動再起動」の watchdog + チャネル単位の resume で回避

## 8. ロードマップ

| Phase | 内容 | 状態 |
|---|---|---|
| **P0** | リポジトリ立ち上げ (本書・README・LICENSE・層の宣言) | **完了** |
| **P1** | **★最優先: SIMD 化** (§8.1)。動径点方向のベクトル化で全体 1.5 倍以上を狙う | **次** |
| **P2** | v0 コード取り込みとモジュール化。`ionization.jl` を L0–L5 に分割。selftest と refcheck を CI に載せる | |
| **P3** | 「捨てている量」の出口を付ける — GOS / dσ/dΔE / δ_l / 阻止能。ここで初めて Temari が EDX 以外に使える | |
| **P4** | 弾性側 — f_x(s)、Mott–Bethe、Mott DCS。振動子強度和則の検証を追加 | |
| **P5** | EELS 定量 — σ(β, Δ)。Egerton SIGMAK/SIGMAL と Hartree–Slater GOS との系統比較 (この分野で最強の外部検証材料) | |
| **P6** | 光子側 — 光イオン化 σ_nl、β_nl。xraylib との突き合わせ | |
| **P7** | M 殻、完全 Dirac 連続状態 | |

### 8.1 P1: SIMD 化 (作者指示 2026-08-05 により最優先)

**なぜ最優先か**: 出口を増やす前に土台を速くする。GOS や弾性へ広げるほど同じ
カーネルが呼ばれる回数が増えるので、先に効かせた方が総和で得。加えて、テーブル
再生成が年 1 回で十数時間かかる現状を対話的に使える速度へ近づけたい。

**対象と根拠** (2026-08-04 のプロファイル実測):

| 部位 | 占有 | 性質 | SIMD の効き |
|---|---:|---|---|
| 球ベッセル漸化 `sph_jl_all!` | 58 % | λ 方向は逐次依存、**動径点方向は完全独立** | ★本命。3〜4 倍見込み |
| R 積分の内側ループ | 24 % | 内積型・メモリ律速 | 転置 + `@simd` で 8.3 倍 (実測) |
| その他 | 18 % | | |

両方効けば全体 **1.5〜2.4 倍**。

**⚠ 先に直すべき正しさの欠陥** (2026-08-05、codex 指摘 → 実測で確認):

`sph_jl_all!` の Miller 経路は `scale = (sin(x)/x) / out[1]` で規格化しているが、
**j₀(x) ≈ 0 となる x ≈ nπ で 0/0 になる**。Miller の生値は任意スケールなので
raw_j₀ は丸め誤差そのものになり、スケール係数が壊れて全 λ が汚染される。

実測 (scipy 参照、lmax=40):

| x − nπ | 相対誤差 |
|---|---|
| 0 | **0.06〜1.9** (破綻) |
| 1e-12 | 1e-4 |
| 1e-9 | 1e-7 |
| 1e-6 | 5e-10 |

誤差は **1e-16/|sin x|** で効く。目標精度 1e-6 を割るのは |x−nπ| < 1e-8 の窓のみ。
v3 生成 (5×10¹⁰ 回の評価) で被弾は約 320 回と見積もられるが、1 回の被弾は動径 5000 点の
うち 1 点を 1e-7 ずらすだけで R 積分への影響は 1e-11 相対。**出荷済みデータへの実害は無い**。

修正: **j₀ と j₁ の大きい方で規格化する** (両者は非零の x で同時にゼロにならない)。

```julia
s, c = sincos(x); j0 = s/x; j1 = (j0 - c)/x
scale = abs(j0) >= abs(j1) ? j0/raw0 : j1/raw1
```

**設計方針**:

1. **動径点をレーンに載せる**。Miller の下方漸化 `jm = (2l+1)/x·jc − jp` は λ に
   ついて逐次だが、異なる r については独立。8 点 (AVX-512) を同時に回す
2. **分岐の扱い**が実装の山場。現行は `x > lmax+10` で上方漸化、それ以外は Miller、
   `x < 1e-12` は δ_l0。1 ブロック内で x が閾値をまたぐと分岐が割れるので、
   (a) x でソートしてブロックを均質化する、(b) マスクで両方計算する、
   (c) 閾値でブロックを切る、のいずれか。x = q·r は r について単調なので (c) が素直
3. **途中リスケール** (`abs(jc) > 1e250` で 1e-200 倍) はレーンごとに条件が違う。
   ⚠ 一括リスケールは**危険** (codex 指摘): 下方漸化でも l ≲ x では振動と零点で
   |jc| は単調でなく、レーン間で桁が大きく違いうる。最大レーンが発火したとき
   小さいレーンが 1e-350 → 0 にアンダーフローする。**レーン別マスクで縮小**し、
   係数は 1e-200 でなく **2 の冪** (指数部だけ動き丸め誤差ゼロ) を使う
4. **配列レイアウトを (tile, λ+1) へ転置**。現行 `jl_tab` は (λ+1, tile) で、
   固定 λ の動径走査がストライド λ+1 になり、**生成側と内積側の両方に不利**。
   転置すれば SIMD store も後段の内積も連続になり、レーン別リスケールも連続。
   さらに規格化 (`out .*= scale`) を R 積分に融合すればメモリ往復を 1 往復省ける
6. **λ 外側・動径点内側のバッチカーネル**にする。`invx[i] = 1/x[i]` は漸化前に
   一度だけ計算し、`@simd` は動径ループにのみ付ける (依存のある λ ループには付けない)
7. **対数格子の再利用** (codex 指摘、要検証): 動径メッシュは A 区間が対数等間隔、
   B 区間が線形の**混成**。A 区間だけなら x = q·r の相異なる値は
   n_q×n_A ではなく ~(m·n_q + n_A) 個で済む (q の比を dt_log と整合させる必要あり)。
   試算で球ベッセル評価 6.2M → 1.2M = **5 倍削減**、全体 1.9 倍。
   ⚠ q グリッドを変えるのでビット同一性は失われる
5. **依存を増やさない**。`SIMD.jl` や `LoopVectorization.jl` に頼る前に、配列を
   組み替えて `@simd` / `@inbounds` で LLVM に自動ベクトル化させられないか試す。
   Temari の「依存ゼロ」は設計原則 (§3) なので、外部パッケージは最後の手段

**⚠ ビット同一性を失う**: 総和順序が変わるため、既存テーブルとはビット一致しなく
なる。§6 の規律どおり **テーブル全再生成とセット**で実施し、manifest に宣言する。
逆に言えば、次のテーブル世代 (v4) を作るタイミングと合わせるのが最も無駄がない。

**検証**: selftest T0 (球ベッセル vs scipy) と refcheck を通したうえで、
SIMD 版とスカラー版の差が求積誤差 (~1e-6) より十分小さいことを確認する。
`sph_jl_all!` 単体のマイクロベンチと、`compute_channel` のエンドツーエンド両方で測る。

**やらないこと**: **GPU 化**。作者機は RTX 4060 で、コンシューマ Ada の FP64 は
FP32 の 1/64 (0.24 TFLOPS) しかなく、Ryzen 9950X の AVX-512 (約 2.2 TFLOPS) に
**9 倍負ける**。FP32 へ落とす道も、Miller 漸化が 1e-250 まで振れる (1e-200 倍の
リスケールを挟む) ため不可。加えてホットスポットが「逐次依存チェーン」と
「メモリ律速の内積」で、どちらも GPU の不得手。FP64 が速いカード
(A100/H100/MI 系) が前提にできるまで検討しない。

P2 の完了までは **ReciPro 側 (`tools/IonizationGen/handout/`) が正本**。v3 テーブルの
本番生成が走っている間はそちらを触らない (2026-08-04 時点で生成中)。生成完走後に
Temari へ移し、以後は Temari が正本になる。

## 9. 公開に向けて

- 実装の大半は AI (Anthropic Claude) が行い、**処方の選択と検証は瀬戸が行った**旨を
  README に明記する (現行 handout/README.md と同じ姿勢)
- Bote–Salvat 係数は NIST BoteSalvatICX.jl (Unlicense) からの機械抽出。原典 2 報の
  引用を求める文言を残す
- 開発時の比較検証に使った Oxley–Allen 2000 の論文表と µSTEM (GPL) の公開データは
  **リポジトリにもテーブルにも一切含めない**
- 論文化するなら「公開された近代的・相対論的 GOS が存在しない」という空白が
  最も訴求力がある

## 10. 関連

- 現行コード: `C:\Users\seto\source\repos\ReciPro\tools\IonizationGen\handout\`
  (`ionization.jl` / `ionization.py` / `gen_production.jl`)
- ReciPro 側の設計正本: `.project-guidance/ReciPro/ReciPro_STEM-EDX設計.md`
- 大塚さんへの提供パッケージ: `tools/IonizationGen/handout/` 一式 (README 同梱)
