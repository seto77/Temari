# 次チャットへの指示書 (2026-08-16 09:40 作成。フリート走行中)

⚠ 本書は「次に何を、どの順で、どう気をつけてやるか」だけを書く。根拠は正本へ:

| 正本 | 内容 |
|---|---|
| **`src/gen_factors.jl`** (ヘッダ) | ★ 出荷処方・生成時ゲート G0–G5・provenance の規約 |
| **`tools/temari_factors_contract.py`** | ★ 実行可能な loader 契約 (Python 参照 loader + 負のミュータント 17 種) |
| `tools/factors_loader.jl` / `tools/check_factor_tables.jl` | Julia 参照 loader と QC F1–F10 |
| `schema/temari_factors_v1.schema.json` | X1 の schema |
| `tools/make_factors_manifest.jl` / `make_factors_release.sh` / `factors_regen_check.sh` | X14 (manifest・決定論的梱包・再現性) |
| `docs/scattering_factor_dataset_plan_2026-08-10.md` §4.17–4.23.8 | 予算・処方・作者決定 |
| `docs/next_chat_2026-08-15.md` | 前日の指示書 (§2-1〜2-3 済み。§2-3 の残り = 本日実施、§2-4 = 本日フリート化) |

## 1. 走行中のフリート (直列 2 段。08-16 09:23:01 発火)

`c:/tmp/temari_factors_2026-08-16/launch_fx.sh` が taskscheduler ジョブ **`Temari_factors_v1`**
から発火し、frozen worktree **`c:/tmp/temari_factors_2026-08-16/repo` (commit `3c9d691`、
指紋 `f1793942…`)** のコードで直列に:

1. **dataset-factors v1 の出荷生成** (86 元素、8 レーン、重い順、3 pass skip 機構) →
   出力 = **`src/prod_factors_v1/SF_Zxxx.json`** (+ `runlog/`)。見込み 3〜5 h (~13:00–14:00)
2. **端点切断 2 段目** (`run_ep2_fleet.sh`) → `c:/tmp/temari_endpoints2_2026-08-16/`。
   極端例 6 元素 (Rn/Au/Cs/Na/Li/H) を **dt/16 で deep** (r0/10・r0/100・rmax×1.5・rmax×2、
   tight、ラダー) + 標本 14 元素を粗 dt で deep。見込み 6〜9 h (Rn/Au 律速。~22:00)

進行は `launch.log` の「終了」行と `fleet.log` / `ep2.log` の pass 行。**PC 再起動があっても
タスク登録は生き残る** — `schtasks /Change /TN Temari_factors_v1 /ST <数分後>` でトリガーを
引き直せば skip 機構が続きから回す (8/14 の実績と同じ)。watchdog は報告のみ (`fx_watchdog.log`)。

⚠ **フリート中に主ツリーの `src/` を触るのは自由** (worktree が frozen)。ただし
`tools/certify_*` は ep2 が worktree のものを使うので主ツリーで直しても走行には無関係。

## 1.5 ⚠⚠ run 2 (86 元素の再生成) — 20:00 に予約済み。**出荷は run 2 の出力**

run 1 (worktree `repo` = `3c9d691`) は **Cs (Z=55) が G3 の副ゲート (K⁶ 項の見積り ≤ 1e-10 Å) で
不合格**になった — 残差 1.29e-10 Å は主閾値 1e-9 の 1/8 で、K⁶ 項そのもの (級数打ち切り。値の
問題ではない)。私の閾値設計ミス。G3 を **M₈ 項込みの 4 項展開との比較 (残差 ≤ 1e-11 Å)** に直した
(`f27ed05`、実測 Cs 1.8e-14 / Au 1.8e-15 / H 0)。単一の指紋を保つため、**新 worktree
`c:/tmp/temari_factors_2026-08-16/repo2` (commit `f27ed05`、指紋 `f8e829b7…b3a9`) で 86 元素を
再生成する run 2** をタスク `Temari_factors_v1_run2` (20:00) に予約した。
出力 = **`src/prod_factors_v1_run2/`** (run 1 = `src/prod_factors_v1/` は 85 元素、比較材料)。
台本 = `run_factors_fleet_run2.sh` / `launch_run2.sh` (中身検査つき skip、完了数で exit code)。
見込み 3.5 h (端点 2 段目の重い 2〜3 本と並走) → 23:30 頃。

**完走後の順序**: (a) `check_factor_tables.jl src/prod_factors_v1_run2 --certify-dir … --tight-extra
c:/tmp/temari_tight_extra_2026-08-16 --expect-fingerprint f8e829b7d473597359fa5a20b48968ddc7d5023ad55084939802b314d916b3a9`
→ (b) **run 1 vs run 2 の f_x/f_e 配列が 85 元素で byte 一致**することを確認 (X14 の別プロセス・
別時刻の再現性。指紋・gates 欄だけ違うはず。`python -c` で配列を比較) → (c) `mv src/prod_factors_v1
src/prod_factors_v1_run1 && mv src/prod_factors_v1_run2 src/prod_factors_v1` (MANIFEST.md 草稿は
run 1 側にあるので run 2 へ移す) → (d) 以下 §2 のとおり。

## 2. 出荷生成の完走後にやること (順)

### 2-1. QC を通す (完走 ≠ 健全)

```bash
julia tools/check_factor_tables.jl src/prod_factors_v1 \
      --certify-dir c:/tmp/temari_certify_2026-08-11 --tight-extra c:/tmp/temari_tight_extra_2026-08-16 \
      --expect-fingerprint f8e829b7d473597359fa5a20b48968ddc7d5023ad55084939802b314d916b3a9   # run 2 の指紋
```
- `--tight-extra` = Yb (70) の tight (τ/10) 参照 (v1 認証はラダー前で未収束、L¹ 走は f_x 未保存)。
  `tools/fleet/tight_extra.jl` で 08-16 昼に補完 (`c:/tmp/temari_tight_extra_2026-08-16/tight_z070.json`)。
  無ければ F8 が Yb を「検査不能」として落とす (黙って通さない)
- F1 86/86・F2 指紋一意・F5 MB 恒等式・F6 ゲート台帳・**F8 認証副本との突き合わせ**が全部通ること。
  ⚠ F8 は 2026-08-16 昼に組み直した (§4 の発見): F8a 同一解/別反復 [記録] / **F8b 出荷 f_x の
  停止誤差 |ship − tight| + 0.10×B_scf ≤ B_scf** [ゲート] / F8c 認証 prod との差 [記録] /
  **F8d 出荷 f_e の停止誤差 + 認証の f_e 格子誤差最悪 3.817e-8 + allowance ≤ B_num,e** [ゲート]
  (f_e には grid/scf の個別配分が無い。B_scf を流用すると H が落ちる — 実測値であって欠陥ではない)。
  17 元素時点で F8b 0.353× / F8d 0.421× (ともに H が最悪)
- 落ちた元素があれば**原因を見てから**。GC クラッシュ痕は `logs/SF_Zxxx.log`
- `python -c "import json,jsonschema,glob..."` は release script が回すので個別には不要

### 2-2. golden vector を凍結 (X13)

```bash
PYTHONUTF8=1 python tools/temari_factors_contract.py src/prod_factors_v1 --make-golden schema/factors_golden_v1.json --negative
julia tools/check_factor_tables.jl src/prod_factors_v1 --golden schema/factors_golden_v1.json   # 言語間 (F9)
git add schema/factors_golden_v1.json && git commit
```
- 元素 = C/Fe/Cs/Au、節点外 12 点 + 節点 4 点、許容 相対 1e-12 (⚠ 緩めない — 端条件の違いは
  H で 1e-10 しか無い)。負のミュータント 17/17 検知を確認してから凍結

### 2-3. X14 (再現性) と manifest・MANIFEST.md

```bash
REPO=c:/tmp/temari_factors_2026-08-16/repo bash tools/factors_regen_check.sh 1 26   # byte 同一
julia tools/make_factors_manifest.jl src/prod_factors_v1
```
- `src/prod_factors_v1/MANIFEST.md` を書く (人向けの正本: 処方 / 生成の記録 (開始・終了・
  再起動・再試行元素) / QC の数値 / 認証へのポインタ / Julia 1.12.6 のピン留め / commit と指紋)。
  F の `src/prod_v5_jl/MANIFEST.md` を雛形に

### 2-4. 端点切断 2 段目の報告 (完走後。⚠ **公開の前に済ませる** — codex)

`PYTHONIOENCODING=utf-8 python tools/endpoints_aggregate.py c:/tmp/temari_endpoints2_2026-08-16 --md /tmp/ep2.md`
で集計し (⚠ `certify_endpoints.jl --aggregate` はヘッダにあるだけで未実装)、
`docs/endpoint_truncation_2026-08-14.md` に §5 として追記 (dt/16 での予算比、飽和比、ラダー試行、
重複 5 元素は dt/16 側を正)。
⚠⚠ **読み方 (08-16 に私が一度誤読した)**: ツールが保存する差は**どちらも基準格子との差**
(|f(r₀/10)−f(r₀)| と |f(r₀/100)−f(r₀)|) なので、切断誤差 E(r₀) が r₀→0 で消えるなら**両方 ≈ E(r₀)
で比 ≈ 1 (飽和) が正常**。比 ≫ 1 は未飽和。「比 < 1 = 縮む」を期待しない。基準格子の切断誤差の
上界 = max(Δ)。差が **tight 2 解の停止ゆらぎ (~5e-10、τ/100 診断 U′ ≤ 2.7e-10 の 2 倍) 以下**なら
比は意味を持たず「切断効果 ≤ 停止ゆらぎ」と書く。17/20 時点の速報: r₀ 側の上界 0.0087×B_grid
(Au 粗 dt)、r_max 側は Li dt/16 の 6e-11 以外厳密 0、比が意味を持つ元素は無し (全部床以下)。
事前指定どおり数値をそのまま書く (結果で変えない)。
`ep2.log` の末尾が `ep2 fleet done: 20/20` でなければ (INCOMPLETE)、`schtasks /Change` で再発火
(skip 機構は中身検査つき)。

### 2-5. 梱包と release

```bash
bash tools/make_factors_release.sh src/prod_factors_v1 dist     # 2 回組んで同 SHA + 展開検証 (Julia QC 込み)
```
- release タグは **`dataset-factors-v1.0.0`** (コード release とも `dataset-v5.0.0` とも別)。
  Zenodo は作者判断
- `docs/factors_release_README.md` の数値 (0.16×/0.33× 等・「H と Fe で再現」) が最終 QC /
  2-3 の再現性検査と食い違えば直す。**runlog/ は archive に入れない** (決定論の主張と両立させるため)

### 2-6. 文書の締め

計画書 §4 の X 表 (X1/X13/X14 → ✅)、§6 の作業順 8–9、引き継ぎ書ヘッダ、CLAUDE.md、
mkdocs (Data ページに factors を足す) — F dataset の公開時と同じ手順。

## 3. ⚠ 運用規律 (前日から変わらず + 本日の追加)

- 起動 = タスクスケジューラのトリガー自然発火 + ASCII VBS。nohup と手動 Start は死ぬ
- **出荷生成は frozen worktree から**。主ツリーで src/ を触っても走行中の生成には
  効かないが、**再生成 (X14) は同じ commit の checkout で行う** (指紋が変わる)
- **codex に節目で相談し、完了済みの項目も再確認させる** (作者指示 2026-08-16)。
  本日のレビュー結果 = 欠陥 10 件 (全部対処済み) は本書 §4
- golden の許容 1e-12 / 契約テストの丸め包絡は**緩めない**。緩めるなら理由を書いて次版で
- 和文 md の強調は行内で閉じる。検査 = `PYTHONIOENCODING=utf-8 python tools/md_emphasis_check.py <files>`

## 4. 本日 (08-16) にやったことの要約

- ⚠⚠ **発見 (10:00–10:40): 同じ commit・同じ Julia・同じ `solve_prod` 手順でも、SCF は認証 (08-11) の
  解と別の反復で止まることがある** — 17 元素中 同一解 10 / 別反復 7 (Z=71,74,75,78,79,82,86。
  Au: 固有値 相対 1.9e-10、f_x 最大差 6.7e-10 = 最下位桁 2 単位)。**単発プロセスでの再生成 (Au、
  worktree) は出荷と byte 同一** ⇒ 生成器は同機・同 commit・単発プロセスで再現可能で、認証プロセス
  (16 レーン、tight 3 水準の後に同一プロセスで prod) の文脈依存。コード上に未初期化配列・グローバル
  状態・スレッド並列は見当たらず、KLI の 22×22 LAPACK 解は BLAS スレッド数非依存を確認。原因未特定。
  **codex 推奨の診断 = 「同一プロセスで Au を 2 回」(実行中 `c:/tmp/temari_factors_diag/au_twice.jl`)
  → 次に「tight5 → prod5 を同一プロセスで」(2.5 h) を反復ごとの ρ hash・KLI 行列 hash・固有値括弧を
  ログしながら**。⚠ 停止許容内 (F8b/F8d で予算内) なので出荷には影響しないが、「決定論的」の
  主張は「同機・同 commit・単発プロセス」に限定して書く (MANIFEST.md 草稿・README に反映済み)
- スマホ用の進捗ページ (Artifact) = https://claude.ai/code/artifact/d4e8bfb1-b628-49a5-9d1f-9a0e91bf5fc2
  (30 分ごとに更新した。フリート完走後は最終状態のまま)

- **codex レビュー 2 回目 (09:50)** の欠陥 12 件: 対処済み = loader の通常読み込みでの検証
  (dataset/schema/charge/内部 z/格子 SHA/長さ/有限) / 11 桁閉包の全数検査 / NAK 検査は
  「区間 0 の 3 次式を x₂ へ延ばして**値と傾き**を再現」(C² 前提なしで NAK と同値) /
  golden の完全性 (元素集合・s 点列・許容・SHA・model_id/版/指紋) / 再丸めミュータント /
  **SciPy CubicSpline (独立実装) との突き合わせ 3.3e-16** (許容 1e-12 の根拠) / manifest の
  全キー深い照合 / release から runlog を除外・tar mode 正規化・展開物に Julia QC / F8 の表現
  「同じ解の経路」(ρ ビット同一の証明ではない) / ep2 台本の中身検査つき skip と exit code。
  **対処しない (走行中・凍結のため)** = 走行中の `run_factors_fleet.sh` のファイル名 skip
  (この走は新規ディレクトリ + 同一指紋なので実害なし。リポ版 `tools/fleet/` は修正済み) /
  `existing_is_current` の f_e 長・ゲート・収束の追加検査 (次版の生成器で) /
  gen_factors.jl の notes 文言「byte-identical with the same fingerprint and Julia version」
  (プラットフォーム条件が抜けている — README で補った。生成器は凍結中なので触らない)

- `src/gen_factors.jl` 新設 (認証 `solve_prod` と同一手順の SCF → `compute_fx(atom=)` →
  δ 形 f_e → 11 桁丸め → 決定論的 JSON + runlog 副ファイル)。パイロット H @dt/16 は
  認証 prod_stage5 と f_x が 11 桁丸めの範囲 (5.0e-12) で一致、norm_correction 17 桁一致
  (= ρ ビット同一)、再生成 byte 同一
- codex レビュー (00:10) の欠陥 10 件を全部対処: 揮発情報の分離 / include 閉包の指紋
  (+CRLF 正規化) / ゲート台帳 (G0–G5 の値・閾値・合否) / cfg 断言・-t 1 強制 / 予算の等式 /
  G3 の M₈ 打ち切り見積り / skip 前の版・指紋検査 (stale 退避) / LE 明示 / dirty の別欄 +
  hard fail / 中性不変量
- 契約・QC・manifest・梱包・再現性の道具一式 (X1/X13/X14 の実装。golden の凍結だけ完走待ち)
- 契約テストの解析検査で **t 格子の最初の区間 (h_t ≈ 6e-7) は 2・3 階微分が丸めで悪条件**
  (相対 1e-6 / 3e-4 の床) と分かり、NAK 検査を「区間 0 の 3 次式を x₂ へ延ばして y₂ を再現」
  (良条件) に置き換えた。⚠ 規約違反ではなく検査の条件数の問題
- 負のミュータントで **NAK/NAK f_x (H で相対 1e-10) と s 上 f_e (6e-11) は 1e-9 では
  検知できない**ことが分かり、golden 許容を 1e-12 (言語間の実測 0.0) に締めた
