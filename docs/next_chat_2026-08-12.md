# 次チャットへの指示書 (2026-08-12 12:30 作成)

⚠ **本書は「次に何を、どの順で、どう気をつけてやるか」だけ**を書く。
根拠と実測値は正本を読むこと:

| 正本 | 内容 |
|---|---|
| **`docs/grid_certification_run_2026-08-12.md`** | ★ **全 Z 認証の実行報告**。結果・欠陥・裁定・L¹・障害・私の誤り |
| `docs/scattering_factor_dataset_plan_2026-08-10.md` | トラック全体の正本 (§4.22 格子 / §4.23 f_e) |
| `docs/handoff_scattering_factor_2026-08-11.md` | 現在地と作業順、**踏んだ罠の表** |
| `docs/grid_certification_preregistration_2026-08-11.md` | 事前登録 (⚠ **本文は書き換えない**。実行後の追記は冒頭の枠) |

---

## 0. 冒頭で確認すること (30 秒)

```bash
# 計算が終わっているか
ls c:/tmp/temari_certify_2026-08-11/z0{68,69,70}.json 2>/dev/null
cat c:/tmp/temari_certify_2026-08-11/remaining.log

# スケジューラのジョブ
powershell -NoProfile -Command "Get-ScheduledTask | Where-Object {\$_.TaskName -like 'Temari*'} | ForEach-Object { \$_.TaskName + ' : ' + \$_.State }"

# リポの状態 (作業ツリーが汚れていないこと)
cd c:/Users/seto/source/repos/Temari && git log --oneline -3 && git status --porcelain
```

**2026-08-12 12:40 時点の状態**

| | |
|---|---|
| 認証済み | **83 / 86 元素** (出力 = `c:/tmp/temari_certify_2026-08-11/`) |
| **計算** | ⚠ **走っていない。作者の指示でこのチャットでは開始しない** |
| 未完 | **Er 68 / Tm 69 / Yb 70** — ⚠ **次チャットで開始する** (§0.1) |
| 最後のコミット | `890b853` |
| 8/13 01:00 | タスク `Temari_dt32_allZ` が **Ready**。⚠ **中身の選択が未決 (§2)** |
| **並列数** | ⚠ **作者の指示で 16 → 8 レーンに下げた** (フリート 3 本とも `LANES="${LANES:-8}"`) |

### 0.1 ⚠⚠ 長時間計算の起動方法が**まだ確立していない**

**3 回試して 3 回とも途中で死んだ。**⚠ **機構は特定できていない。**

| 走 | 起動方法 | 生存 | 終了コード |
|---|---|---|---|
| 1 | `nohup` (Bash ツール内) | **15 時間** (8/11 17:55 → 8/12 09:14) | セッション終了と同時 |
| 2 | `nohup` | 9 分 | — |
| 3 | **タスクスケジューラ (Interactive)** | **4.5 分** | **`0xC000013A` = STATUS_CONTROL_C_EXIT** |

⚠ **走 1 は私のツール呼び出しを 15 時間くぐり抜けている**ので、
「ツールを叩くと死ぬ」ではない。**09:14 のセッション再開を境に何かが変わった**
とみるのが自然だが、⚠ **確かめていない**。

⚠ **「スケジューラ経由なら生き残る」という私の判断は実測で否定された** —
`0xC000013A` は Ctrl+C 相当で、**対話セッションのコンソールに巻き込まれている**。

⚠ **S4U (ログオン状態に関わらず実行) での再登録は「アクセスが拒否されました」で失敗**した
(管理者権限が要る)。

**⇒ 次チャットで最初にやること: 起動方法を 1 つ決めて、まず 10 分の試走で確かめる。**
候補:

1. **管理者権限の PowerShell で S4U 登録** (非対話セッションで走る。最も確実)
2. `schtasks /create /RU <user> /RP <password>` でパスワード付き登録
3. **作者が手で起動する** (⚠ 私のセッションと無関係になる。確実だが手間)
4. Windows サービス化 (⚠ 大げさ)

⚠⚠ **10 分の試走で生存を確認してから本番を投げること。**
**4 時間走らせてから死んでいたと気づくのが一番高い。**

---

## 1. ⚠⚠ 最初に読むべき「この作業の性質」

**採用格子 dt/16 は、83 元素すべてで空間離散化予算の 22 % 以下**だった
(最悪 Z=71 Lu で 0.217 × B_grid)。**これは動かない結果である。**

⚠⚠ **しかし事前登録した合否判定は 46/83 しか通っていない。**
落ちた 37 元素も上限・停止誤差は余裕で、原因は**私が置いた点ごとの収縮検定
(q ゲート) の設計欠陥**である。codex の裁定:

- `violating = 0` は**事前登録した合否条件そのもの**。「分類は主軸ではない」は通らない
- ⚠⚠ **「上限の数値は分類と独立だから上限の主張は生きる」も否定された** —
  上限を「保守的な尾の上界」と呼ぶ根拠が**収縮確認に依存している**ため
- **規則を修正すれば、見た 83 元素についての事前登録は死ぬ**。
  修正規則は**別の事前登録として凍結し、前向きに走らせる**こと
- 一次結果は「格子が不適格」ではなく「**当初の手順では認証に失敗した**」

⇒ 次チャットの主題は、
**修正した認証規則を凍結して、前向きに走らせること**である。

---

## 2. ★ 最優先の作者判断 — 8/13 01:00 の窓で何を回すか

タスク `Temari_dt32_allZ` が **2026-08-13 01:00 に発火する**。
**起動スクリプトのパスを差し替えるだけで中身を変えられる** (登録は据え置きでよい)。

⚠⚠ **何もしなければ案 A (dt/32、22 h) がそのまま走る。**
⚠ しかも §0.1 のとおり**起動方法がまだ確立していない**ので、
**01:00 に発火しても数分で死ぬ可能性が高い**。
⇒ **§0.1 を先に片付けてから、§2 を決めること。**

| 案 | スクリプト | 所要 | 得られるもの |
|---|---|---|---|
| **A (登録済)** | `c:/tmp/temari_certify_2026-08-11/run_extend_fleet.sh` | 約 22 h | 全 Z の dt/32 で点ごとの δ₄。⚠ **同じ悪条件の推定量が 1 つ増えるだけ** |
| **B (推奨)** | `c:/tmp/temari_certify_2026-08-11/run_l1_fleet.sh` | 約 14.5 h | **密度 L¹ の 4 水準**。s に一様な絶対上界 + 収縮比 2 個 |

**B を推す理由** (実行報告 §4):

- 今回の失敗は「点ごとの比 q が悪条件」だった。dt/32 を足しても
  **q = |δ₄|/|δ₃| という同じ形の推定量**が増えるだけで、零点も低信号も相殺も残る
- L¹ は**相殺が無く、零点が無く、s に一様**で、**元素あたり 1 スカラー**
- 3 元素の実測で収縮比 **0.248 / 0.251 / 0.252**、界は sup の **2.5〜2.8 倍**
- ⚠⚠ **点ごとで 993 点違反した Xe が、密度レベルでは 0.251 で縮む**

**B を選ぶ場合の手順**:

```powershell
# 起動スクリプトを差し替える (タスクの再登録)
$bash = "C:\Program Files\Git\bin\bash.exe"
$action = New-ScheduledTaskAction -Execute $bash -Argument '-lc "bash /c/tmp/temari_certify_2026-08-11/run_l1_fleet.sh"'
$trigger = New-ScheduledTaskTrigger -Once -At ([datetime]'2026-08-13 01:00:00')
$set = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable -ExecutionTimeLimit ([TimeSpan]::FromHours(20)) -MultipleInstances IgnoreNew
Register-ScheduledTask -TaskName 'Temari_dt32_allZ' -Action $action -Trigger $trigger -Settings $set -Force
```

⚠⚠ **どちらを回すにせよ、走らせる前に規則を凍結してコミットすること。**
既に見た 83 元素は、規則変更については**開発データ**である。

⚠ **B には未解決の設計事項が 1 つある** — **未収束元素 (Tm 69 / Yb 70) の扱い**。
L¹ 走も tight (τ/10) を使うので同じ問題が起きる。§3-2 で決めること。

---

## 3. 作業の順序

### 3-1. ⚠ まず: 残り 3 元素の結果を見る (計算が終わっていれば)

```bash
julia tools/certify_grid.jl --aggregate c:/tmp/temari_certify_2026-08-11
julia tools/certify_fe.jl  c:/tmp/temari_certify_2026-08-11
```

⚠ **Tm 69 / Yb 70 は tight に届かず判定 ❌ が確定している。**得られるのは
「第 3 水準でも未収束か、drho がどう動くか」という**原因調査の材料**だけ。
⚠ **認証には使えない** (未収束の解で測った格子差は停止誤差と混ざる)。

### 3-2. ★ 修正した認証規則を設計し、**事前登録し直す**

**主軸を「点ごとの比」から「密度の L¹ 界」へ移す。**

$$E(dt/16)(s) \le \sum_{k\ge4}\|\Delta\rho_k\|_1 \le \frac{\|\Delta\rho_4\|_1}{1-q^*} \quad (\text{全 } s)$$

実装は `tools/certify_l1.jl` (4 水準 dt/4…dt/32、tight のみ)。
⚠ **欠落の突き合わせ**と**規格化の検算**は既に入っている。

**凍結する前に決めること**:

1. ⚠⚠ **未収束元素の扱い** — 候補: (a) その元素だけ閾値を到達可能な値へ緩めて記録する
   (b) `max_iter` を上げる (c) β を下げる (d) production 閾値で測り停止誤差を別勘定で
   大きく取る。⚠ **どれも「その元素だけ別扱い」なので、規則に明記してから走らせる**
2. **点ごとの比を診断に降ろす**か、三分類 (`verified` / `violated` /
   `unresolved_low_signal`) にするか。⚠ codex は**三分類 + 検定不能点の別途認証**を推した
3. ⚠ L¹ 界が重元素で十分きついか — **3 元素しか測っていない**。
   `‖Δρ₃‖₁/B_grid` は 0.029 → 0.121 → 0.301 と Z とともに増える。
   ⚠ **単調性を仮定して外挿しない**

⚠⚠ **凍結したら、走らせるまで 1 文字も変えない**。
変えたら「開発データへ移した」と明記する。

### 3-3. f_e の切替点を直す (⚠ T_comp,e はその後)

⚠ **水素で切替点の経路整合が |比−1| = 13.7** と外れている (合格は 5 % 以内)。
解析規則 $K_{sw}^2 = 2|\delta M_2/\delta M_4|$ の **[0.005, 0.2] への丸め**が
効いた可能性がある。⚠ **直すまで T_comp,e の値を置かない。**

実測済み (83 元素): 格子誤差の最悪 **Z=65 で 3.817e-08 Å**、停止の最悪
**Z=62 で 2.567e-08 Å**、**最大誤差の位置は中央値 s = 0** (δM₂ が支配的)。

⚠ アンカーは**用途**にする。**モデル差は sanity check に留める** (codex)。
⚠⚠ **$K^2f_e$ を契約対象にしない** — K→0 で重みが消え f_e(0) の誤差を無制限に許す。

### 3-4. 片付け (どれも短い)

| | 作業 | ⚠ 注意 |
|---|---|---|
| 1 | **`pointwise_bound` の複製を統合** | `certify_fe.jl` の `pointwise_bound_diffs` は `certify_grid.jl` の複製。フリート実行中は触れないので分けた。**判定を作る場所を 2 つに増やしたままにしない** |
| 2 | **集計に欠落の突き合わせを足す** | ⚠ 現状は Z が欠けても沈黙する。`certify_l1.jl` には入っている。**負のテストで「1 元素抜くと落ちる」ことを実演してから** |
| 3 | 端点切断 (r₀ / r_max) | `tools/certify_endpoints.jl` は実装済・未実行。⚠ **dt を細分しても測れない別の誤差源** |
| 4 | 標本 14 元素の `--prod-all` と τ/100 | 事前登録 §4.1 |

### 3-5. 未着手のトラック

**OA 2000/2001 による独立検証** (引き継ぎ書 §6)。申し送りと変換仕様は揃っている
(`c:\tmp\Temari_申し送り_OxleyAllen検証_2026-08-11.md` /
`c:\tmp\TaskA_Oxley2000_Temari_conversion.md`)。
⚠ **F(s, E₀) 側の計算が要る**ので、認証フリートと同時には走らせられない。
⚠ **OA の数表をリポへコピーしない** (ライセンス。比較は scratchpad で)。

---

## 4. ⚠⚠ 運用上、必ず守ること (今回すべて実際に破って痛い目に遭った)

| 規律 | なぜ |
|---|---|
| ⚠⚠ **長時間計算の起動方法はまだ無い** | `nohup` も**タスクスケジューラ (Interactive) も途中で死んだ** (§0.1)。⚠ **「スケジューラなら大丈夫」と一度書いたが、実測で否定された**。**10 分の試走で生存を確認してから本番を投げること** |
| **並列数は 8 レーン** | ⚠ 作者の指示 (2026-08-12)。16 レーンは負荷が大きすぎた。フリート 3 本とも `LANES="${LANES:-8}"`。⚠ **8 レーンでのコストは未実測** — 16 レーンでの数字をそのまま使わない |
| **フリート実行中にリポジトリを触らない** | `certify_grid.jl` の `tool_sha()` は **JSON を書く瞬間にファイルを読む**。途中で変えると**実際に走ったコードと違うハッシュ**を記録する |
| **失敗した元素は必ず再試行してから性質を語る** | GC クラッシュ 3 件・KLI の NaN 3 件は**すべて非決定論的**で、再試行で全部成功した。⚠ **1 回の失敗を元素の性質と読むな** |
| **監視の前提を壊す操作をしたら、監視を見直す** | 優先度を BelowNormal へ下げたことで watchdog の前提が壊れ、**健全な 20 プロセスを kill した** (約 51 CPU 時間の損失) |
| **wedged の判定は相対で** | 「他プロセスの中央値が明確に正のときだけ疑う」。⚠ **絶対量の判定には二度と戻さない**。実績 = 7 時間 誤検出ゼロ + 真の wedge 1 件検出 |
| **強調の修正は手でやる** | 一括の正規表現は `**A** … 、**B**` の**A の閉じと B の開きを対応させて壊す**。⚠ **今回で 2 回目**。判定は `tools/md_emphasis_check.py` |
| **1 元素・途中集計から全体を語らない** | 「q > 1 は 0 点」(20 元素) → 全 83 元素では **187 点**。「格子を細かくすれば収束する」(Yb) → **Tm で崩れた** |
| **コストの数字に「何レーンで測ったか」を添える** | 単独では n_r に線形、16 レーンでは **2.5 倍遅い**。今回コスト見積りを 4 回外した |
| **本番生成の直前に必ず commit する** | `gen_production.jl` は dirty なら `-dirty` を付ける。認証ツールも同様に来歴を記録する |

---

## 5. ⚠ この認証が答えていないこと (主張を書くときに必ず併記する)

- **s > 6 Å⁻¹** / **節点と節点の間** / **補間・loader・丸め** (別契約 B_repr)
- **Z > 86・イオン・励起状態** / **Dirac + KLI 処方そのもののモデル誤差**
- **r₀ / r_max の切断誤差** (dt を細分しても測れない。別試験・未実施)
- **3 水準に共通する誤差** (差分法の原理的な死角。負のテスト D2 で実演済)
- ⚠ **未完の 3 元素**。うち Tm・Yb は**未収束で認証に使えない**
- ⚠⚠ **事前登録した合否判定は 46/83 しか通っていない**
- ⚠ **L¹ 界による覆いは、まだ 3 元素でしか測っていない探索的な設計**

**使ってよい言い方** (codex の起草):

> Z = 1…86 の全元素について、出荷 7681 節点上で事前登録した多段収束試験を実施した。
> 各離散化段の解析解に対する 2 次収束と、全元素の点別の符号付き収縮に基づき、
> 将来段の収縮率を q ≤ 0.5 とする保守的な尾の評価のもとで、dt/16 の推定残差が
> B_grid = 6×10⁻⁸ 電子以下であることを確認した。

⚠ 「厳密上界」「数学的保証」「連続 s ∈ [0,6] 全域を証明」は**使わない**。
