# 次チャットへの指示書 (2026-08-20 夜に執筆。次のチャット = 08-24 頃の想定) — 08-22/08-23 指示書の残りを実行した結果

**⭐ 冒頭でこれを読む**。前段 = `docs/handover/next_chat_2026-08-23.md` (部分波 v6・pilot v4・ε ノードの発見)。
作者の指示「08-22 の指示書を読んで、Codex と深く議論しつつ、慎重に粘り強く最後まで作業を進める」(2026-08-20 17:1x〜)
で進めた。⚠ 本書には**実測**と**作者指示・作者判断**と**私 (Claude) の解釈・作業仮定**が混ざる。区別して書いた。

## 0. 八行で

1. ★★★ **作者判断 4 件 (v6 の範囲) は起草の推奨を「作業仮定」として採用した** (§2)。等比 s 格子は束ねない /
   **n1 20 → 40** / Python 参照実装を v6 に移植 / v5 への注記は ERRATA 別立て。**deep 認証は起動していない**
2. ★★★ **版の名乗りを「承認済み spec の canonical JSON の生バイト SHA-256」に置き換えた** (§3)。
   `spec/temari_dataset_v6.0.0.spec.json` (sha `749fadc5af79c975…`) + `spec/RELEASES.json` + E₀ 目録 (525ch / 14,796 行、
   Float64 の bit 表現)。本番入口は fail-closed (`--profile v6_high`・repo 外の `--out`・legacy ENV 拒否・
   `--allow-dev` は 0.0.0-dev 固定)。検査側 C16b は独立に読む (三値)。負のテスト 37 件
3. ★★ **コミットの連鎖** (各段にビット同一ゲート、両側空キャッシュ): 1 挙動不変 `8e5ad5b` → 2 n1+profile `e1f25f5` →
   3 spec/fail-closed/C16b → 3b heartbeat + watchdog `--profile` → 4 Python 移植 + refcheck (§4)
4. ★ **メモリ交換 (4 枚 @4400 → 2 枚 @5200、63.7 GB) のベンチ**: E1 (同一 commit・同一ジョブ) は 08-05 と ≤ 3.4 % で一致 =
   **高速化なし**。STREAM は 1 スレッドで飽和 (帯域律速ではない)。再起動後の全コア boost 代理は −15 % だが E1 には
   一様に出ない。正本 = `../qcamp/bench_2026-08-20/README.md`
5. ★ **v5 errata を出した**: `src/prod_v5_jl/ERRATA.md` (部分波の処方感度 + ε ノード) + data.md (ja/en) に 1 項目。MANIFEST 不変
6. (起動の記録 — 実行時に埋める)
7. (残作業 — 実行時に埋める)
8. ⚠ 踏んだ罠: heredoc のバックスラッシュ (3 回目。memory 更新) / worktree は CRLF (編集スクリプトを改行非依存に) /
   SCF キャッシュ指紋に触る commit のゲートは両側空キャッシュ (memory `symmetric-cache-gate`) / `redirect_stdout(IOBuffer)` は
   Julia で使えない / Python の CHANNELS は K/L のみ (M 殻の参照値は作れない)

---

## 1. ★ 最初にやること

```bash
cd /c/Users/seto/source/repos/Temari
git log --oneline -12
cat ../qcamp/RUNBOOK_v6_launch_2026-08-20.md          # GO/NO-GO 表と起動手順 (実行時の記録つき)
cat ../qcamp/bench_2026-08-20/README.md               # メモリ交換のベンチ
ls ../qcamp/prod_v6_run1/ 2>/dev/null | head          # 走行中ならここに JSON が増える (INCOMPLETE がある間はデータセットではない)
```

## 2. 作者判断 4 件の扱い (作業仮定)

| # | 判断 | 採用 | 根拠 |
|---|---|---|---|
| 1 | 等比 s 格子 | 束ねない | v6 は model_id 不変の数値処方修正。形式変更 + ReciPro 改修 (schema 3 / formatVersion 5 / GridAt) は別世代 |
| 2 | ε ノード n1 | 20 → 40 全行一律 | `eps_nodes_threshold_2026-08-20.md`: Rn M5 6.0e-05 → 2.5e-07、+10 % 以下 |
| 3 | Python 参照実装 | v6 移植 | `lkin_rule` 必須引数、case ごと規則、K/L の v6 case 5 本 |
| 4 | v5 への注記 | ERRATA 別立て | `src/prod_v5_jl/ERRATA.md`、MANIFEST 不変 |

**作者が別の判断なら**: 再生成の起動前なら止めるだけ。起動後なら run dir (`../qcamp/prod_v6_run1`) を捨てる (1.5 日)。
deep 認証 (σ(β,Δ) v4、16 レーンで 14〜24 日) は**作者判断待ちのまま**。

## 3. 版の名乗り (V6_SPEC) の実装 — 正本 = `docs/notes/v6_spec_draft_2026-08-20.md` §0.5 + `tools/make_v6_spec.jl` 冒頭

- 生成側: `load_approved_spec` (registry の承認 SHA と生バイト一致でなければ nothing) → `settings_match_spec` (純関数:
  処方・model_id・schema・S_GRID bits・settings 全値・lkin・x_alpha・s_cert・tail・E₀ 規則・チャネル集合・**実行時に組み直した
  E₀ 目録の hash**・gates) → `presc_dataset_version` は "6.0.0" / "5.0.0" (検査側 `permit_legacy=true` のみ) / "0.0.0-dev"
- 本番入口 (`gen_production.jl`): 厳密な引数解析 / `--profile` 必須 / `--out` 必須かつ repo 外 / legacy ENV 拒否 /
  `--allow-dev` は出力を 0.0.0-dev に固定 / 既存ファイルは版・spec・文脈を検査してから skip / 文脈の違う partial は隔離 /
  run dir に `RUN_SPEC.json` + `SPEC_SNAPSHOT_*.json` + `INCOMPLETE`
- 検査側 (`check_tables.jl` C16b): 生成側の関数を呼ばず独自に読む。三値 (PASS / FAIL / SKIP)。`--expect-version 6.0.0` で SKIP も不合格。
  ファイル名↔中身、トップレベルのキー集合、settings のキー集合完全一致、Bool のすり抜け防止、E₀ 格子と s_cert を目録と bit 同一
- 負のテスト: `tools/c16_negative_test.jl` 37 件 (合成 6.0.0 の陽性対照 + 1 箇所ずつ + 適用性 + registry/spec 異常系 + 生成側の純関数)
- ⚠ **限界**: spec と registry を揃って間違えれば通る。これは drift と自己一致の検出であって正しさの証明ではない

## 4. Python 参照実装 (コミット 4)

- `src/ionization.py`: `compute_NK(..., lkin_rule=None, lkin_radius_frac=0.999, lkin_margin=12)` (None は ValueError)、
  `containment_radius` (Julia と同じ定義)、worker で v5/v6 分岐、CLI `--lkin v5|v6` 必須。selftest は `lkin_rule="v5"` を明示
- `src/selftest.jl` refcheck: case ごとの `lkin_rule` (無ければ error)
- `tools/make_reference_values.py`: v6 case 5 本 (Fe K @200 対照 / C K @400 / Ca L1 @400 / Ag L1 @200 / Au L3 @200)。
  **隔離した空 cwd** から走らせる (Python の SCF キャッシュはコード指紋を持たない)。provenance は case 単位
- `tools/lkin_rule_unit_test.py`: 合成配列の純粋規則テスト 16 件 (整数境界・含有半径の解析値・入口の拒否)

## 5. 監査・代表生成・リハーサル (2026-08-20 19:49〜21:0x、実測) — 詳細は `../qcamp/RUNBOOK_v6_launch_2026-08-20.md` §5

| ゲート | 結果 |
|---|---|
| 監査 (HIGH v6、10 case、`../qcamp/audit_v6c/`) | 部分波の振り (cap 320 + margin 20 + 含有率 0.9999 同時) は全 case ≤ 3.8e-07 (Zn M1 @400)、角度 ×1.5 ≤ 8.2e-08、n_q ×1.5 ≤ 4.4e-08、ppw ×1.27 ≤ 1.9e-07 (F)。⚠ **「eps nodes ×1.4」行は無効** (リテラル n1=28 = 下向きの振り。コミット 5 `4f7fc88` で修正) |
| ε ノードの上向きプローブ (`eps_n3_probe*.log`) | n1 40→56: Rn L1 @30 1.1e-07 / Rn L2 @30 4.8e-08 / **Rn M5 @30 2.4e-07 / @100 2.8e-07**、参照 (n1 80 + n3 40) も同値。n3 は ≤ 1e-09。⇒ n1=40 の残差は F で量子化半歩 5e-07 の下 (ε ノード note §0-4 と整合) |
| 代表生成 (3 スレッド、repo 外 `../qcamp/rep_v6`、`--profile v6_high`) | Fe K 28 行 (0.8 分/行) / Zn M1 22 行 (1.2→2.7 分/行) / Rn M5 28 行 (1.3→1.8 分/行)。**C1–C16 のファイル単位は全部 OK、C16b も PASS 相当** (落ちるのは 525 本の集合照合だけ)。v5 との差 = **Fe K 2.0e-07 / Zn M1 1.63e-03 (s=0.15, E₀=350)、σ_own +5.8e-03** — 掃引の予測どおり |
| kill/resume リハーサル (`../qcamp/rep_v6_resume`) | Fe K を 9 行目で**プロセス木 kill** (子 julia.exe 消滅) → watchdog exit=1 → attempt 2 が partial から 9/28 行を再利用 → 完走。**中断なしの Fe K と 28 行すべてビット同一** (F / N0 / σ / s_cert / tail / diag、文脈・指紋も同一) |
| 2/3/4 スレッド | HIGH --v4 7 チャネルのスナップショットがすべてビット同一 |
| メモリ | 3 スレッドのレーン = Private 2.8〜3.2 GB (working set ~1 GB)。8×3 で ≈ 28 GB (1.3×28+12 = 48 ≤ 63.7 GiB) |
| heartbeat | ε ノードごと (Rn M5 で 116 ノード/行) にログへ flush。watchdog の 15 分規則はそのまま |

⚠ 罠: 代表生成の Rn M5 を `--lane 56/57` で起動したら**空レーン** (M5 は Bote 表に 9 副殻ある元素だけ = 54 チャネル) で何も
計算せず exit 0 — `--lane` の index は `all_channels((tag,))` で引くこと。resume リハーサルの自動 kill は WMI の CommandLine が
juliaup の子では exe パスしか返さず対象を特定できなかった (手動で木 kill)。

## 5.5 起動の記録 (2026-08-20 21:02:14)

| 項目 | 値 |
|---|---|
| commit (生成 worktree `C:/tmp/temari_v6_gen`、detached) | **`7991cdb`** (= `80d01e2` + `.gitattributes`。src は `4f7fc88` 以降不変) |
| spec | `spec/temari_dataset_v6.0.0.spec.json` sha256 **`749fadc5af79c9753a7718455d6a29cbe428891863b18b8ab908cac1ef4bf211`** (registry `spec/RELEASES.json`) |
| run dir | `C:/Users/seto/source/repos/qcamp/prod_v6_run1` (repo 外。`RUN_SPEC.json` / `SPEC_SNAPSHOT_749fadc5af79c975.json` / `INCOMPLETE` / `LAUNCH.txt` / `MONITOR.log`) |
| 構成 | **8 レーン × 3 スレッド** (`--gcthreads=1`、BLAS 1)、90 s stagger、`tools/lane_watchdog.sh i 8 3 +1.11 "" $RUN` with `PROFILE=v6_high` |
| ログ | `C:/tmp/temari_prod_v6_run1_lane{0..7}_log.txt` (heartbeat = ε ノードごと)、監視 = `fleet_monitor.sh` 5 分ごと → `MONITOR.log` |
| レーン割当 | 525 チャネルを `(k-1) % 8` で 66/66/66/66/66/65/65/65 本 (tag 順 × Z 順の round-robin) |
| 見積り | 14,796 行 × 60〜70 s/行/レーン ÷ 8 ≈ **31〜36 時間** → 2026-08-22 朝〜昼 |
| Julia | 1.11.9 (v3〜v5 と同じピン) |

- ⚠ **起動直前の罠**: 最初の起動 (21:00) は launch_fleet の A 再検査「spec が src と一致しない」で NO-GO — worktree のチェックアウトが
  `core.autocrlf=true` で **spec の JSON を CRLF に変換**し、承認 hash と合わなくなっていた (codex 4 巡目が警告していた点)。
  `.gitattributes` に `spec/*.json -text` を足して commit (`7991cdb`)、空の worktree で hash 一致を確認してから再起動 (21:02)
- 起動後 40 分の観測 (J・heartbeat・孤児・RUN_SPEC 一致) は §5.6 に記す。**中止の条件** = codex 4 巡目の一覧 (RUN_SPEC/spec 不一致、同一レーン二重、
  孤児 julia.exe、heartbeat 誤検知 kill、Available RAM < 12 GiB、`[sane]` 2 回、独立クラッシュ複数)

## 5.6 起動後 40 分の観測 (21:03〜21:44、`MONITOR.log` 9 サンプル) — **継続の判定**

| 指標 | 観測 | 判定 |
|---|---|---|
| プロセス | julia.exe 8 本 = レーン数 (二重・孤児なし)、全レーン attempt 1、kill 0、`[sane]`/`[gate]`/ERROR/PERMANENT 0 | ✅ |
| RUN_SPEC / INCOMPLETE / SPEC_SNAPSHOT | 1 / あり / 1 本 (`749fadc5…`) | ✅ |
| heartbeat | 最大間隔 40 s (起動時の SCF)、定常 ≤ 6 s | ✅ (15 分規則に対し余裕 20 倍以上) |
| 進捗 | 21:44 で **294 行 / 14,796、JSON 8 / 525** (レーンごと 34〜38 行)。軽い K 殻から始まるので行単価は今後重くなる。単純外挿 ≈ 33 h | 見積り 31〜36 h と整合 |
| メモリ (J) | julia の Private 2.6〜3.4 GB/レーン、合計 21〜23 GB (見込みどおり)、物理空き 28〜31 GB、working set 合計 6 GB | ✅ |
| ⚠ commit charge | 59 → 67.5 GB / 上限 70.3 GB (96 %)。julia は 21.7 GB で横ばい、**他プロセス (claude ×N / VS Code / OneDrive / Defender …) の private が 34 GB**。ページファイルは**システム管理** (今セッション中に 3,968 → 5,126 MB へ自動拡張済) なので上限は需要で伸びる | 継続。30 分報告で監視 (割り当て失敗・pagefile 拡張の有無) |

⇒ **8 レーンのまま継続** (21:45)。中止条件は §5.5。30 分おきの定期報告 (毎時 17 分・47 分、セッション内の cron) で
進捗・kill・メモリ・commit を追う。

## 6. 残作業 (着手順)

1. **走行の見守り → 完走** (見込み 08-22 朝〜昼): 30 分報告。完走 = 8 レーン COMPLETE + JSON 525 + partial/stale/tmp 0 + julia.exe 0
2. **昇格** (RUNBOOK §4.1): run dir で `check_tables.jl $RUN --expect-version 6.0.0 --eb` (C9 込み) → manifest (`tools/make_manifest.jl`、`--verify`) →
   `src/prod_v6_jl/` を新規に作り `INCOMPLETE` を先に置く → 525 本をバイトコピー → コピー先で verify + 同じ QC → 最後に `INCOMPLETE` を消す →
   `MANIFEST.md` (v5 の形式に倣う。運用記録 = kill/再起動の回数、`generator_commit` の出現数、Julia 版) を `git add -f`
3. **ReciPro 配備**: `pack_resource.py` の PACK_LAYOUT を v6 へ、`.bin` + `Crystallography.dll` を**同時**差し替え、`EdxCheck all` + `AlchemiCheck all`、
   **golden の再凍結**。v6 の F は v5 と M 殻で 1.6e-03 違うので `AlchemiCheck golden` は必ず落ちる → 再凍結が正しい対処
4. **release `dataset-v6.0.0`** (`tools/make_dataset_release.sh` の決定論的 tar.gz) + **DOI の取り直し** (作者ログイン)。data.md / verification.md の
   数値 (v5 の LOO 1.16e-03 等) を v6 の QC 値に更新、ERRATA は v5 の隣に残す
5. 完走後の小物: `docs/notes/error_budget_2026-08-19.md` の行を v6 の監査値へ (ε ノード行は上向きプローブの値) / `tools/e0_interp_probe.jl sweep` を v6 で /
   `docs/README.md` に `spec/` の説明 / 用語 Phase 3 (ファイル名 4 本 `*_contract_*.md` + 配布同梱 tools) は release と同時
6. **作者判断のまま**: deep 認証 (σ(β,Δ) v4、16 レーンで 14〜24 日 — v6 完走後なら CPU は空く) / EN の "contract" / Zenodo
7. 次々世代の台帳 (変えていない): 等比 s 格子 (形式変更 + ReciPro) / ppw 30 / Python の M 殻 / `gen_factors.jl` の JSON 文字列 6 箇所の旧パス (factors の次回生成で)

## 7. 踏んだ罠 (§0-8 の詳細)

| 罠 | 何が起きたか | 対処 / 教訓 |
|---|---|---|
| heredoc のバックスラッシュ (3 回目) | Python の編集スクリプトの `"\\r\\n"` が実際の CR/LF に化けて .py が壊れた | **バックスラッシュを含む Python は例外なく Write で**。memory 更新 |
| worktree は CRLF | autocrlf=true のチェックアウトで (a) 編集スクリプトの LF 前提の照合が 0 件一致、(b) **spec の JSON が CRLF になり承認 hash と不一致 → 起動 1 回目 NO-GO** | 編集スクリプトは改行非依存に / `.gitattributes` に `spec/*.json -text` (`7991cdb`) / launch_fleet の A 再検査が効いた |
| SCF キャッシュ指紋 | l0_numerics / l1_atomic に触る commit の after 側だけ SCF が再計算され、非決定性が差分に化けうる | ゲートは両側空キャッシュ + payload sha 照合 (memory `symmetric-cache-gate`) |
| 監査の振り幅のリテラル | `n1=28` が HIGH n1=40 では下向きの振り | `1.4 × HIGH_SETTINGS` から引く (`4f7fc88`、memory `bumps-relative-to-current-settings`) |
| 空レーン | M5 は 54 チャネルなので `--lane 56/57` は何もせず exit 0 | index は `all_channels((tag,))` で引く。「exit 0 = 計算した」ではない |
| juliaup の子プロセス | `kill -9` はランチャだけ殺し julia.exe が孤児化する。WMI の CommandLine も exe パスしか返さない | watchdog は WINPID から `taskkill /T /F` (`1351b78`)、手動 kill も木で |
| `redirect_stdout(IOBuffer)` | Julia では使えない (Pipe のみ) | 関数に `io` kwarg を持たせる |
| Python の CHANNELS | K/L のみ。M 殻の参照値は作れない | v6 case は K/L で選び直し、限界を JSON の `_scope` に明記 |
| commit charge | 8 レーンで commit 96 % (物理は 30 GB 空き)。ページファイルはシステム管理で伸びる | 30 分報告で監視。物理空き < 12 GiB / 割り当て失敗で中止 |
