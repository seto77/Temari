# 次チャットへの指示書 (2026-08-14 04:50 作成)

⚠ 本書は「次に何を、どの順で、どう気をつけてやるか」だけを書く。根拠は正本へ:

| 正本 | 内容 |
|---|---|
| **`docs/notes/grid_certification_l1_run_2026-08-14.md`** | ★ L¹ 認証の実行報告 (86/86 完走、certified 83、予算超過ゼロ) |
| `docs/notes/grid_certification_preregistration_v2_2026-08-12.md` | 凍結済み規則 (`ebe585c`)。⚠ H の分類順序バグは実行報告 §3 |
| `docs/notes/scattering_factor_dataset_plan_2026-08-10.md` | トラック全体の正本 (§4.23 = f_e の T_comp 設計) |
| `docs/handover/handoff_scattering_factor_2026-08-11.md` | 現在地 (冒頭が 8/14 更新済み) と踏んだ罠 |

## 1. 現在地と作者決定

- **L¹ 認証は完走・成立** (2026-08-14 04:09)。数値の残る未測定は
  **B_repr (表現誤差) と端点切断**のみ
- ★ **作者決定 (2026-08-14): T_comp,e は案 A (記述的契約) = 1e-7 Å**。
  内訳は f_x と同型に **B_num,e = 9.09e-08 / B_repr,e = 9.09e-09 Å**。
  実測 (格子 3.82e-08 + 停止 2.64e-08 = 合成 ~0.71×B_num,e) は収まっている。
  ⚠ **凍結は B_repr,e の実測後** (§2-1 → §2-2 の順を守る)

## 2. 作業の順序

### 2-1. B_repr / B_repr,e の測定 (最初にやる)

**方法**: v1 認証走の f_x 副本 (`c:/tmp/temari_certify_2026-08-11/z*.f64`、
84 元素 × 15,361 点) を使う。**奇数番目 = 出荷 7681 節点でスプラインを組み、
偶数番目 = 封印した中点 7680 点で評価**して計算値との差を取る — 中点は格子選択に
使っていない検証点なので、補間誤差の直接測定としてそのまま使える。

- f_x: **not-a-knot 3 次スプライン** (計画書 §4.9 の収録規約) を s 上で。
  合格 = 全元素・全中点で ≤ B_repr = 9.09e-09 電子
- f_e: 表に直接収録し **t = s² 上で補間** (経路 D、§4.13)。f_x 副本から
  `certify_fe.jl` の道具 (`fe_from_fx`) で f_e を組み、t 上のスプラインで同様に。
  合格 = ≤ B_repr,e = 9.09e-09 Å
- **JSON 十進丸め**も忘れない: 出荷桁数で書いて読み戻した往復差を上と別に測る
  (どちらも B_repr の内数)
- ⚠ **経路 A (補間した f_x から f_e を導く) を測っても意味は無い** — 既に棄却済み
  (どの格子でも 1〜3 桁不合格。f_x′(0)=0 を not-a-knot が保証しない)
- ⚠ Tm 69 / Yb 70 は v1 副本が無い (未完走)。**82…84 元素で測って「2 元素欠け」を
  明記**する。埋めるなら §2-6 の当初規則走の後
- ⚠ 補間誤差は「中点だけが悪い」構造 (節点上は厳密に 0) なので、
  **中点の max がそのまま代表値**。121 点均一補間の 3.28e-03 (計画書実測) と
  比べて桁の妥当性を検算すること

### 2-2. T_comp,e / T_comp の契約凍結

B_repr,e・B_repr が収まったら:

1. 計画書 §4.23 に作者決定 (案 A、1e-7 Å) と実測内訳を記載
2. **契約は出荷経路と突き合わせる** ([[contract-vs-shipping-consumer]] の教訓 —
   コードで書いただけの契約は端点・範囲外・座標変換でずれる)。出荷 loader は
   まだ無いので、**契約の実行可能形 (golden vector 込み) は X1 (schema) /
   X13/X14 (梱包) と同時に組む**。この指示書では「値の凍結」まで
3. 超えていたら: 節点数・桁数・補間規約の再検討として**作者判断へ戻す**
   (勝手に丸めを増やさない)

### 2-3. 規則 v2.1 の起草 (H の分類順序)

実行報告 §3 のバグ修正: **low_signal (‖Δρ₃‖₁ ≤ 1e-9) を model_violated より
先に評価する**。事前登録 v2.1 として短い文書を起草し、`certify_l1.jl` を直し、
`l1_negative_test.jl` に「低信号かつ q₂₃ > 0.5 の合成例が low_signal に分類される」
負のテストを足す。**再走が要るのは H/He/Li の 3 元素だけ** (低信号床は D₃ で
判定され、他 83 元素は D₃ > 1e-9 なので判定が変わらない — これを集計で確認して
書く)。⚠ 3 元素の再走も新規則凍結後に。

### 2-4. 端点切断 (`certify_endpoints.jl`、実装済・未実行)

一次スクリーニング = 全 Z を粗い dt で r₀/10・r_max×1.5 (安い)。
起動は確立方式 (タスクスケジューラのトリガー自然発火 + ASCII の VBS 非表示起動)。
8 レーン。⚠ tight 閾値で行う (production だと停止ゆらぎを測ってしまう)。

### 2-5. 標本 14 元素の追加診断 (事前登録 v1 §4.1 の残り)

`--prod-all` (U が格子に依らないこと) と τ/100。⚠ **τ/100 は Fe 級で
届かないことが P3 で確定済み** — 「τ/10 が十分 tight であることを、
τ/100 が**届く元素で**確かめる」と目的を再定義してから回す。
届かない元素は「検査不能」と記録する ([[fixed-threshold-assumes-sn]])。

### 2-6. 低優先 (レーンが空いたら / 別セッションで)

- Tm/Yb の当初規則走 (`run_remaining.sh` 再発火。L¹ で両方 certified 済みなので
  材料集めのみ。副産物として §2-1 の 2 元素欠けが埋まる)
- OA 2000/2001 独立検証 (F(s,E₀) 側。フリートと同時不可)
- データセット公開の梱包 X1/X13/X14 (§2-2 の契約実行形と同時)

## 3. ⚠ 運用規律 (8/13-14 の走で確定したものを含む)

- **起動方法**: タスクスケジューラの**トリガー自然発火** + **ASCII のみの VBS**
  非表示起動。⚠ nohup と手動 Start は死ぬ。**10 分試走してから本番**
- **PC 再起動はあり得る** (8/12 に 2 回)。タスク登録は生き残る。JSON の有無 +
  `run_*.sh` の skip 機構で再開する
- **wedged は CPU 増分の相対判定で** (20〜30 秒サンプル × 2 回連続ゼロ + 他が正)。
  ⚠ **ログ mtime は使えない** — `certify_l1.jl` 系は元素の全水準を解いてから
  まとめて印字するので、重元素は 3〜10 時間無音が正常。kill → 再試行で
  5/5 全回収の実績
- **失敗も未収束も非決定論的** (Er の stage 4 は再走で素直に収束した)。
  1 回の失敗で性質を語らない
- **信号がその測定の停止許容より小さい量は測れていない** ([[signal-below-tolerance]])。
  差分量を判定に使う前に信号/床の比を見る
- **凍結後は走らせるまで 1 文字も変えない**。欠陥を見つけたら (H の例のように)
  規則は触らず記録し、修正は次版の事前登録で
