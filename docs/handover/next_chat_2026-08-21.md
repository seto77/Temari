# 次チャットへの指示書 (2026-08-19 夜) — 積み残し

**⭐ 冒頭でこれを読む**。前段 = `docs/handover/next_chat_2026-08-20.md` (認証の走行と
その日の作業)。本書は**看板刷新 → 作業リスト → B9/B10/B6/B8/B3 を片付けたあとの残り**。

⚠ 本書には**実測**と**作者決定**と**私 (Claude) の解釈**が混ざる。区別して書いた。

## 0. 五行で

1. **全格子認証は完走した** — 14,796 行 / 236,728 窓 / 16 時間 44 分、**レーン再起動 0、
   例外行 0** (`ae82361`)。⚠ **完走 ≠ 解析完了**。作者側の検証作業は続いている
2. ★★★ **窓求積の最悪食い違いが 8.648e-03** — 契約草案は spike 標本から **3.6e-08** と
   書いていた。**5 桁**。⇒ **`sigma_beta_delta_contract` の §5A を書き換えるまで凍結しない**
3. ✅ **`tools/certify_sigma.jl` / `beta_spike.jl` の旧 docs パスは修正済** (`ae82361`)。
   本走が終わるのを待って当てられた。⇒ **この件はもう残務ではない**
4. **看板は「非対角 F(s,E₀)」に絞り済み**、docs は 3 サブディレクトリへ再編済み。
   作業リストの正本 = **`docs/notes/work_list_2026-08-19.md`**
5. **B9 / B10 / B6 / B8 / B3 は実施済**。★ どれも「やってみたら別のことが分かった」ので
   §2 に列挙した。**次に効くのは B1**

## 1. ★ 最初にやること — 認証の結論を契約へ落とす

### 1.1 σ(β,Δ) 契約 §5A の書き換え (⚠ 凍結の前提条件)

`ae82361` が出した数字を `docs/notes/sigma_beta_delta_contract_2026-08-18.md` §5A へ。
今の §5A は**代表標本の観測最大**で、実態と **5 桁**違う。

- **最悪窓 8.648e-03** (草案は 3.6e-08)
- **最悪 8 行はすべて Xe M4**。⚠ **E₀ を 170 → 400 keV にしてもほとんど動かない**
  ⇒ 難しさは**標的側**から来ている (ビームではない)
- 機構: d 始状態の双極子終状態は **f 波**。遠心障壁が閾値直上を抑えるので、
  断面積の**最大が窓の端ではなく内側に来る**。16 点則がそのピークを解けない
- ⚠ `observed_max` のまま書く。**`≤` と書かない**

### 1.2 ⚠ 同じコミットが撤回した 3 件 — 蒸し返さない

| 一度そう報告した | 実際は |
|---|---|
| 閾値則が原因 | ✗ 代入すると任意の終状態 l で整数冪が残る |
| 部分波の切り替えが kink | ✗ 切り替え前の状態を**実際に**再現させると **2.66e-16** しか動かない。⚠ 最初の試行は何も強制できておらず、**負のテストだけがそれを暴いた** |
| 床はその切り替え | ✗ 被積分関数の**決定論的な粗さ 約 2e-09**。刻み幅 6 桁にわたって平坦で、**出荷 F(s) の経路も同じものを共有している** |

## 2. ★ 実施済みだが、やって初めて分かったこと

**どれも「予定どおり終わった」ではない**。次の判断に効くのでここに置く。

| 項目 | 何が分かったか |
|---|---|
| **B6** Mott CDF | ★★ **NIST 配置の Δμ=0.001 が前方を分解しない** — 先頭 1 区間 (θ≤2.56°) が σ_el の **28–67 %**、30 keV で最悪。⇒ **出荷形式を決める前に前方の刻み方を決める**。また節点間を GL 1 枚で積むと足りず、**l_max からサブパネル数を導出**して 5.82e-06 → 7.86e-15 |
| **B8** 参照ベクトル | ★★ **契約の散文に曖昧さ 2 件**。②の s 基底の読み違いは **3.3e-03** = E₀ 補間誤差の最悪と同じ桁。⇒ **`data.md` の契約本文に追記済** (commit `6d10f07`) |
| **B3** 処方差 | ★★ 既報の Al/Co **2.8e-3 / 5.7e-3 を厳密に再現**したが、**それは 1 配置**。全 246ch では **4.14e-02 = 約 15 倍** |
| **B10** 誤差予算 | C6 の出荷 v5 最悪は **1.16e-3** (8.84e-4 は v3)。「床は s>8 求積誤差の約 3 倍」は撤回済だが**旧文が同じ MANIFEST の 214 行上に残っている** |
| 監査 | `tools/md_emphasis_check.py` は**引数なしだと 0 ファイル**を走査する = 空振りの合格。全 docs では **124 箇所** (公開サイトは 0) |

## 3. §1 が済んでから

### 3.1 B1 — `verification.md` の外部比較を全格子で入れ替える

正本 = `docs/notes/external_gate_2026-08-19.md`。⚠⚠ **codex の判定で方針が変わっている**:

- **「4 チャネルを総入れ替え」には反対**。代表チャネルの物理的説明は**残し**、
  全格子統計を**追加**する。6,300 条件は独立な 6,300 実験ではなく、
  **1 モデル・1 参照 DB 上の相関した点**だから
- 必要な表示: shell / Z / エネルギー損失 / ρ ごとの**層別**、median・IQR・P5/P95、
  min/max と**その具体的条件**、除外件数、「**著者が回した比較であって第三者検証ではない**」
- ⚠ ρ 帯の GOS 比と β・窓積分の σ 比は**別量**。逆数のように書かない
- ⚠ **中間帯 ρ 0.3–0.8 は一度も比較していない**。この帯は σ(β,Δ) の**最大 39 %** を
  運ぶ (尾根帯は 0.00–5.79 %) ⇒ **未比較の帯が尾根帯より重い**。この 1 文だけでも価値がある

### 3.2 B3 の続き — 実経路へ通した測定

`AlchemiCheck sens` + `AlchemiRequest.ChannelDataOverride` (ReciPro 側)。
⚠ codex の推奨は**2 本を並べて**出すこと:

| 値 | KIND |
|---|---|
| 帯最大 × 帯別伝達係数 | `scenario-sensitivity` (実施済、`prescription_impact_2026-08-19.md` §3) |
| v3/v5 の表を実経路へ投入したサイト比差 | `direct-measurement` (**未実施**) |

⚠ さらに codex は **release-to-release 比較** (各 release を当時の native loader と
範囲契約のまま通す) を**別立て**にすべきだと言っている。そちらは continuum 処方・
s 範囲・tail 契約・チャネル範囲・loader 挙動の差が全部入るので、
**「κ-Dirac だけの効果」とは呼べない**。

## 4. 認証と無関係にできるもの

### 4.1 A13 → B7 (⚠ 作者ログインが必要。私には実行できない)

**この順で**。同じ著者名・ORCID・keywords・related identifier の書式を流用できるため。

- **A13**: Zenodo 21872050 のメタデータが刷新前 (title / description / keywords に
  `off-diagonal` も `mixed dynamic` も無い、related_identifiers 2 件)。
  ⚠ **ファイルは凍結だがメタデータは編集でき、新 DOI は発行されない**
- **B7**: dataset-factors v1.0.0 の DOI。⚠⚠ **`zenodo_deposit.md` §5 の
  「New version を押す」には従わない** (別一族が 21872049 の系譜に取り込まれる)。
  **新規 Dataset レコード**で、`dist/` の既存 tarball をそのまま上げる。
  ⚠⚠ **再ビルド禁止** — commit `7f88b85` で `factors_release_README.md` が変わって
  いるので、今再ビルドすると byte 同一にならない
- ⚠ Zenodo description に **v1.0.0 archive の erratum へのリンク**を書く

### 4.2 B6 の続き — 出荷形式を決める前の判断

⚠ **前方の刻み方**を決めないと Mott は出荷できない (§2 の実測)。選択肢:
① NIST 配置のまま (現行 sampler と互換だが前方が潰れる) ② 前方に節点を足す
③ μ ではなく θ か log(1−μ) で刻む。**作者判断**。
⚠ 既定 `l_cap=600` は軽元素の高エネルギーで足りない (Al 30 keV は l_max=774)。

### 4.3 小さいもの

- **`docs/notes/` の 124 箇所の強調破れ** (`**…。**` が閉じない)。公開サイトは 0 なので
  GitHub 上の見た目だけ。⚠ `python tools/md_emphasis_check.py docs/*.md docs/notes/*.md
  docs/handover/*.md docs/release/*.md` と**対象を明示**して走らせること
- **`src/prod_v5_jl/MANIFEST.md:114`** に撤回済みの「約 3 倍」が残っている
  (撤回は `:328-329`)。⚠ MANIFEST は出荷記録なので**書き換えてよいか作者判断**
- **`docs/notes/src_defect_2026-08-07.md:317`** と
  `fs_external_validation_2026-08-07.md:166,307` に「Ag は未解明」が残っている
  (解決は `src_defect:256` §8)
- **`src/l0_numerics.jl:38-42`** に古い監査値 (K ~7e-6 / L ~2e-6)。⚠ **src なので
  指紋が動く** — 次の全再生成のときに

## 5. 保留・不採用 (蒸し返さない)

| 項目 | 判定 |
|---|---|
| `.zenodo.json` + GitHub 連携で software DOI | ⚠ **保留**。webhook が**以後の全 tag** を拾い `dataset-*` が software 系譜へ混入する。必要なら**手動 deposit** |
| ソーシャルプレビュー / OG カード | 採用だが**低優先**。⚠ `coverage.svg` は 3.03:1 なので 2:1 への再構成が要る |
| Mott 求積の (Z, ε) 掃引 | **Mott を対外製品化すると決めてから** |
| イオンの f_x / f_e | **今回は不採用**。src 凍結解除 = 世代交代 |
| B9 の 170 点を B8 に流用 | **不可**。目的が違う (片方は内挿を避け、片方は突く) |

## 6. 凍結で塞がっているもの (再提案しない)

正本 = `docs/README.md`「Old paths」(**五箇所**)。

- `src/*.jl` の旧 docs パス **34 箇所 (11 ファイル)** — ソース指紋。
  `physics_pointer` は出荷 JSON へ逐語
- `src/prod_*/` の **15 名** — 公開データ、SHA-256 凍結
- `schema/*.schema.json` の `$id` 書き換え — ⇒ **URL 側を作る (実施済 `f10ba44`)**
- `tools/temari_contract.py` に `--make-golden` を足す — 同梱。⇒ 外から import (実施済)
- `docs/release/*_release_{README,LICENSE}.md` — verbatim 同梱
- factors v1 に per-row の `s_cert` 相当 — tarball SHA を壊すうえ**入れる中身が無い**
- ✅ **`tools/certify_sigma.jl` + `beta_spike.jl`** — `ae82361` で修正済。
  ⚠ **次に認証を回すときは、また同じ制約がかかる** (走行中は 1 byte も触れない)

## 7. この 2 日で足したもの (場所だけ)

| 何 | どこ |
|---|---|
| 作業リスト (35 件、優先順位つき) | `docs/notes/work_list_2026-08-19.md` |
| 位置づけ (看板の根拠) | `docs/notes/evaluation_report_2026-08-19.md` |
| 誤差予算 (KIND 列つき) | `docs/notes/error_budget_2026-08-19.md` |
| ベンチマーク仕様 (10ch × 17 節点) | `docs/notes/benchmark_spec_2026-08-19.md` |
| B8 事前登録 + 実施結果 | `docs/notes/b8_preregistration_2026-08-19.md` |
| v3→v5 の処方差 | `docs/notes/prescription_impact_2026-08-19.md` |
| 参照ベクトル (50 本) | `verification/f_v5_postrelease_vectors.json` |
| 第 2 評価器 / 生成器 | `tools/f_contract_oracle.jl` / `tools/b8_make_vectors.jl` |
| Mott CDF プローブ | `tools/mott_cdf_probe.jl` |
| v3/v5 比較 | `tools/v3_v5_prescription_diff.py` |

commit 範囲 = `7a864b5` … `6d10f07` (⚠ うち `ae82361` は別セッション)。⚠ **`src/` は 1 行も変えていない**
(`git diff 25e0d39..HEAD -- src/` は `src/IMPORT.md` のみ)。
