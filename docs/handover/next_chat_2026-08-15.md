# 次チャットへの指示書 (2026-08-14 09:20 作成。フリート完走後に更新予定)

> ⚠ **2026-08-16: 本書の残項目 (§2-3 の出荷生成本体 + X1/X13/X14、§2-4 の 2 段目) は着手済み**。
> 正本は `docs/handover/next_chat_2026-08-16.md` へ移った。

⚠ 本書は「次に何を、どの順で、どう気をつけてやるか」だけを書く。根拠は正本へ:

| 正本 | 内容 |
|---|---|
| **`docs/notes/repr_measurement_2026-08-14.md`** | ★ B_repr/B_repr,e の実測 (84 元素) と設計問題 2 件 |
| **`docs/notes/endpoint_truncation_2026-08-14.md`** | ★ 端点切断スクリーニング (86/86、≤1.2 % B_grid) |
| `docs/notes/grid_certification_l1_run_2026-08-14.md` | L¹ 認証 v2.1 最終 (certified 83 / low_signal 3 / violated 0) |
| `docs/notes/grid_certification_preregistration_v2.1_2026-08-14.md` | v2.1 の凍結規則 (`cddd5d0`) |
| `docs/notes/scattering_factor_dataset_plan_2026-08-10.md` §4.23.8 | T_comp,e の作者決定と保留理由 |
| `docs/handover/next_chat_2026-08-14.md` | 前日の指示書 (§2-1〜§2-4 実施済み、§2-5/2-6 フリート中) |

## 1. 走行中のフリート (直列 3 段。⚠ 8/14 に PC 再起動 1 回で中断 → 再発火済み)

`c:/tmp/temari_endpoints_2026-08-14/launch_ep.sh` が taskscheduler ジョブ
`Temari_endpoints_st1` から発火し、直列に:

1. ~~端点切断 (86 元素)~~ — **完走・報告済み** (08:56、`70bbbba`)
2. ~~標本 14 元素~~ — **★ 14/14 完結 (08-16 01:14)・報告済み** (`2dce835` =
   `docs/notes/sample14_diagnostics_2026-08-16.md`)。KLI NaN 4 + wedged 3 + 再起動 1 を
   pass/retry 機構が全回収した
3. ~~Tm/Yb の当初規則走~~ — **完走 (08-15 15:46)、v1 副本 86/86**

**経過**: 初回発火 08:05 → 端点切断完走 08:56 → sample14 t100 進行中の
**11:53 に PC 再起動 (8/14 では 1 回目) で全滅** → 22:45 に発見、
`schtasks /Change /ST 22:50` でトリガーを引き直して**再発火成功** (22:50:02 に
skip 機構が endpoints を 1 秒で飛ばし t100 再開)。
⚠ 中断前に **Cr 24・Gd 64 の τ/100 が KLI NaN で即死**していた —
v1 で確立済みの非決定論的現象で、JSON が無いので再発火時に自動再試行される。
pass 2 まであるので 1 回の NaN では欠けない。

進行は `launch.log` の「終了」行で分かる。**PC 再起動があってもタスク登録は
生き残る** — `schtasks /Change /TN Temari_endpoints_st1 /ST <数分後>` で
トリガーを引き直せば skip 機構が続きから回す (22:50 の実績)。
見込み: t100 完了 ~01:30 / prod-all ~06:00 / Tm/Yb ~10:00 (8/15 朝)。

## 2. フリート完走後にやること (順)

### 2-1. ~~標本 14 元素の集計~~ — ★ 完了 (`2dce835`)

正本 = `docs/notes/sample14_diagnostics_2026-08-16.md`。**τ/100 が届くのは Z ≤ 11 の
4 元素だけで U′ ≤ 0.030×B_scf (τ/10 十分性の直接証拠)、壁は Z = 11 と 18 の間。
不達 10 元素は検査不能と記録。--prod-all は 14/14 で U の格子非依存を確認**。

### 2-2. ~~certify_repr を 86 元素で再実行~~ — ★ 完了 (`139d591`)

86/86 で結論不変 (Tm 0.611× / Yb 0.586× は合格圏。f_x 超過は Cs/Ba のまま、
clamped-left で全 86 元素 0.123× 以下)。

### 2-3. ~~作者判断待ち 3 点~~ — ★ 全部確定した (2026-08-15 夜、計画書 §4.23.8)

(i) f_x = 左端 clamped + 右端 NAK / (ii) f_e = **δ 形求積 + f_x 求積の補償和の
併用** (moment も同規則で補償和) / (iii) 11 桁 + s 軸は式定義。
**T_comp,e = 1e-7 Å (案 A) は凍結済み**。

⇒ **★ 実装済み (2026-08-15 夜、`b84779e`)**: `l5_exit_fx.jl` に
`one_minus_j0` + `xray_deficit` (δ 形) を新設、`xray_form_factor` /
`density_moment` を **Neumaier 補償和**に変更 (⚠ classic Kahan は f_x の
符号振動和で破綻する — T12c が [1e16,1,−1e16] で実際に捕まえた)、
`compute_fx` の f_e を δ 形へ配線し δ形↔MB 整合ゲート (s ≥ 0.2、実測 ~1e-15) を
常設。selftest に T12c (δ 形 vs 閉形式 5.6e-16 / T12 は 300 倍改善)。
検証済み: 無効化版との差 = 旧丸め床レベル (C 6.7e-14 / Au 9.4e-13) /
refcheck 9.044e-08 / **bitident v3・v4 差分ゼロ** (F v5 経路は無傷)。
残る実装 = loader 仕様の契約文 (clamped-left) と出荷生成本体 — X1/X13/X14 と同時。

### 2-4. 残り (端点切断の 2 段目)

事前登録 v1 §8 の**極端例** (原点側 = Au・Rn / 遠方側 = H・Li・Na・Cs) を
**採用格子 dt/16 でも** + **標本の deep** (r₀/100・r_max×2、縮み比を見る)。
一次が小さかったことを理由に**取りやめない** (事前指定を結果で変えない)。
`certify_endpoints.jl --stage 5 --deep` が実装済み (ラダー入り)。
⚠ dt/16 の SCF は重い (Au tight 1 本 ~4000 s)。8 元素 × 5 SCF ≈ 1.5 日級 —
フリート設計してから。

## 3. ⚠ 運用規律 (前日から変わらず + 本日の追加)

- 起動 = タスクスケジューラのトリガー自然発火 + ASCII VBS。nohup と手動 Start は死ぬ
- **未収束解との差は何も測っていない** — endpoints 初走で Yb の r₀/10 が
  9 倍過大に出た (試行 2 で収束させたら 0.0045×)。SCF が絡む差分測定は
  必ず元素単位ラダーで収束させてから
- **`certify_*` 系のツールはフリート走行中に触らない** (tool_sha が嘘になる)
- 凍結後は走らせるまで 1 文字も変えない。欠陥は記録して次版の事前登録で
- 和文 md の強調は行内で閉じる。検査 = `PYTHONIOENCODING=utf-8 python tools/md_emphasis_check.py <files>`
