# 次チャットへの指示書 (2026-08-19 深夜) — 2 本の指示書を一括して進めた結果

**⭐ 冒頭でこれを読む**。前段 = `docs/handover/next_chat_2026-08-20.md` (認証の走行と窓の穴) と
`docs/handover/next_chat_2026-08-21.md` (積み残し)。作者の指示で**両方を一括して**進め、
「これ以外の残作業」も掃いた (§6)。本書がその結果と、次に何をどの順でやるか。

⚠ 本書には**実測**と**作者決定**と**私 (Claude) の解釈**が混ざる。区別して書いた。
⚠ **src は本セッションでも 1 行も変えていない** (`src/IMPORT.md` のみ。指紋非対象)。

## 0. 六行で

1. ★★★ **窓の規則は決めた (作者判断待ちの材料は全部揃った)** — 座標は床の下で見分けがつかず、効くのは
   「単一規則か等比パネルか」。**等比 16 パネル × GL16 (256 点) なら 90 条件すべて床 (≤ 3.6e-08)**。
   旧規則は Ca M1 Δ₁=0.01 eV で**相対誤差 1.00** (`window_quadrature_2026-08-19.md` §8.3)
2. ★★ **床 ~1e-08 の正体 = `ppw = 25` (連続状態 ODE の離散化)**。私の n_q 仮説は 2×2 で反証された
   (n_q 240→1216 で不変、ppw 25→100 で 22〜1200 倍低下。§8.4)。出荷 F(s) も共有する床
3. ★★ **本番候補を実装した** — `tools/sigma_beta_delta.jl` (sin²θ 等比 16×16 + ε_c 境界 + 継ぎ目分割
   GL12 + n_q 1216 + 入力検証 + own/Bote 両方)。出荷 JSON の σ_own と 4.6e-06 で一致、1/6 スレッドで
   ビット同一。**認証 v2** (`tools/certify_sigma_v2.jl`) を事前登録して **pilot を走らせた** (§2、§7)
4. ⚠⚠⚠ **pilot の結果: β ≤ 30 mrad は全窓床 (最悪 5.3e-08) だが、β ≥ 60 mrad × 高 ε へ届く窓で 32/174 窓が
   不合格 (最悪 9.45e-05、Zn M3 @300)**。機構は 2 つとも特定: (a) **l_max(ε) の階段** (src の l_kin が
   ε ごとに 1 ずつ増え、大 β で 1 段 ~1e-06 跳ぶ。負のテスト込み) と、(b) ★★★ **src の l_kin =
   ⌈κ·min(r_core, 6/Z)⌉+12 が 3p/3d のような広がった軌道の高 ε で収束していない** (Xe M4 @400 keV、
   ε=30 keV、β=π で l_max 18 → 80 が **+7.3 %**)。(b) は**出荷 F(s)/σ_own の処方の問題**で、σ_own ~1e-03、
   高 s の M4/M5 は百分率級の可能性 — **F 側で測るまで主張しない。最優先の測定** (§7.1)。
   正本 = `docs/notes/certification_v2_pilot_2026-08-19.md`
4′. ⚠⚠ **deep 認証 (≈1,580 行) は 16 レーンで 3.4〜4 日 (v1 の実測)、HIGH + l_max 変更なら 5〜8 日** —
   起動は**作者判断**。候補 v2 の中身も作者判断 (§2.1)
5. ★ **B1 完了** — `verification.md` (EN/JA) に全チャネル σ(β,Δ) 比較の層別表。⚠ **「108 元素」は誤りで
   81 元素 (Z = 6–86)・525 チャネル** (108 は先方 DB の収録数) — 16 箇所を直した
6. ★ **B3 の direct-measurement 完了** — v3/v5 の表を ReciPro の実経路へ通して**サイト比 2.45e-03**
   (1 配置。`prescription_impact_2026-08-19.md` §3.3)。n_q 1216 の補間影響は直接測定で **4.74e-07**
   (5.6e-08 は取り下げ)。l_cap=128 は C/N/O K × 高 E₀ で張り付くが F(s) への影響 ≤ 8.3e-05 相対

---

## 1. ★ 最初にやること

```bash
cd /c/Users/seto/source/repos/Temari
git log --oneline -6          # 115f229 … 5d325ab … b5c2b01 … 43c8c40
ls -la ../cert_v2_pilot_lane*.jsonl          # pilot の出力 (§2.2)
julia +1.11 --project=. tools/certify_sigma_v2.jl ../cert_v2_pilot_lane*.jsonl --summary
```

**読む順**:

1. `docs/notes/window_quadrature_2026-08-19.md` **§8** — 今夜の実測と決定 (床の正体・規則・実装・認証)
2. `docs/notes/certification_v2_preregistration_2026-08-19.md` — 事前登録 (標本・合否・限界・費用)
3. `docs/notes/sigma_beta_delta_contract_2026-08-18.md` **§5A.7** — 数値観測値の 3 層 (単純加算しない)
4. `tools/sigma_beta_delta.jl` の冒頭 — 候補規則の表

⚠ 前の 2 本 (`next_chat_2026-08-20.md` §8 の「撤回した主張 6 件」) は今も有効。今夜も 1 回外した
(§5 の 7 件目)。

---

## 2. ★★★ 作者判断 — 候補規則の採否と、deep 認証の起動

### 2.1 決めた規則 (⚠ 採否は作者。⚠ pilot で大 β × 高 ε の弱点が出たので **v2 の中身から判断**)

**v1 (pilot で走った規則) → v2 で変える候補 (事前登録は新版として別に commit してから pilot を回し直す)**:

| | v1 (pilot) | v2 案 |
|---|---|---|
| 連続状態 | ppw 25 / dt_log 2e-3 / l_cap 96 / sig 1e-12 (= PROD、**出荷値ではなかった**) | **出荷 HIGH と同じ** ppw 30 / dt_log 1e-3 / l_cap 128 / sig 1e-13 |
| l_max | src の式 (ε ごとの階段、m = min(r_core, 6/Z)) | **κ に比例させて 6/Z 上限を外す** (例 ⌈κ·r_core⌉+12。`tools/sigma_beta_delta.jl` の `eps_setup_lmax` で可能。階段の段が小さくなり打ち切り誤差が消える。費用は重元素 d 殻の高 ε で最大 7×) **または** 切替点をパネル境界に (階段だけ消える) **または** v1 の主張域を β ≤ 30 mrad / Δ₂ ≤ 5 keV に限る |
| 配管 | 行単位 flush | **窓ごと flush** (実施済。Ca M1 @400 が 2 時間超で watchdog に殺された) |

⚠ `l_max_policy = :window_max` (窓の上端の l_max を全ノードに) は**不可** — 低 ε に高 l を強いると
src の Coulomb 関数が DomainError で落ちる (Fe K、l=42)。κ に比例する形にすること。

| | 候補 v1 |
|---|---|
| 窓 | θ = atan(√ε, √(ε_max−ε)) 上の**等比 16 パネル × GL16** (256 点)。θ₁=0 は最下パネル [0, 1e-4·θ₂] + 15 等比 |
| ε_c | 参照関数の切替点 (37.8371 keV) を**パネル境界に追加** |
| 角度 | PCHIP 継ぎ目 + `bt2=0` 分割 **GL12**、`Q² = dq²·e^x`、環状は直接区間 |
| n_q | **1216** (直接測定で補間影響 4.74e-07。2432 は単価 2 倍でオラクル用) |
| ppw | 25 (⚠ = PROD。出荷 F(s) は HIGH の 30。床 ~1e-08〜3e-08 は ppw の離散化から来る) |
| 規格化 | own と Bote の両方を常に返す。分母 = 同じ規則で [0, ε_max]・β = π |

根拠: `window_quadrature_2026-08-19.md` §8.1–8.4 (5 行 × Δ₁ 3 × 幅 6 = 90 条件、基準の健全性を別座標で
毎行確認)。⚠ **sin²θ 単一 GL256 は壊れる** (6.9e-05)、√ε 単一 GL512 なら床だが点数 2 倍。
⚠ 座標 (√ε vs sin²θ) の差は実測では見えない — sin²θ を採るのは上端 ε_max の √ 分岐点も正則化する理論上の
利点のため (codex 推奨)。

### 2.2 pilot (sentinel 11 行) — 完了 (§7、正本 = `certification_v2_pilot_2026-08-19.md`)

事前登録 §2.1 の sentinel 行 (Xe M4 @400/170、Au M5 @200、Ca M1 @400、
C K @400/30、Fe K @200、Zn M3 @300、Rn M5 @300、Ag L1 @200、I M2 @200) × 最大 18 窓 × β 8 本。
10 レーン × 1 スレッド、2026-08-19 19:25 起動。

```bash
julia +1.11 --project=. tools/certify_sigma_v2.jl ../cert_v2_pilot_lane*.jsonl --summary
```

結果 = §7。層別は `python tools/cert_v2_report.py "../cert_v2_pilot_lane*.jsonl"`。

### 2.3 deep の起動 (⚠ 作者判断。起動前に `git status --porcelain -uno` が空であること)

```bash
cd /c/Users/seto/source/repos/Temari
STAGGER=45 bash tools/run_cert_v2_fleet.sh deep 16 1      # 16 レーン × 1 スレッド、v1 で 3.4〜4 日 (v2 なら 5〜8 日)
julia +1.11 --project=. tools/certify_sigma_v2.jl ../cert_v2_deep_lane*.jsonl --summary
```

- ⚠ 走行中は `tools/{sigma_beta_delta,angular_split_v2,angular_sweep,beta_spike,certify_sigma_v2}.jl` を
  1 byte も触らない (`CERT_FP_V2` が変わり済みの行が捨てられる)
- ⚠ 事前登録の合否 (rtol 1e-7 + atol 1e-9·σ_ref) を**事後に変えない**。不合格は不合格として記録
- 費用を下げる案 (codex の優先順): 重複する窓を 1 回だけ評価 / σ_ref と Bote 分母の再利用 / 無効窓を
  状態構築前に除く。⚠ **E₀ の中央を落として端だけにしない** (標本を広げるたびに悪化した履歴と逆向き)
- `sentinel` プロファイル (全格子 × [0,1000] + [0,ε_max]、オラクル無し、約 40 時間) は別判断

### 2.4 まだ blocking なもの (契約 §5A.6 の続き)

| | 内容 | 状態 |
|---|---|---|
| 1 | `observed_max` は 2 規則の一致統計 ⇒ 狭い構造は差から消える | **変わらず**。P–O は床 (ppw) の上でしか見えない |
| 2 | 角度の 1 点標本 | v2 は行に 2 点 (50 eV、0.05ε_max) + 28,800 組の掃引。縦は閉形式オラクル |
| 3 | ε_c の段差 | **測定済** (§8.1)。段差 4e-06〜2e-05、全域 σ へ ≤ 2.5e-09。パネル境界で対処 |
| 4 | 新規則の再認証 | **pilot 完了 (大 β × 高 ε で不合格 32 窓)。候補 v2 と deep は作者判断** |

---

## 3. ★★ 今夜の実測 (正本 = `window_quadrature_2026-08-19.md` §8)

| 何 | 結果 | 道具 |
|---|---|---|
| ε_c の段差 | 相対 4e-06〜2e-05 実在、σ 寄与 ≤ 2.5e-09 | `tools/eta_bessel_probe.jl` |
| n_q 補間 (直接) | 1216 で 4.74e-07 (Ca M1 @400 ε=2000 eV)。5.6e-08 は取り下げ | `tools/nq_interp_direct.jl` |
| 窓の座標 × 規則 (90 条件) | 等比 16×16 (√ε も sin²θ も) ≤ 3.6e-08。単一 sin² GL256 は 6.9e-05。旧規則 1.00 | `tools/window_coord_probe.jl --width2` |
| 床の帰属 | **ppw** (n_q 不変、ppw で 22〜1200×)。共通 Q 格子は効かない | `tools/window_floor_probe.jl` |
| ノード単価 | n_q 240: 0.04–0.12 s / 1216: 0.16–0.40 s / 2432: 0.3–0.8 s (角度は <1 ms) | scratchpad |
| 候補 vs 出荷 σ_own | Fe K @200 で 4.6e-06 (出荷経路の既知の求積誤差の大きさ) | scratchpad |
| l_cap 張り付き | C/N/O K × ≥300 keV で張り付く (HIGH l_cap=128)。F(s) 相対 ≤ 8.3e-05、絶対 ≤ 1.8e-07 | `tools/lcap_saturation_probe.jl` (`speedup_v4` §8) |

---

## 4. ★ docs 側で終えたこと

- **B1**: `docs/src/{en,ja}/verification.md` に σ(β,Δ) 全チャネル比較の節 (層別 26 行、除外 29、
  「作者が回した比較」、「ρ 帯 GOS 比と別量」、0.83–1.11 の撤回)。比の CSV =
  `verification/sigma_ratio_zhang_2026-08-19.csv`。`tools/sigma_vs_zhang.py --strata --csv`
- **「108 元素」→ 81 元素 (Z = 6–86)・525 チャネル** を 16 箇所 (外部ゲート note・契約・CLAUDE.md・08-20)
- 撤回済み主張の残存 5 箇所に訂正注 (certification §5.4 / 契約 §10 / window_quadrature §0 / 08-21 §1.2 /
  nq_nx §14.5 の存在しない参照)
- 和文の強調破れ **124 箇所を 0 に** (notes 76 + handover 48)。`md_emphasis_check.py` は UTF-8 出力に
- 小物: Ag +0.5 % 解決済の注記 / zenodo_deposit (factors 行・§5 別レコード) / factors_release_README の
  2 文 / docs/README「Old paths」(Five → Three + 解消済 2) / IMPORT.md / CONTRIBUTING・PR テンプレの
  bitident 4 コマンド / speedup_audit verdict (16 → 6 件) / A9 ALCHEMI 文 / A11 Mott 3 択 / B8 runner
  (`b8_make_vectors.jl --verify`、50/50 PASS + 負のテスト) / certify_endpoints ヘッダ / 使用済み
  タスクスケジューラ 8 件を削除 / docs/README の索引に window_quadrature と事前登録を追加
- **B3 direct**: ReciPro 側 `tools/AlchemiCheck/PrescriptionTests.cs` (`sens-v3v5`、tools ローカル repo
  `1fbebd7`)。⚠ ReciPro 本体の working tree には私以外の変更 (ReciPro.csproj、サブモジュール) があり、
  触っていない

---

## 5. ⚠⚠ 今夜の撤回 (1 件) と、踏んだ罠

| 撤回した主張 | 何が誤りだったか | 教訓 |
|---|---|---|
| 「1e-08 の床は n_q=240 の Q 表補間誤差」 | 2×2 で n_q は効かず ppw が効いた | [[separate-hypotheses-with-2x2]] — 競合仮説を同時に振る |
| 「ppw 25 / n_q 240 / n_x 64 は出荷値」(08-20 §4 の表のラベルを信じた) | 出荷 F(s) は HIGH (ppw 30 / n_q 360 / n_x 96)。PROD は σ(β,Δ) スパイクの設定 | 出荷 JSON の `settings` を読んでから書く |
| 「大 β の不合格は l_max の階段」で止めかけた | 階段の下に**打ち切り誤差そのもの** (Xe M4 30 keV で 7 %) があった。`:window_max` で値が 8e-04 動いた理由を追って見えた | 「差が消えた」で止めず、値がどちらへ動いたかを追う |

- ⚠ **codex は未 push の commit を読めない** (Windows sandbox がプロセス起動を拒否)。2 回とも GitHub の
  main を読んで設計レビューになった。コードを見せたいときは push するか本文に貼る ([[codex-sandbox-cannot-read-local]])
- ⚠ **規則を上げると認証費用が積で跳ねる** (点数比 × 単価比)。全格子 20〜40 日 ⇒ 二階建て (sentinel + 層別)
  へ ([[certification-cost-scales-with-rule]])
- ⚠ Bash heredoc に Python の三重引用符を入れると bash が途中で切れる — 長い Python は Write でファイルへ
- ⚠ `.NET` の console は cp932 で和文が化ける — PowerShell で `[Console]::OutputEncoding = UTF8` にして実行

---

## 6. ★ 「これ以外の残作業」の掃引結果 (作者の追加指示)

72 エージェントで handover 全 19 本・notes・CLAUDE.md・memory・docs サイトを掃き、敵対的検証を通った
未完了項目を 3 群に分けた (正本は scratchpad の報告。要点だけ):

**(1) できる (作者不要) — 今夜やった**: 上の §4 (撤回注・108 元素・強調・小物・B8 runner・l_cap 張り付き・
B3 direct)。**残り**: 尾根帯 δF(s) の s 分解 (上界 2.9–7.7e-02 のまま) / speedup_audit の残り 6 件 /
Levinson 定理の検査 / F(s) 側の roughness 伝播 (1.5e-10) / GUI v0.1 の既知の限界 3 点 /
ReciPro `TruncationBound` の 2 領域化 / SIGMAK/SIGMAL 比較 (σ(β,Δ) 本実装後)。

**(2) 作者判断・作者ログインが必要** (中): Mott σ_el 残差 6–9 % の帰属と製品化 / ReciPro 要望 P3–P5 の
採否記録 / methods・dataset 論文の計画 / 契約 §5B ゲート値 + n_x 64/96 の版管理 (08-20 §4 は 64、契約 §7.5 は
96 — **どちらが出荷か先に一致させる**) / v3 利用者向け文言の掲出先 / Bethe 尾根を追うか取り下げるか /
ホスト安定性の切り分け (memtest・DIMM 2 枚・4400 MT/s 再試験) / OA 独立検証トラック / **A13 → B7 Zenodo**
(前から) / B6 Mott CDF の前方刻み (前から) / MANIFEST v5 :114 の「約 3 倍」書き換え可否 (前から)。
(低): E2/E3 を閉じるか / 公開 API seam / スピン分極 SCF / 監査の空き 4 レンズ / factors 契約テストの CI /
frozen worktree 3 本の削除 / roadmap の σ(β,Δ) 行 / 次世代の Julia ピン。

**(3) 凍結で次の全再生成まで不可**: F v6 世代に束ねる台帳 (等比 s 格子 / R 表の E₀ 行間共有 / 閾値近傍
E₀ ノード / 混合 Q 横断項 / P3-1 / −Re(DX*) / ppw / 処理系 / src コメント・旧パス 34 箇所 /
`l0_numerics.jl:38-42,87` / **l_cap 256 (今夜の実測)**) — 08-18 §4 の順序を次正本へ転記する /
factors v2 束。

---

## 7. pilot の結果 (2026-08-19 19:25–21:30、10 レーン × 1 スレッド)

正本 = `docs/notes/certification_v2_pilot_2026-08-19.md` (層別表込み)。要点は §0-4。
再集計: `python tools/cert_v2_report.py "../cert_v2_pilot_lane*.jsonl"`。
⚠ pilot の JSONL は旧指紋 (`1b8fc01a654f03f7`) — その後 `certify_sigma_v2.jl` (flush・層別キー) と
`sigma_beta_delta.jl` (`l_max_policy` 診断経路) を変えたので、次の走行は新指紋になる。

### 7.1 ★★★ 最優先の測定 (作者判断の前にできる、src は触らない)

**src の部分波打ち切り (l_kin の 6/Z 上限) が出荷 F(s) に残す誤差を F 側で直接測る**:
`compute_NK` 相当を tools に写して l_max を上げ (κ に比例)、Xe M4 / Au M5 / Ba M4 @ 200–400 keV の
N(0) と F(s) (特に s ≥ 4) の変化を取る。百分率級なら F v6 の処方変更 (m の定義) と MANIFEST への注記が要る。
⚠ 高 l を低 ε に強いると Coulomb 関数が落ちるので、l_max は κ に比例させる。

---

## 8. 道具 (今夜足したもの)

| ファイル | 中身 |
|---|---|
| `tools/sigma_beta_delta.jl` | ★ 本番候補 (規則・検証・診断・own/Bote) |
| `tools/certify_sigma_v2.jl` + `tools/cert_v2_watchdog.sh` + `tools/run_cert_v2_fleet.sh` | ★ 認証 v2 (1 窓 = 1 JSONL 行、window_id、out_of_domain、部品別指紋) |
| `tools/window_floor_probe.jl` | 床の 2×2 分離 |
| `tools/window_coord_probe.jl --width2` / `:sin2geo` | 基準を等比パネルに替えた幅の実験 |
| `tools/lcap_saturation_probe.jl` | l_cap 張り付き |
| `tools/sigma_vs_zhang.py --strata --csv` | 外部比較の層別と CSV |
| `tools/b8_make_vectors.jl --verify` | B8 fixture の runner |
| `ReciPro/tools/AlchemiCheck/PrescriptionTests.cs` | `sens-v3v5` (B3 direct) |
| `docs/notes/certification_v2_preregistration_2026-08-19.md` | 事前登録 |

⚠ 中間出力はリポの外 (`../qcamp/`、`../cert_v2_pilot_lane*.jsonl`)。

## 9. 参考: 本セッションの commit

```text
b5c2b01  Put the full-grid external comparison on the site, and clear the sweep of small debts
5d325ab  Apply the codex review to the σ(β,Δ) candidate before the pilot
115f229  Record the night's measurements: the floor is ppw, the rule is decided, B3 measured directly
```
