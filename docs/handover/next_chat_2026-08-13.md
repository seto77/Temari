# 次チャットへの指示書 (2026-08-12 19:50 作成)

> ⚠⚠ **本書は `docs/handover/next_chat_2026-08-14.md` に置き換えられた** (T_comp,e の
> 作者決定 = 案 A を含む)。次チャットはそちらを読むこと。
>
> ★ **追記 (2026-08-14 04:30): §2-1 (完走確認・集計) と §2-2 (文書化) は実施済み。**
> フリートは 2026-08-14 04:09 に 86/86 で完走し (certified 83)、
> 実行報告 = `docs/notes/grid_certification_l1_run_2026-08-14.md` (`01ce954`)。
> 残るのは §2-3 (Tm/Yb の当初規則走 — 優先度は下がった。L¹ で両方 certified 済み)、
> §2-4 の表 (端点切断・標本 14 元素・T_comp,e・OA)、および
> **H の分類順序バグを直す規則 v2.1 の起草** (実行報告 §3)。

⚠ 本書は「次に何を、どの順で、どう気をつけてやるか」だけを書く。前版
(`docs/handover/next_chat_2026-08-12.md`) の課題は本日ほぼ消化した。根拠と実測は正本を読むこと:

| 正本 | 内容 |
|---|---|
| **`docs/notes/grid_certification_preregistration_v2_2026-08-12.md`** | ★ **凍結済みの事前登録 v2** (L¹ 認証、`ebe585c`)。⚠ **フリートが走り切るまで 1 文字も変えない** |
| `docs/notes/grid_certification_run_2026-08-12.md` | v1 の実行報告 + 本日の追記 (f_e 修正・起動方法確立) |
| `docs/handover/handoff_scattering_factor_2026-08-11.md` | トラックの現在地 (§2 の残作業表) |
| `c:\tmp\codex_question_l1_preregistration_2026-08-12.md` | codex 3 巡の裁定記録 |

## 0. 冒頭で確認すること

```bash
# L¹ フリート (8/13 01:00 発火、約 15 h・8 レーン見込み) の状態
ls c:/tmp/temari_certify_l1_2026-08-13/z???.json 2>/dev/null | wc -l   # /86
tail -3 c:/tmp/temari_certify_l1_2026-08-13/launch.log 2>/dev/null
tail -3 c:/tmp/temari_certify_l1_2026-08-13/fleet.log 2>/dev/null
powershell -NoProfile -Command "Get-ScheduledTask -TaskName Temari_dt32_allZ | % State"
cd c:/Users/seto/source/repos/Temari && git log --oneline -3   # 先頭 = ebe585c
```

## 1. 本日 (8/12) に確定したこと

1. **★ 長時間計算の起動方法を確立した** — タスクスケジューラの**トリガー自然発火**
   + **VBS 非表示起動** (`wscript //B` + `Run(..., 0, False)`)。10 分試走で検証し、
   本日の全フリート・パイロットがこの方式で走った。⚠ **VBS は ASCII のみ**
   (cscript は BOM 無し UTF-8 日本語で行構造が壊れる。実測)。⚠ **nohup は
   セッション落ち・PC 再起動の道連れ** (18:37 の再起動で実証)。
2. **★ 事前登録 v2 を凍結し (`ebe585c`)、8/13 01:00 のタスクを L¹ フリートへ
   差し替えた** (`l1_launch.vbs` → `launch_l1.sh` → `run_l1_fleet.sh` →
   `tools/certify_l1.jl`、制限 36 h)。設計はパイロット 4 本が作り直した:
   - **bound = 1.1·‖Δρ₃‖₁·`q*/(1−q*)` + 1e-13** (収縮モデル + 10 % 安定性余裕)。
     ⚠ ‖Δρ₄‖₁ は τ/10 では停止残差しか測れない (β 変更で +121 %) — q₃₄ は
     信号条件付き診断へ降格
   - **τ/100 は棄却** — Fe/Au はどの mixing でも届かない (P3)。閾値は τ/10
   - **ラダーは元素単位で β 統一**: 全水準 (β0.2, 1200) → 1 水準でも未収束なら
     全水準 (β0.08, 2400) で解き直し。⚠ 水準ごとに β を混ぜると格子差に停止点差が
     混入する
   - **β ストレス試験 5/5 通過** (C/Ar/Fe/Xe/Au、D₃ 変動最大 3.31 %)
   - 判定は五値: certified / uncertifiable / uncertifiable_low_signal (‖Δρ₃‖ ≤ 1e-9、
     H/He 級) / uncertifiable_scf / model_violated
3. **f_e の切替点整合を修正した** (`99fe985`)。原因は 3 種 (H = 信号ゼロ / Al =
   展開の収束破れ / Tb = S/N 55 の床揺らぎ)。1 % 規則 + 上限 0.02 + S/N 依存許容で
   **83 検査可能元素の許容超えゼロ**。⚠ **T_comp,e の値は作者判断待ち**
   (アンカーは用途。格子最悪 3.817e-08 Å / 停止最悪 2.640e-08 Å)
4. **`pointwise_bound` の複製を統合し、certify_grid の集計に欠落突き合わせを足した**
   (`d46d6bd`)。当初規則 (v1) の一次結果は **46/84 合格** (Er 追加。最悪上限 =
   Er の 0.221×B_grid、未収束起因の ❌)
5. **Er 68 の当初規則走が完了** (stage 4 のみ未収束 — Tm/Yb と同じ β0.2/1200 の壁)。
   ⚠ **Tm/Yb は PC 再起動 (18:37) で中断し、未再走** (§2-3)

## 2. 次チャットの作業 (この順で)

### 2-1. L¹ フリートの完走確認と集計

```bash
julia tools/certify_l1.jl --aggregate c:/tmp/temari_certify_l1_2026-08-13
```

- 見込み: 01:00 発火 → **~16:00 完走** (τ/10・8 レーン、約 15 h。⚠ ラダー試行 2 の
  元素は 2 倍かかる)。欠落があれば aggregate が exit 1 する
- 報告は**層別** (§4: L¹ 設計 = C/Ar/Xe / ラダー設計 = H/Tm/Yb/Fe/Au / 前向き 78)
- ⚠ 言い換えの規約は事前登録 §8。「証明」「厳密上界」は使わない
- ⚠ フリートが失敗していた場合: `run_l1_fleet.sh` は JSON があれば skip するので、
  同じタスクを再登録して再発火すれば続きから走る (起動方法は §1-1 の方式で)

### 2-2. 結果の文書化

実行報告 v2 (`grid_certification_run_2026-08-13.md` 等) を書き、
`handoff_scattering_factor` の冒頭を更新する。⚠ 事前登録 v2 本文は変えない
(結果は実行報告へ。事前登録の「実行後の追記」枠は v1 の前例に倣う)

### 2-3. Tm/Yb の当初規則走の再投入 (レーンが空いてから)

`run_remaining.sh` を §1-1 の方式で再発火 (Er は skip される)。⚠ 得られるのは
原因調査の材料のみ (❌ 確定)。⚠ L¹ フリートと同時に走らせない (8 レーン上限)

### 2-4. 残りの片付け

| 作業 | 注意 |
|---|---|
| 端点切断 (`certify_endpoints.jl`) | 実装済・未実行。dt 細分では測れない別誤差源 |
| 標本 14 元素の `--prod-all` と τ/100 | ⚠ **τ/100 は Fe 級で届かない** (P3 実測)。「届く元素だけの診断」に目的を再定義してから回す |
| Ar の τ 締め差 (τ/100) | P2 は再起動で中断・未再開。τ 締め差の標本は C 1 元素のみ (事前登録 §7 に明記済み)。補強するなら Ar 1 元素の τ/100 を回す — ⚠ 非常に深い (stage 4 で 5507 s) |
| T_comp,e | **作者判断待ち** (§1-3) |
| OA 2000/2001 独立検証 | 未着手。F(s,E₀) 側の計算が要るのでフリートと同時不可 |

## 3. ⚠ 運用規律 (前版から変わったもののみ)

- **起動は §1-1 の確立方式で** (「起動方法がまだ無い」は解消済み)
- **並列は 8 レーン** (作者指示、継続)
- **フリート実行中はリポジトリを触らない** — 特に `certify_l1.jl` と、それが
  include する `certify_grid.jl`。⚠ 凍結版 = `ebe585c`
- **PC 再起動はあり得る** (本日 18:37 に実際に起きた)。スケジューラのタスクは
  生き残る。プロセスの死は JSON の有無で判定し、`run_*.sh` の skip 機構で再開する
- **測定器の信号がその測定の停止許容より小さいなら、その量は測れていない**
  (‖Δρ₄‖ の教訓。比・分類を作る前に信号/床の比を見る)
