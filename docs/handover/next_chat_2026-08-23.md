# 次チャットへの指示書 (2026-08-20 未明に執筆。次のチャット = 08-23 頃の想定) — 08-22 指示書を一晩で実行した結果

**⭐ 冒頭でこれを読む**。前段 = `docs/handover/next_chat_2026-08-22.md` (候補 v1 の pilot と部分波打ち切りの発見)。
作者の指示「指示書に従って進める。Codex と深い議論。朝 7 時まで CPU をフルに使ってよい。30 分ごとに進捗報告」
(2026-08-19 22:4x〜) で一晩走らせた。本書がその結果と、次に何をどの順でやるか。

⚠ 本書には**実測**と**作者指示・作者判断**と**私 (Claude) の解釈**が混ざる。区別して書いた。
⚠⚠ **本セッションで src を変えた** (作者指示による。§2)。出荷済み v5 データは不変だが、HEAD で生成すると v5 は再現
できない (再現は `TEMARI_LEGACY_V5_CUTOFF=1` の検証ゲート経由のみ)。

## 0. 七行で

1. ★★★ **作者指示 (02:2x)**: 「Temari は発展途上でほぼ誰にも使われていない。src 指紋や公開済みデータセットが変わる
   ことを気にしなくてよい (DOI は取り直す)。**資産を守ることに執着せず、新しく正確な物理量を計算することに重きを置く**」
   → CLAUDE.md 冒頭と memory `assets-are-not-sacred` に記録。**以後の既定 = 処方の欠陥は src を直して再生成**
   (検証の規律は緩めない)
2. ★★★ **出荷 F(s) (dataset v5) の部分波打ち切り感度を全 525 チャネルで測った** (`tools/lkin_sweep.jl`、出荷経路の
   ビット一致の写し): **M1 (3s) は F の絶対差 +1.65e-03 (s ≈ 0.15–0.3、Z = 30–41 @400 keV)、σ_own +5.7e-03 / M2・M3
   1.3e-03 / 4e-03 / M4・M5 6.3e-04 / 1.2e-03 / 軽元素 L 殻 1.6e-04 / 6e-04 / K ≤ 3e-07**。「試験した、より収束した側」
   への処方差 (aggressive reference との差 ≤ 1.1e-05)。正本 = `docs/notes/lkin_truncation_2026-08-19.md` §6
3. ★★★ **指示に沿って src の部分波規則を v6 にし、HIGH の数値設定も上げた** (`381e777` → `056aa89` → `1dcaf5a`):
   `l_kin = ⌈κ·r(含有率 0.999)⌉ + 12` (Dirac 経路は G²+F² の含有半径)、**HIGH v6 = l_cap 256 / n_x 192 / n_phi 96 /
   n_q 720** (v5: 128 / 96 / 48 / 360)。要因計画 (`tools/lkin_rule_study.jl`、18 行 × 24 規則、2 段の参照) で部分波を選び
   (**cap が律速**、v6 規則は aggressive reference に対し ΔF ≤ 4.1e-07、計算量 ≈ 3.5 倍)、改訂した監査で**次の残差が
   3s/3p の角度求積 (7e-06) と n_q** と分かり、n_x 192 / n_phi 96 / n_q 720 で ≤ 8e-08 (+45 %) にした
   (`lkin_truncation_2026-08-19.md` §6.5–6.6)。**検証ゲート: `TEMARI_LEGACY_V5_CUTOFF=1` で旧コードとビット同一
   (QUICK 12 チャネル + HIGH 7 チャネル、差分ゼロ)、selftest ALL PASS、破損行 fail-closed の負のテスト ALL PASS**。
   `dataset_version` は v6 の範囲が凍結されるまで `6.0.0-dev` を名乗る (§3.1)。**再生成の計算量 ≈ v5 の 5 倍 ⇒ ≈ 1.5 日**
   (代表チャネルの完全生成で実測中 — §1 の表)
4. ★★ **σ(β,Δ) 候補 v2 の pilot**: v1 の不合格 32 窓のうち β ≤ 100 mrad は全部床へ。残る β = 200 mrad の不合格は
   2×2 で**純粋な窓求積の不足** (margin・ppw・cap に不変、32/48 パネルで 2e-09)。パネル数掃引で 24 が足りた →
   **v3 (24 パネル、オラクル 32) を事前登録して pilot を 03:27 に起動**。途中集計 (05:10、108/≈190 窓): **全窓合格、
   β = 200 の最悪 4.8e-08** (v2 で 3.9e-06 だった Ca M1 [0,1e5] が 4.8e-08、Zn M3 [0,1e5] が 1.1e-09)。最終形は
   **v4 = src の v6 規則 (:src) + 24 パネル** (codex: F と σ を 1 つの処方に)。deep は v4 の pilot の後 (作者判断)
5. ★ **用語**: 作者指摘で「契約」→ **仕様 / 保証 / 規約 / 適合テスト**、「数値予算」→ **数値誤差の内訳**、経済用語を
   避ける (memory `terminology-contract-to-specification`)。一括置換 (358 箇所 + ファイル名) は次セッション (§5-6)
6. ★ codex と 3 巡 (設計 → 解釈 → src 変更のレビュー)。3 巡目で直した: 2 成分含有半径 / チェックポイント文脈に規則を入れる /
   破損行を fail-closed / 監査の cap 方向 / `6.0.0-dev` / 単一の legacy スイッチ / C16 の旧ファイル既定
7. ⚠ 踏んだ罠: s = 16 が E₀ ≤ 35 keV で運動学的上限の外 (279 行再計算) / LK_S 10 点は峰を 3〜13 % 過小評価 (321 点で
   測り直した) / margin +48 は低 ε で Coulomb 関数が DomainError / Bash heredoc (三重引用符・`\n`) 3 回 → memory 更新

---

## 1. ★ 最初にやること

```bash
cd /c/Users/seto/source/repos/Temari
git log --oneline -20
# 打ち切り感度 (v5 出荷) — 確定値
julia +1.11 --project=. tools/lkin_sweep.jl ../qcamp/lkin_sweep_lane*.jsonl --summary      # LK_S 10 点、1083 行
julia +1.11 --project=. tools/lkin_sweep.jl ../qcamp/lkin_full_lane*.jsonl --summary       # 出荷 s 格子 321 点、M × 最大 E₀
# F v6 の部分波規則の要因計画
julia +1.11 --project=. tools/lkin_rule_study.jl ../qcamp/lkin_rule3_lane*.jsonl --summary
# pilot v2 (完走) / v3 (走行中か確認: ../cert_v3_pilot_lane*_log.txt の COMPLETE)
python tools/cert_v2_report.py "../cert_v2b_pilot_lane*.jsonl"
python tools/cert_v2_report.py "../cert_v3_pilot_lane*.jsonl"
julia +1.11 --project=. tools/certify_sigma_v2.jl ../cert_v3_pilot_lane*.jsonl --summary
# 診断ログ
cat ../qcamp/znm3_diag.log ../qcamp/cam1_panels.log
```

| 走行 | 起動 | 状態 (本書執筆 04:30) | 出力 |
|---|---|---|---|
| `lkin_sweep` (v5 src vs krc12 (vs krc32c256)、LK_S、525ch) | 08-19 22:53 | **完走** (1083 行 + 例外行 279 の再計算) | `../qcamp/lkin_sweep_lane{0,1,2}.jsonl` |
| `lkin_sweep --sgrid full` (M × 最大 E₀、321 点) | 08-20 01:35 | 終了 (272 行。⚠ 予定 279 行のうち M5 の 7 行が未計算のまま — 再開は同じコマンドで) | `../qcamp/lkin_full_lane{0,1}.jsonl` |
| `lkin_rule_study` (18 行 × 24 規則) | 03:12 | **完走 18/18** (04:20) | `../qcamp/lkin_rule3_lane{0,1,2}.jsonl`、集計 `lkin_rule_study_final.txt` |
| pilot v2 (sentinel 11 行) | 08-19 23:14 | **完走** (仕様内 192 窓 = 合格 187 / 不合格 5 (β=200: Ca M1 ×4, Zn M3 ×1) + 仕様外 6) | `../cert_v2b_pilot_lane*.jsonl` |
| **pilot v3** (24 パネル) | 03:27 | 走行中 (33 窓 04:05)。最長レーンは 07:00 を越えうる | `../cert_v3_pilot_lane*.jsonl` |
| Zn M3 2×2 / パネル数掃引 (scratchpad 台本) | 00:37 / 01:38 | **完了** | `../qcamp/znm3_diag.log`, `cam1_panels.log` |

⚠ pilot の走行中に `tools/sigma_beta_delta.jl` と `certify_sigma_v2.jl` を変えた (v3 の追加、src_lmax の追従) ので、
**落ちて再起動したレーンだけ別指紋**になる (`--fp` で分けて集計)。走行中プロセスの記録は起動時の指紋のまま。

**読む順**: `lkin_truncation_2026-08-19.md` §6 → `certification_v2_pilot_2026-08-19.md` §6 →
`certification_v3_preregistration_2026-08-20.md` → 本書 §2–§5。

---

## 2. ★★★ 作者指示と、それに沿って変えた src (2026-08-20)

### 2.1 指示 (原文の要旨)

- 02:2x: **資産保護より正確な物理量** (§0-1)。
- 03:0x: 用語 — 「契約」は二者間の拘束力ある合意で自分の規則には不適切 → 仕様/保証/規約/適合テスト。「数値予算」→
  数値誤差の内訳。不自然な経済用語を避け科学的用語で。**「契約が要らないような、普遍的で安全で誰でも誤解なく使える
  コードを提供することが重要」** (入力検証・単位と座標の明示・範囲外の明示的な拒否で、読み方を別文書で約束しなくても
  誤用できない設計)。

### 2.2 src の変更 (commit `381e777` → `056aa89`)

| ファイル | 何 |
|---|---|
| `src/l0_numerics.jl` | `LKIN_RULE` (:v6 既定)、`LKIN_RADIUS_FRAC = 0.999`、`LKIN_MARGIN = 12`、**HIGH v6 = l_cap 256 / n_x 192 / n_phi 96 / n_q 720** (v5: 128/96/48/360)。`TEMARI_LEGACY_V5_CUTOFF=1` で v5 の組にまとめて戻る (検証ゲート専用)。`settings_dict` に `lkin_rule / lkin_radius_frac / lkin_margin` |
| `src/l5_channel.jl` | `lkin_partial_waves()` (v5: ⌈κ·min(r_core, 6/Z)⌉+12 / v6: ⌈κ·r_eff⌉+margin、r_eff = 含有率 frac の半径。**Dirac 経路は G²+F²**、非相対論/SRC は u_b²)、`bound_containment_radius()`。`eps_setup` / `eps_worker` / `compute_NK` に kwargs `lkin_frac`, `lkin_margin` (監査で振る) |
| `src/l5_exit_edx.jl` | `compute_channel` が settings の `lkin_frac` / `lkin_margin` を渡す |
| `src/gen_production.jl` | `presc_dataset_version(p; lkin_rule, l_cap, n_x, n_phi, n_q, lkin_radius_frac, lkin_margin)`: v5 = (v5 規則, 128/96/48/360) → "5.0.0" / v6 = (v6, 256/192/96/720, 0.999, 12) → **"6.0.0-dev"** / QUICK・PROD → "0.0.0-dev"。`is_shipping_format` は S_GRID そのものを比較。チェックポイント文脈 (`production_context_sha256`) と出力 JSON の settings に規則が入る。**破損行 fail-closed** (2 度目も異常 / ppw 再試行後も異常 → その行は書かず failures に記録)。監査 (`audit`) の振り方: l_cap 256→320 / margin 12→20 / 含有率 0.999→0.9999 / 3 つ同時 |
| `tools/check_tables.jl` | C16 が JSON の settings から同じ引数で版を引き直す (lkin_rule の無い旧ファイル = v5、l_cap 128) |
| `tools/sigma_beta_delta.jl`, `lkin_truncation_probe.jl` | `src_lmax` は r_b/u_b を渡せば src の関数を呼ぶ (:src 経路の assert)。`:src_v5` = v5 の式 (出荷 v5 の基準)。`lkin_sweep.jl` の "src" は `:src_v5` に付け替え |

**検証 (実施済)**: (i) `TEMARI_LEGACY_V5_CUTOFF=1` で `tools/bitident_snapshot.jl` (QUICK、v3 5ch + v4 7ch) が変更前
commit `035471a` の worktree と**差分ゼロ** (2 回: `381e777` と `056aa89` の後) = 副作用なし [[temari-verification-gate]] /
(ii) v6 既定ではスナップショットが動く (意図した変化) / (iii) `selftest` ALL PASS (308 s, 300 s) /
(iv) 含有半径は u_b² と G²+F² で Au L3/M5・Zn M1・Xe M4 でも 5 桁一致 (小成分は内側に集中) ⇒ 要因計画 (u_b²) の結論は
そのまま G²+F² に持ち越せる。

**要因計画の要点** (`tools/lkin_rule_study.jl`、18 行、参照 refB = (r_core, +32, cap 320)、refA = (r_core, +32,
cap 256) は refB と ΔF 1.5e-07 で一致 = 参照は安定):

| 規則 | max\|ΔF\| (s ≤ 2) vs refB | \|ΔN0/N0\| | 計算量 (Σ l_max の refB 比、中央値) |
|---|---|---|---|
| src (v5) | 1.60e-03 | 5.7e-03 | 0.12 |
| r_core+12, cap 128 (= 候補 v2/v3 の `:kappa_rc`) | 1.1e-05 (cap 律速) | 3.3e-05 | 0.48 |
| r(0.99)+12, cap 256 | 2.7e-06 | 6.6e-06 | 0.37 |
| r(0.99)+20, cap 256 | 1.05e-06 | 2.5e-06 | 0.42 |
| **r(0.999)+12, cap 256 (= v6)** | **4.1e-07** | **9.1e-07** | **0.46 (v5 の ≈ 3.8 倍)** |
| r_core+12, cap 160 | 4.3e-06 | 1.1e-05 | 0.57 |

codex の推奨 = r(0.999)+12/cap 256 (量子化の半歩 5e-07 の内側、物理的に自然な半径、κ とともに伸びる)。

---

## 3. ★★★ 作者判断 (材料は揃えた)

### 3.1 (A) F v6 の範囲を凍結して再生成する

- 今の HEAD の v6 = **部分波規則 + HIGH の角度・Q 表 (n_x 192 / n_phi 96 / n_q 720) + l_cap 256**。F v6 の台帳 (08-22 §6 (3))
  の他の項目 — **等比 s 格子** (作者決定済、形式変更 + ReciPro 側)、ppw、src コメント・旧パス 34 箇所、`l0_numerics.jl:38-42,87`
  のコメント — を**今回の再生成に束ねるか**は作者判断 (M1 の n_q は 720 で対処済)
- codex: 版の名前は **処方 + 正確な S_GRID + schema + HIGH 全設定 (n1/n2/n3, l_cap, n_x/n_phi/n_q, sig_thresh, ppw, dt_log) +
  部分波規則 + 量子化の一式 (`V6_SPEC`) で定義**し、生成側の名乗りと C16 を同じ定義から引く。⚠ 今の暫定キー
  (l_cap, n_x, n_phi, n_q, 部分波) は ppw 等を見ないので **"6.0.0-dev" のまま本番を回さない**。凍結したら "6.0.0"
- **起動手順 (codex の順)**: ① 範囲の凍結 (等比格子 / ppw / コメント・旧パス — ⚠ src コメントの修正は指紋に入るので
  代表生成より**前**に) → ② `V6_SPEC` を書き、版の名乗りと C16 を同じ定義から → ③ commit、`TEMARI_LEGACY_V5_CUTOFF`
  が未設定であること、新しい出力ディレクトリ → ④ 代表チャネルの完全生成 + C1–C16、監査の再実行 (HEAD の HIGH で)、
  本番構成でのメモリ・時間 → ⑤ "6.0.0" に固定、clean tree の確認、起動
- **再生成の計算量 (代表チャネルの実測込み)**: v5 は 16 レーンで 6 h 47 min (≈ 26 s/行/レーン)。HIGH v6 で **Fe K 28 行 9.1 分、
  Zn M1 22 行 17.1 分 (8 スレッド、過負荷時。`../qcamp/gen_v6_test_{K,M1}.log`)** ⇒ 2 スレッド換算で K ≈ 1 分/行、M1 ≈ 3 分/行
  ⇒ 全格子で **≈ v5 の 4.5 倍 ≈ 1.3〜1.5 日** (32 論理コア、SCF キャッシュの作り直し +1〜2 時間を含む)。
  **QC**: 生成した 2 本に `tools/check_tables.jl` で C1–C16 合格 (C15 は部分集合のため想定内の NG、C16 の版の引き直しも一致)。
  **v5 との差**: Zn M1 max|ΔF| (s ≤ 2) = 1.63e-03 (s = 0.15、E₀ 350 keV)、σ_own +5.8e-03 / Fe K 2.1e-07 — 掃引 (§0-2) の予測どおり。SCF キャッシュは src 指紋が変わったので全部作り直し (+1〜2 時間)。⚠ 代表チャネル (Zn M1 / Fe K) の完全生成
  (`../qcamp/gen_v6_test/`、`gen_v6_test_{M1,K}.log`) — **済** (上の行)
- **起動前の確認 (codex の一覧)**: ① 破損行 fail-closed の負のテスト — **済** (`tools/sane_row_negative_test.jl` ALL PASS。
  ⚠ ppw 再試行後の経路はゲート違反を人工的に起こせないので未検査) ② 改訂した `gen_production.jl audit` — **済**
  (`../qcamp/audit_v6.log`: 部分波の振り ≤ 4e-07、8 ケース。⚠ HIGH v6 への更新 (1dcaf5a) 後に**再実行**すること) ③ cap 256 の
  最悪ノードを本番のレーン構成 (16 レーン × 2 スレッド) でメモリ計測 — 未 ④ 代表チャネルの完全生成 + C1–C16 — **済**
  (Zn M1 / Fe K、`tools/check_tables.jl ../qcamp/gen_v6_test` で 2 OK、C15 の欠けは想定内) ⑤ legacy ゲート + selftest — **済** (1dcaf5a)
  ⑥ **監査の再実行** (HEAD の HIGH v6 で。`../qcamp/audit_v6.log` は角度・n_q が旧基準) — 未 (範囲凍結後に)
- 起動: `tools/lane_watchdog.sh` 系 (v5 と同じ。`docs/handover/next_phase_2026-08-11.md` の手順)。**直前に commit** (dirty 警告)

### 3.2 (B) σ(β,Δ) の規則と deep

- v3 (24 パネル、`:kappa_rc`) の pilot の結果を見る (§1)。v3 は「パネル数だけ変えた実験」として残し、**最終候補は v4 =
  `l_max_policy = :src` (v6 規則) + 24 パネル + オラクル 32 パネル** を別に事前登録 → pilot (β = 200 の旧不合格 5 窓を
  含む) → deep (≈ 1 行 4 core·h × 1.5 ⇒ 14〜18 日。deep-lite は別名で −50 %、中央 E₀ は M 殻と広がった軌道で残す)。
  ⚠ σ ツールの n_q = 1216 は **src の 720 に合わせて下げない** (σ は継ぎ目分割の角度積分と独自の測定根拠を持つ。「共有する
  処方」= 物理と部分波の方針であって出口ごとの数値設定ではない)。v4 の事前登録に差を明記し、旧不合格窓で 720 vs 1216 の
  感度を 1 回測る (codex)。⚠ n_q 1216 の補間影響は「5.6e-08 (自己収束、5ch)」ではなく **4.74e-07 (直接測定、8 条件)** を引く
- `indicator` はパネル数の推定器に使えない (GL16 標本ではモード ≥ 16 が見えない)。固定 24 が簡単
- **ppw 30 の窓での処方感度 ~4e-07** (Zn M3 [0,1e5] β=200) は P と O に共通で P−O に出ない — 数値誤差の内訳に並記

### 3.3 (C) 出荷 v5 (公開中) の扱い

- 指示により v6 で置き換える前提。それまで v5 の MANIFEST / data.md に「部分波打ち切りの処方感度」を追記するかは作者判断
  (08-23 初版に載せた文案は git 履歴 `a0b...` の §3。数値は §6.3/6.4 の確定値: M1 1.65e-03 / M2・M3 1.3e-03 / M4・M5 6.3e-04 /
  σ_own 5.7e-03 / 軽 L 1.6e-04 / K ≤ 3e-07)。**「バイアス」「上界」と書かない** (処方感度、収束側への向き)

### 3.4 (D) 用語の一括置換

「契約」358 箇所 (66 ファイル) + ファイル名 4 本 (`*_contract_*.md`) + tools 5 本 (`temari_contract.py` 等は配布物同梱 →
リリースと同時) を 仕様/保証/規約/適合テスト へ。「数値予算」→「数値誤差の内訳」。作者の了承済 (03:0x)。

---

## 4. 今夜の実測 (正本はそれぞれの note)

| 何 | 結果 | 正本 |
|---|---|---|
| 打ち切り感度 (525ch、LK_S) | §0-2。F の感度は E₀ とともに増える (中央値 ×2.5〜3.3)、N(0) は弱い。30 keV でも M 殻 F 3〜8e-04 | `lkin_truncation_2026-08-19.md` §6.1, §6.4 |
| 同 (出荷格子 321 点、M × 最大 E₀) | M1 1.65e-03 (s 0.15–0.3) / M2 1.26e-03 / M3 1.28e-03 / M4 6.30e-04 / M5 6.35e-04 | 同 §6.3 |
| krc12 の aggressive reference との差 | 279 行中 263 行で ≤ 5e-06、超過 16 行 (Z=30–35 @400) でも ≤ 1.1e-05 | 同 §6.1 |
| 要因計画 (規則の選択) | §2.2 の表。cap が律速 | `tools/lkin_rule_study.jl --summary` (note は次セッションで §6.5 へ) |
| pilot v2 | β ≤ 60 最悪 2.2e-08、β=100 格子内 ≤ 3e-08、β=200 格子内 3.2e-07 (Zn M3) / 格子外 Ca M1 3.9e-06。行 ≈ 4.1 core·h | `certification_v2_pilot_2026-08-19.md` §6.1 |
| Zn M3 2×2 | 16 vs 32 パネル差 3.2e-07 は margin・ppw・cap に不変 ⇒ 窓求積の不足 | 同 §6.2 |
| パネル数 16/24/32/48 | 24 で Ca M1 2 窓 + Zn M3 が β=200 まで床 (≤ 4e-09) | 同 §6.3 |
| 含有半径 u_b² vs G²+F² | 5 桁一致 (Au L3/M5, Zn M1, Xe M4, Rn M5, Fe K) | 本書 §2.2 |

---

## 5. ★ 次にやること (着手順)

1. (pilot v3 完了後) `certification_v2_pilot` §6.4 に v2/v3 の確定値、事前登録 v2/v3 の §7 に完走記録、
   `error_budget_2026-08-19.md` の lkin 行を確定値に、`lkin_truncation` §6.5 に要因計画の表 (18 行)
2. **σ(β,Δ) v4 の事前登録** (`:src` + 24 パネル + オラクル 32) → pilot → (作者判断) deep
3. **F v6**: 範囲の凍結 (§3.1) → `V6_SPEC` → 起動前の確認 5 点 → 再生成 (1〜1.5 日) → QC (C1–C16) → MANIFEST v6 → ReciPro 配備
   (`.bin` + `Crystallography.dll` 同時、`EdxCheck all` + `AlchemiCheck all`) → release / DOI 取り直し
4. 用語の一括置換 (§3.4)
5. 残りの小物 (08-22 §6 (1)): Levinson 定理の検査 (中性静電場では成り立つが全束縛状態の数え上げが要る。Mott σ_el vs NIST が
   符号規約を既に拘束 → 優先度低) / 尾根帯 δF(s) の s 分解 / speedup_audit 残り 6 件 / ReciPro `TruncationBound` の 2 領域化
   (ReciPro 本体の working tree に作者の未 commit 変更があり触らなかった) / SIGMAK/SIGMAL 比較 (Egerton の式を写す必要 →
   転記ミスの危険。今夜は見送り) / イオンの f_x/f_e (作者の質問 02:1x に回答済: 難所は契約…もとい**仕様** — 電子配置の
   明記、陰イオンの Watson 球、f_e の q/s² 発散の正則部分収録。次々世代)

---

## 6. 道具 (今夜足した・変えたもの)

| ファイル | 中身 |
|---|---|
| `src/l0_numerics.jl`, `l5_channel.jl`, `l5_exit_edx.jl`, `gen_production.jl` | §2.2 |
| `tools/lkin_sweep.jl` ★ | 全 525ch の掃引 (JSONL、lane、再開、`--summary`、`--sgrid full`、`--policies`/`--conv-policies`、来歴) |
| `tools/lkin_rule_study.jl` ★ | 部分波規則の要因計画 (半径 5 種 × margin × cap、2 段の参照、規則ごとの try/catch) |
| `tools/lkin_truncation_probe.jl` | `:kappa_r` (r_eff 指定)、`:src_v5`、運動学的上限の外の K は NaN、`s_nodes` |
| `tools/sigma_beta_delta.jl` | `:kappa_rc` + `l_max_margin`、`SIGMA_RULE_V2` / `V3`、`node_rl` (P・O・角度が共有)、`rule_config`、`rule_version`、`src_lmax` が src に追従 |
| `tools/certify_sigma_v2.jl` | `--rule v1|v2|v3`、規則ごとの指紋 (オラクルのパネル数込み)、(指紋, 行) の済み判定、指紋混在の集計拒否 (`--fp`/`--allow-mixed`) |
| `tools/run_cert_v2_fleet.sh` / `cert_v2_watchdog.sh` | `CERT_RULE` / 第 7 引数 |
| `tools/cert_v2_report.py` | cap 張り付きの層、規則の版 |
| `tools/check_tables.jl` | C16 が settings から版を引き直す |
| `docs/notes/certification_v2b_preregistration_2026-08-19.md` ★ / `certification_v3_preregistration_2026-08-20.md` ★ | 事前登録 (走行前に commit、§7 に起動記録) |

⚠ 中間出力はリポの外 (`../qcamp/`、`../cert_v2b_pilot_lane*.jsonl`、`../cert_v3_pilot_lane*.jsonl`)。scratchpad の台本
(`znm3_diag.jl`, `cam1_panels.jl`, `smoke_v2.jl`) はセッション限り — 結果はログ (`../qcamp/*.log`) と note に写した。
検証ゲートの worktree `…/scratchpad/wt_before` (035471a) は消してよい (`git worktree remove`)。

## 7. ⚠ 今夜の撤回・罠

| 何 | 教訓 |
|---|---|
| 「Ca M1 は M 殻の最悪」→ Ca M1 は**出荷格子の外** (M1 は Z=30–86)。格子内の最悪は M1 Z=30–41 | 標本が格子内かを先に確かめる |
| LK_S 10 点の「s ≤ 0.5 の最大」は峰 (s ≈ 0.15–0.25) を外し 3〜13 % 小さい | [[requested-grid-may-not-resolve]] — 出荷格子で測る |
| s=16 が E₀ ≤ 35 keV で運動学的上限の外 → 279 行が例外 | 出荷の s_cert の規則を写しにも入れる |
| margin +48 は低 ε で Coulomb 関数が DomainError | 「参照を上げる」にも上限がある。cap だけ上げた refB に変更 |
| 「機構 (a)(b) は消えた」→「P−O 上の痕跡が消えた」 / 「収束」→「aggressive reference と閾値内で一致」 / 「バイアス」→「処方差」 | codex の語法レビュー。[[self-convergence-underestimates]] |
| Bash heredoc に Python 三重引用符 (2 回) と `\n` (1 回) | 長い置換は Write でファイルへ (memory 更新済) |
| ⚠⚠ **`atom_cache/` (SCF キャッシュ) を消してしまった** (05:32): 検証ゲート用の一時 worktree に `mklink /J` で atom_cache を共有し、`git worktree remove --force` したらジャンクションの先まで削除された。再生成可能 (v6 は src 指紋が変わり全部作り直し) で走行中プロセスは無傷だが、**旧 checkout で v5 を速く再現する能力は失った** (SCF の散発的な別反復のため再計算は 1e-8 級で違いうる)。`refs/atom_cache` (25 本) は無事 | ジャンクションを一時 worktree に作らない / 消す前に `rmdir` でリンクだけ外す (memory `junction-inside-worktree-deletes-target`) |

## 8. 参考: 本セッションの commit (古い順)

```text
334c35b  σ(β,Δ) candidate v2 (HIGH + κ·r_core), preregistration v2, lkin_sweep
184a65c  Apply the codex review before the v2 pilot
b04ccfe  lkin probe: kinematic limit
…        notes / report / handover (多数)
035471a  σ(β,Δ) candidate v3 (24 panels)
381e777  src: partial-wave cutoff rule v6 (LKIN_RULE), HIGH l_cap 256
056aa89  Apply codex round-3 review (two-component containment, fail-closed rows, audit, 6.0.0-dev, legacy switch)
(以降は git log)
```
