# 引き継ぎ — 原子散乱因子 (f_x / f_e) データセット (2026-08-14 朝に更新)

> ## ★★★ 最新 (2026-08-14 昼) — B_repr / B_repr,e を実測した。⚠ 設計問題 2 件を発見
>
> **正本 = `docs/repr_measurement_2026-08-14.md`** (実装 = `tools/certify_repr.jl`、
> v1 副本 84 元素の後処理。Tm/Yb 欠け)。
>
> - **f_e (経路 D、t=s² 上 NAK): 検査可能域で 84/84 合格** (最悪 Zn 0.331×B_repr,e)。
>   低 s は MB 検証値の丸め床 2a₀ε̂/K² に沈むので検査不能として別掲
>   (s_ok 最大 0.0093、中点の 0.078 %)。真の補間誤差は h_t⁴ 論証で床の下
> - ⚠ **f_x (s 上 NAK、現行 §4.9): Cs 1.222× / Ba 1.185× で超過 (82/84)**。
>   全元素の最大誤差が端区間 (s=0.0004) に集中する not-a-knot の設計問題。
>   **左端 clamped (f′(0)=0、偶関数性の厳密な帰結) で全元素 9.8–9.9 倍改善**
>   → 最悪 0.123×、11 桁丸め込み 0.163× — 規約変更は作者判断
> - ⚠⚠ **f_e 節点値を MB で構成すると床が予算を食う** — 最小非零節点で
>   Au 1.28e-07 Å = **1.41×B_num,e** (超過 6 元素)。
>   **δ 形求積 (∫ρ(1−j₀)r²dr を直接) で構造的に消える** —
>   dt/16 実スケールで実証済み
>   (MB 床 1.5e-08 @Z=6 を実測、δ 形は展開と 2.3e-10 で整合・格子不変)
> - JSON 十進丸めは**有効数字 11 桁**で丸め寄与が補間の下に消える
> - ⇒ **T_comp,e = 1e-7 Å (案 A、作者決定 8/14) の凍結は保留** —
>   f_e 構成法 (δ 形求積) と f_x 端条件の作者判断とセット (計画書 §4.23.8)
>
> ## ★★ L¹ 認証が全 Z 完走し、v2.1 で最終状態になった (2026-08-14)
>
> **正本 = `docs/grid_certification_l1_run_2026-08-14.md`**。
> **最終: certified 83 / uncertifiable_low_signal 3 (H・He・Li)**、
> model_violated 0 / 欠落なし。**bound の最悪は Er 68 の 0.583×B_grid で予算超過はゼロ**。
> H の分類順序バグは **事前登録 v2.1** (`cddd5d0` 凍結、負のテスト [D] つき) で
> 修正し、H/He/Li だけ再走した — **L¹ 値は旧走とビット同一** (決定論的ラダーの
> 設計どおり)、変わったのは status ラベルだけ。
> ラダー試行 2 が v1 で認証不能だった Tm・Yb を救済し、q₃₄ 診断は信号のある
> 75 元素すべてで尾の仮定 (q* = 0.5) を支持した。
> 規則 = v2 (`ebe585c`) + v2.1 (`cddd5d0`)。出力 = `c:/tmp/temari_certify_l1_2026-08-13/`。
> 残る数値認証は端点切断・標本 14 元素の追加診断 (2026-08-14 にフリート発火済み、
> 下の §2 表)。f_e の切替点は修正済み (`99fe985`)。

> ## 冒頭に読むこと (2026-08-12 朝時点の記録)
>
> **⭐ 全 Z 認証を実行した。正本 = `docs/grid_certification_run_2026-08-12.md`**
>
> 採用格子 `dt/16 = 6.25e-05` (n_r = 323,400) の空間離散化誤差は、
> **83 元素すべてで予算の 22 % 以下**だった (最悪 Z=71 Lu で 0.217 × B_grid)。
> 停止誤差も 0.036–0.252 × B_scf。
>
> ⚠⚠ **ただし事前登録した合否判定は 46/83 しか通っていない**。
> 落ちた 37 元素も上限・U は余裕で、原因は**点ごとの収縮検定 (q ゲート) の設計欠陥**
> である。**床を |δ_2| に掛けたが、雑音が乗るのも上限を決めるのも |δ_3|** だった。
> codex の裁定により、**規則を修正すれば見た元素についての事前登録は死ぬ**。
> 一次結果は「格子が不適格」ではなく「**当初の手順では認証に失敗した**」である。
>
> ★ 代替設計として**密度の L¹ 界**を実測した (3 元素で収縮比 0.248–0.252)。
> **s に一様で、零点も相殺も雑音増幅も無い**。実装 = `tools/certify_l1.jl`。
> ⚠ ρ を保存していないので**再実行が要る**。
>
> - 事前登録 = `docs/grid_certification_preregistration_2026-08-11.md` (`04eae96` で凍結)
> - 出力 = **`c:/tmp/temari_certify_2026-08-11/`** (リポ外。JSON + f_x のバイナリ副本)
> - 集計 = `julia tools/certify_grid.jl --aggregate c:/tmp/temari_certify_2026-08-11`
> - f_e = `julia tools/certify_fe.jl c:/tmp/temari_certify_2026-08-11` (**SCF 不要**)
> - 再開 = 同ディレクトリの `run_certify_fleet.sh` を再実行 (既に JSON がある元素は skip)
>
> **⚠ 未完 = Er 68 / Tm 69 / Yb 70**。Tm・Yb は tight (τ/10) に届かず判定 ❌ が確定。
>
> **★ 8/13 01:00 にタスクスケジューラのジョブ `Temari_dt32_allZ` が起動する**。
> 起動スクリプトを差し替えれば **L¹ の 4 水準 (約 14.5 h)** に切替できる。
> 提案 = `c:/tmp/temari_certify_2026-08-11/PROPOSAL_L1_RUN.md`。
> ⚠⚠ **どちらを回すにせよ、規則を凍結してから走らせること**。
>
> ⚠⚠ **フリート実行中はリポジトリを触らない** — `certify_grid.jl` の `tool_sha()` は
> JSON を書く瞬間にファイルを読むので、途中で変えると
> **実際に走ったコードと違うハッシュ**を記録する。
> 編集は「まだ 1 元素も完了していない間」か「完走後」に限る。
>
> 残っている作業は §2 と実行報告 §7。踏んだ罠は §3。

**このトラックの正本は `docs/scattering_factor_dataset_plan_2026-08-10.md`。**
本書はそこからの「現在地」と「次に何をどの順でやるか」だけを抜いたもので、
根拠と実測値は正本の §4.x を見ること。

⚠ F(s, E₀) データセット側の引き継ぎ (`next_phase_*.md`) とは**別トラック**である。
混ぜないこと。⚠ 本日 (2026-08-11) 追加された **OA 独立検証トラック**は §6。

---

## 1. 現在地

### 決まったこと (再議しない)

| 項目 | 決定 | 出典 |
|---|---|---|
| 処方 | **Dirac SCF + KLI** | §1 |
| 計算誤差契約 | **T_comp = 1e-7 電子** (B_num 9.09e-08 / B_repr 9.09e-09) | §4.17 |
| ⚠ 格子への配分 | **B_grid = 6e-08** (B_num の全部ではない) | §4.19・§4.21.3 |
| 補間規約 | **not-a-knot 3 次スプライン** | §4.9 |
| f_e の出し方 | **表に直接収録し t = s² 上で補間** (経路 D) | §4.11・§4.13 |
| 数値 backend | **`dirac_true_midpoint_v1`**。既定は `legacy_v5` のまま | §4.20 |
| **★ 採用格子** | ★ **作者決定: 真の中点 + dt/16 = 6.25e-05** (n_r = 323,400) | **§4.22** |
| **★ SCF 閾値** | **既定 (1e-8 / 1e-9) のままでよい** | **§4.21.5** |
| 収録範囲 | **v1 は中性原子のみ**。イオンは後 | §5 |

⚠⚠ **「補間した f_x から f_e を導く」を標準契約にしない** — 経路 A はどの格子でも
1〜3 桁不合格 (f_x は K の偶関数で f_x′(0)=0 が厳密なのに、not-a-knot がそれを
保証せず f_e が 1/s で偽発散する)。

### ★ 2026-08-11 夕方に完了したこと

| | 内容 | コミット |
|---|---|---|
| 1 | 出荷処方に `numerics=:legacy_v5` を明示。**回帰 (SCF 再試行が届かない) を発見・修正** | `d856f3b` |
| 2 | `factor_convergence.jl --grid / --invariance`。⚠ 自分の 2 つの誤りも修正 | `c0edb0e` |
| 3 | `compute_fx` に backend を配線。⚠ 格子ゲートを B_num → **B_grid** へ | `63cd6a8` |
| 4 | 残差を**その原子の写像で**測るよう修正 + `scf_tolerance.jl` 新設 | `d3f5c24` |
| 5 | **C16 = model_id ↔ 処方 ↔ dataset_version の機械照合** + `prescription_id` | `5feb06b` |
| 6 | `refs/README.md` を新しい文献ライブラリへ向け直す | `789afeb` |

**検証**: ビット同一 v3 `89fc1f67…` / v4 `5c884abf…` **差分ゼロ** /
selftest ALL PASS (190 s) / refcheck **9.044e-08 = 基準値** /
`cache_wiring_test` 14/14 / `c16_negative_test` 8/8 /
`check_tables src/prod_v5_jl` **525/525 OK・C16 通過**。

### 効いている数字

| 量 | 値 |
|---|---|
| **採用格子 dt/16 の格子誤差** (Z=6 パイロット) | **6.91e-10 電子 (B_grid の 0.012)** |
| 同、収縮比 q | 中央 **0.246** (理想 0.25) — p=2 の独立確認 |
| 同、封印した中点 7680 点 | 6.91e-10 (**節点間に隠れた山は無い**) |
| 同、停止影響 U | 1.28e-09 (B_scf の 0.14) |
| dt/8 の格子誤差 (4 元素の最悪 = Au) | 1.64e-08 (B_grid の 0.27) |
| dt/4 の格子誤差 (Au) | 6.51e-08 (1.09) ⚠ **方式依存の境界値**。§4.22.4 |
| legacy at dt (C) | 7.38e-07 — §4.7 の 6.97e-07 と 6 % 一致 |
| SCF 閾値 (既定、4 元素の最悪 = Ne) | 1.46e-09 (ゲートの 0.16) |
| モデル差 (KLI vs OFFV1/DHF) | C 7.469e-03 / He 2.810e-05 電子 |
| 補間誤差 (均一 Δs=0.05, 121 点) | 3.28e-03 電子 → **7681 点が必要** |
| ★ **規格化前の f_x(0) 誤差 @ dt/16** | **6.51e-10** = 1.67e-07/256 (N 段が 2 次であることの別経路の確認) |
| ★ **δf_x の丸め床** | **≈ 1e-13 電子** (低 s の f_e はこれに支配される) |
| ★ Σ n_orb (Z=1…86、κ 分解) | **1231** (Au は 22)。コストはこれにほぼ比例 |

⚠ **数値誤差 (~1e-09) とモデル差 (~2.6e-03) を混ぜて書かない** — 両者は 6 桁違い、
**別の層**である (§3・§4.14)。

---

## 2. 次の作業 (この順で)

### (1) ~~採用格子の作者判断~~ — **完了。dt/16 = 6.25e-05 に決定** (2026-08-11)

作者は推奨の dt/8 ではなく**1 段細かい dt/16** を選んだ。⚠ 私が引き継ぎ書に
書いていた dt/16 の外挿値は**当時 4 倍過大**だった (D = 3E₈ ではなく **3E₈/4**)。
正本は計画書 §4.22。

### (2) 全 production Z で採用格子を認証 — **実行した**

**正本 = `docs/grid_certification_run_2026-08-12.md`**。以下は骨だけ。

| | |
|---|---|
| 結果 | **83 元素で格子誤差は B_grid の 0.217 以下** (最悪 Z=71 Lu)。停止 U も 0.252 以下 |
| ⚠ 合否 | **46 / 83 しか通っていない**。原因は**私が置いた q ゲートの設計欠陥** |
| ⚠ 未完 | Er 68 / Tm 69 / Yb 70。Tm・Yb は tight に届かず ❌ 確定 |
| 代替設計 | **密度の L¹ 界**。3 元素で収縮比 0.248–0.252、界は sup の 2.5–2.8 倍 |

**次にやること** (順序):

1. **q ゲートの修正規則を凍結し、前向きに走らせる** (⚠ 見た 83 元素は開発データ)
2. **L¹ 界を重元素で実測** (⚠ 3 元素だけ。単調性を仮定しない)
3. **f_e の切替点を直す** (⚠ 水素で |比−1| = 13.7)。直すまで T_comp,e を置かない
4. 標本 14 元素の `--prod-all` と τ/100 / 端点切断 (`tools/certify_endpoints.jl`)
5. **`pointwise_bound` の複製を統合** (`certify_fe.jl` が `certify_grid.jl` の複製を持つ)
6. **集計に欠落の突き合わせを足す** (⚠ 現状は Z が欠けても沈黙する。
   `certify_l1.jl` には入れてある。**負のテストで実演してから**)

### (2′) 設計の要点 (実行前に書いたもの。残す)

事前登録 = `docs/grid_certification_preregistration_2026-08-11.md` (凍結済)。

⚠⚠ **私が提案した「生の逐次差 D を上界にする」案は却下された** (codex)。
$E = D/(2^p-1) \le D$ が言えるのは**誤差が同符号の単一冪に従うとき**だけ。
負のテスト D1 が、**符号反転する列を旧案が D = 0.05×B_grid で通す**ことを実演した。

⚠⚠ **production の閾値では格子を測れない** — C の格子差 6.9e-10 に対し
停止の動きが 1.3e-09。しかも**逆符号で相殺**すれば見かけの q まで正常になる。
⇒ 各水準を **production と τ/10 の 2 回**解き、格子は tight 系列で、停止は
U = |f_prod − f_tight| を B_scf で判定する。

完走後にやること:

1. `julia tools/certify_grid.jl --aggregate c:/tmp/temari_certify_2026-08-11`
2. `julia tools/certify_fe.jl c:/tmp/temari_certify_2026-08-11` (f_e。SCF 不要)
3. **標本 14 元素**を `--dt32 --prod-all` で (事前登録 §4.1。別ディレクトリへ)
4. **端点切断** `tools/certify_endpoints.jl` (事前登録 §8)
5. **`pointwise_bound` の複製を統合する** — `certify_fe.jl` が持っている
   `pointwise_bound_diffs` は `certify_grid.jl` の複製である
   (フリート実行中は向こうを触れないので分けた)。**判定を作る場所を 2 つに
   増やしたままにしない**

### (3) H / N の要因実験 — 8 条件

⚠ **予算に必要と分かるまで出荷経路へ入れない** (codex 手順 6)。
**dt/16 で予算を大きく満たしたので不要**である。
⚠⚠ **最終 f_x 求積 Q を第 4 因子として管理する**。
H の高次化設計は §4.19 に確定済 (⚠ Gregory 補正は非推奨)。

### (4) f_e の `T_comp` を決める — **設計は完了、値は測定後** (計画書 §4.23)

⚠⚠ **f_x の予算から導けない**。素朴な伝播 0.0239|δf_x|/s² は s→0 で発散するが、
規格化により δf_x = −K²δM₂/6 なので δf_e(0) = a₀δM₂/3 で有限。
⚠ ただし「規格化したから自動的に O(K²)」ではない — **曲線全体に同じ係数を
掛けている**からである。

⚠⚠ **アンカーは用途にする。モデル差は sanity check に留める** (codex)。
OFFV1 の**有限桁の f_x 表**を低 s で Mott–Bethe 変換すると、見ているのは
モデル差ではなく**補間・丸め差**になる。
⚠⚠ **$K^2f_e$ を契約対象にしない** — K→0 で重みが消え、f_e(0) の誤差を無制限に許す。

★ **実測で分かったこと**: 低 s の δf_e は Mott–Bethe では測れない
(δf_x の丸め床 ε ≈ 1e-13 が 1/K² 倍に増幅される)。**モーメント 2 項展開と
切り替える** — 切替は解析規則 $K_{sw}^2 = 2|\delta M_2/\delta M_4|$ で決める
(雑音の実測から決めると結果を見て閾値を動かすことになる)。詳細は計画書 §4.23.6。

### (5) X1 / X13 / X14 と公開

schema・golden vector・manifest・決定論的梱包。⚠ **F dataset には混ぜない**
(別 release・別 Zenodo record)。

### (6) OA 2000/2001 による独立検証 — **新トラック。§6 を読むこと**

⚠ **未着手**。申し送りと変換仕様は揃っている (§6 と `c:\tmp\TaskA_Oxley2000_Temari_conversion.md`)
が、**F(s, E₀) 側の計算が要る**ので認証フリートと同時には走らせられない。
⚠ **OA の数表をリポへコピーしない** (ライセンス。比較は scratchpad で)。

---

## 3. ⚠ 踏んだ罠 (同じ轍を踏まないこと)

すべて**実際にこのトラックで起きた**もの。★ が 2026-08-11 夕方、★★ が同日夜、
★★★ が 08-12 朝までの全 Z 認証で追加したもの。

| 罠 | 何が起きたか |
|---|---|
| ★★★ **自作の watchdog が健全な 20 プロセスを kill した** | 「計算中のプロセスは必ず CPU を食う」を前提に組んだ判定を、**自分で優先度を BelowNormal へ下げて無効化した**。約 51 CPU 時間を失った。⇒ **監視の前提を壊す操作をしたら、監視を見直す**。直した版は**相対判定** (他プロセスの中央値が明確に正のときだけ疑う) で、7 時間 誤検出ゼロ + 真の wedge を 1 件検出した |
| ★★★ **1 回の失敗をその元素の性質と読んだ** | GC クラッシュ 3 件・KLI の NaN 3 件を「決定論的な欠陥」と 6 時間書き続け、電子配置での分類を 3 回試みて 3 回とも外した。⚠ **再試行したら全部成功した**。そもそも分類すべき対象ではなかった。⇒ **失敗した元素は必ず再試行してから性質を語る** |
| ★★★ **単一の説明で片付けようとして 4 回外した** | ①NaN はランタノイド集中 (W で崩れた) ②NaN は 5d¹ が共通 (Ce が 5d¹ で成功) ③未収束は格子起因 (Yb で成立、**Tm で崩れた**) ④違反は δ₂ の零点近傍 (**半分の元素でしか当たらない**)。⇒ **1 元素・一部の観察から機構を名指ししない** |
| ★★★ **途中集計を全体の性質として語った** | 20 元素で「q > 1 の点は 1 つも無い」と書いたが、**全 83 元素では 187 点ある** (Z=85 At が 172)。⇒ **走行中の集計には「n = いくつでの話か」を必ず添える** |
| ★★★ **一括の強調修正スクリプトで文書を壊した (2 回目)** | 本表に「一括の強調修正スクリプトが文書を壊した ⇒ 手で直す」と**自分で書いてあるのに同じことをした**。正規表現が `**A** … 、**B**` の**A の閉じと B の開きを対応させる**ため。強調検査の総数が 110 → 143 に増えて発覚し、`git checkout` で revert した。⇒ **書いてある注意も、実行の瞬間に思い出さなければ意味がない** |
| ★★★ **観測ゲートを尾の仮定より厳しくし、隙間を不合格にした** | 観測 0.35 / 尾 0.5 と分けたのは正しいが、**その隙間に落ちた点を「不合格条件」にした**。違反 27,188 点の **75.7 % がこの隙間**で、上限が依拠する仮定は満たしている。⚠ **上界が破れたのではない** |
| ★★★ **床を掛ける差を間違えた** | 低信号の床を \|δ₂\| (粗い差) に掛けたが、比で雑音が乗るのも上限を決めるのも \|δ₃\| (細かい差)。p=2 なら \|δ₃\| ≈ \|δ₂\|/4 なので、**δ₂ が床のすぐ上の点は δ₃ が雑音の中**になる |
| ★★★ **「止めるか続けるか」の二択に閉じこもった** | 1 元素が長時間占有すると読んで「殺すか待つか」を諮ったが、⚠ **14 レーンが空いているのだから並行して投入すればよかった**。⇒ **資源の状態を見てから選択肢を数える** |
| ★★★ **記録してあった注意を自分で外挿した** | 「τ/100 は元素によって届かない」と自分で書いておきながら、**「τ/10 なら全元素で収束する」と仮定して tight を設計**した。Tm・Yb で破れた |
| ★★ **外挿値を 4 倍間違えた** | `D = 3E(dt/16) = 3E₈/4` と式には書きながら、表には `3E₈` を入れた。Au の予算比が 0.80 と出て「余裕が薄い」と読み、**設計判断がそこに引きずられた**。式が正しくても数値が別に間違う |
| ★★ **`-uno` が未追跡を見ない** | `git status --porcelain -uno` を `gen_production.jl` から真似たら、**ツール自身が未追跡なのに `dirty=false`** と記録された。追跡ファイルだけ見る規約は「走ったコードがコミットに入っているか」を保証しない |
| ★★ **warm start が効かなかった** | 収束済み ρ を種にしても時間が 1 割も減らない。`vx_kli` が引き継がれず初回反復が Xα のブートストラップに落ちるため。⇒ **効くはずと思ったら測る** |
| ★★ **フリートの並列度でコストが 2.5 倍変わる** | 単独実行の n_r 依存は 2.01 = 厳密に線形。16 レーンではメモリ帯域が律速して 2.5 倍遅い。⇒ **コストの数字は「何レーンで測ったか」とセットでしか意味を持たない** |
| ★★ **低 s の測定が丸め床を測っていた** | δf_e = −2a₀δf_x/K² は **δf_x の丸め床 1e-13 を 1/K² 倍に増幅**する。中点の最大が節点の 8 倍に見えたのはそれ。**信号が K² で消えるのに床は消えない** |
| ★★ **合成テストの指数を 1 つ間違えた** | q = 0.325 の列を「q ≈ 0.98」と思い込み、テストが落ちない理由を**判定器の側に探した**。⇒ **テストの入力は期待値が暗算できる形で直接書く** |
| ★★ **docstring の `\$…\$` で落ちた** | Julia の文字列補間になる。§4.20 に**記録してあった罠を自分で踏んだ**。記録しただけでは避けられない |
| ★★ **走行中にツールを触ると来歴が嘘になる** | `tool_sha()` は JSON を書く瞬間にファイルを読む。途中で編集すると**実際に走ったコードと違うハッシュ**を記録する |
| ★ **Richardson の帰属を 1 段間違えた** | 同じ差 D から `E(h)=D/(1−2⁻ᵖ)` と `E(h/2)=D/(2ᵖ−1)` の 2 つが出る (p=2 で **4 倍違う**)。細い方を粗い行に書き、**採用格子を 1 段甘く見せた**。数字は正しく帰属だけ誤りなので検算では気づけない |
| ★ **予算を B_num に当てた** | 格子への配分は 6e-08 で、9.09e-08 で判定すると **1.5 倍甘い**。Ne の legacy dt/4 が 1.00 → 1.52 に変わる |
| ★ **鍵を作る場所を増やした** | `cache_tag` を取得側にだけ足し、`ensure_converged` を取り残した。**SCF 再試行が誰も読まない鍵へ書き、未収束の原子が使われた**。error は出ない |
| ★ **残差を別の写像で測った** | `fixed_point_residual` が backend 無しで解き直し、真の中点の原子に legacy を当てていた (処方差 7e-07 が真の残差 7e-09 を 2 桁覆う) |
| ★ **最厳水準の未収束で走を全損させた** | 反復上限に当たるのはいつも最も締めた所なので、`error` にすると**収束した水準の結果まで失われる** |
| ★ **`@printf` の書式文字列を `*` で連結した** | マクロ展開時に落ちる。**2 回踏んだ** |
| ★ **一括置換が黙って空振りした** | grep が別の編集のヒットで隠した。⇒ **置換は 1 件ずつ、失敗が例外になる道具で** |
| **背景タスクの生成物を完了通知の前に読んだ** | 書き込み途中 (4788 B) をハッシュした。完全なファイルは 6720 B |
| **`Meta.parseall` は構文エラーを投げない** | ⇒ **構文確認は `include` で** |
| **一括の強調修正スクリプトが文書を壊した** | ⇒ **手で直す**。判定は `tools/md_emphasis_check.py` |
| **点核 Coulomb は spline 経路を素通りする** | `r·V = −Z` が定数なので補間が厳密になる |
| **異方式間の差を 16 で割った** | 同一方式なら **/15**。異方式間には適用できない |
| **s=0 での規格化検査は無意味** | 同じ離散求積で規格化と f_a(0) を計算すれば構成上成立する |
| **表示書式が測定を壊した** | 格子名を `%.2f` で作り、Δs=0.0125 と 0.00625 が衝突 |
| **引数解析が値を Z として拾った** | `--stages 4` の `4` を Z=4 と誤読 |
| **積分区間を誤って飽和させた** | 古典的禁止域まで延ばし、次数が −0.58 と出た |
| **中央値だけを見た** | ⇒ **符号付き比と反転数を必ず出す** |

### ゲートにしてはいけないもの (§4.1)

f_x の単調減少 / 大 K の漸近指数 / 強制済みの f_x(0)=Z /
SCF の規格化残差と電子数 (求積規則の差が支配する)。

---

## 4. 検証の規律

- ⚠ **`l1_atomic.jl` に触る前に `bitident_snapshot.jl` を取る** (v3 無印と `--v4` の
  **両方**)。後からは作れない
- 変更後に差分ゼロを確認 → selftest → refcheck (基準値 9.044e-08)
- **キャッシュキーは resolved config からのみ作る。**⚠ さらに強く —
  **鍵を組み立てる場所を 1 つに減らす**。足し忘れは必ず起きる (★ 上表)
- 新しいゲートは**負のテストで落ちることを実演**してから「効いている」と言う。
  実例 = `check_factors.jl --negative` / `tools/cache_wiring_test.jl` /
  **`tools/c16_negative_test.jl`** (旧検査が素通りすることも同時に実演する)
- ⚠ 誤差の表は**行ごとに使った式を印字する**。同じ差から複数の推定が出る

---

## 5. 相談の記録

codex との議論は `c:\tmp` に残っている (リポジトリ外):

- `codex_question_absolute_floor.md` / `codex_answer_absolute_floor.md`
- `codex_question_absolute_floor_round2.md` / `..._answer_..._round2.md`
- **`codex_question_numerics_wiring.md`** (2026-08-11。配線範囲と測定設計。
  回答は本書 §2 と正本 §4.21 に反映済)

⚠ スレッドは失効するので、続けるときは**文脈を自己完結させて新規スレッド**で投げる。
⚠ `mcp__codex__codex` の初回呼び出しで `config` に
`{"model_reasoning_effort": "medium"}` を渡すこと (`codex-reply` に `config` は無い)。

---

## 6. ★ 新トラック — Oxley & Allen による独立検証 (2026-08-11)

申し送りの原本 = `c:\tmp\Temari_申し送り_OxleyAllen検証_2026-08-11.md` (リポ外)。
論文 PDF は **`C:\Users\seto\source\repos\references\`** (全リポ共通の文献ライブラリ。
2026-08-11 に `Temari/refs` から PDF を移動した。⚠ **データは移動していない**)。

⚠ **これは f_x/f_e ではなく F(s, E₀) 側の検証トラック**である。混ぜないこと。

- **OA 2000 (Acta Cryst A56, 470)** は K 殻 Z=6–50 / L 殻 Z=20–60、50–400 keV、
  s ≤ 2.5 Å⁻¹ を表で持ち、**原子物理が別系統**なので cross-model validation になる。
  比較する量は `η(s) = f_OA(s)/k = σ·F(s)`。⚠ **形状 / 全断面積 / 絶対係数の 3 本に
  分けて測る** (混ぜると原因が特定できない)
- ⚠ 落とし穴は申し送り §1-2 に列挙されている (連続状態 Jacobian の二重適用 /
  DWF off / L 殻は**断面積レベルで非干渉加算**・縮退度を再度掛けない /
  閾値が OA は Cowan・我々は Bote)
- ⚠ **OA の高 s パラメタライズを正解値にしてはいけない** — OA 自身が
  「大きい s で form factor を過大評価する」と書いている
- **OA 2001 (A57, 713)** は `edge` 出口 (EELS) の検証に使える (収集半角 10–40 mrad、
  窓 25–100 eV)
- ⚠ **形状の差が出ても直ちに Temari のバグとは言えない**。独立モデル間の残差なので
  **Z・殻・E0・s のどこに集中するかの残差マップとして保存する**

### 6.1 provenance の監査 — **実施済・該当なし** (2026-08-11)

申し送り §3 の「v5 成果物に v3 由来の SRC 文言が残っていないか」を監査した。

- `src/prod_v5_jl/*.json`・`tables/`・`docs/src/`・`README.md`・`CITATION.cff` を走査
- **取り違えは無い。**JSON に出る "SRC" は `validated` の
  「**v4 が v3 の SRC を置き換えた**」という**正しい履歴記述**だけ。
  `prescription.continuum` は `kappa-resolved Dirac`、
  `model_id` は `…-KDIRAC2C-…-v4-DSCF`
- ⚠⚠ **Zenodo のレコード説明 (DOI 10.5281/zenodo.21872050) は未確認** — 外部なので
  手元から検証できない。**作者が見ること**

⇒ 再発防止として **C16 (機械照合) を入れた**。§1 の表 5 と正本 §4.21 を参照。
⚠ 出荷済み v5 は `prescription_id` を持たないので散文照合のみが効く。
**識別子を引き直す照合は次世代から**。
