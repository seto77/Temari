# v4 生成のための高速化 — 測定の正本 (2026-08-08)

*`docs/handover/next_phase_2026-08-08.md` §2.2 の「⚠ 10 倍が現実的でないなら、先に高速化を
検討する余地がある」を受けた作業の記録。作者判断「**先に高速化してから生成**」。*

**結果: 1 行あたり 24.7 s → 6.3 s (3.9 倍)。全変更がビット同一。**
**525 チャネル・14796 行の生成が 8 レーン × 4 スレッドで約 5.6 時間。**

---

## 0. 三行で

1. **見積りの前提が違っていた**。引き継ぎ書の「v3 比 10 倍」は κ 分解の
   コスト比を **s 17 点**で測った値 (4.8 倍) に依っていた。出荷の **161 点**では
   κ 分解の固定費が薄まり、実測の比は **2.2〜2.9 倍**だった (§1)
2. **プロファイルを取ったら、時間の 37 % が「ポテンシャルのスプライン評価」**
   だった。RK4 の下位刻みで `pot_V` を κ ごとに引き直していたのが原因で、
   κ 間で共有するだけで 1.7 倍になった (§2.1)
3. **v4 が v3 より遅い主因は物理ではなく、最適化の取りこぼしだった** —
   R 積分の q レーン SIMD (E5) が非相対論版にしか入っていなかった (§2.5)

---

## 1. 出発点の測り直し

HIGH 求積 + 出荷 S_GRID (161 点)、`-t 4`、1 E0 行あたりの実測 [s]:

| ケース | v3 (SRC) | v4 (kdirac) | 比 |
|---|---|---|---|
| Fe K 200 kV | 14.3 | 31.6 | 2.21 |
| Sn K 100 kV | 5.5 | 14.2 | 2.58 |
| Au L3 300 kV | 7.9 | 22.6 | 2.86 |
| Zn M1 100 kV | ~7.7 | 25.3 | ~3.3 |
| Au M5 200 kV | 11.1 | 29.8 | 2.68 |

チャネル数は 246 → 525 (2.13 倍) だが、**行数は 7466 → 14796 (1.98 倍)** —
M 殻は端が浅く、過電圧ノードの大半が 30 keV 未満に落ちて間引かれるため。

⇒ **v4 の総コストは v3 比 約 5 倍** (10 倍ではない)。

---

## 2. 入れた高速化 (実施順)

**全て「同じ値を 2 度計算しない / 並べ替えない」型の変更で、ビット同一。**
検証は毎回 `tools/bitident_snapshot.jl --v4` の 7 チャネル差分ゼロ。

### 2.1 Dirac RK4 のポテンシャル評価を κ 間で共有 — **1.72 倍**

`src/l2_continuum.jl` `DiracContinuumSet`。

プロファイル (Fe K 200 kV、`-t 1`) で **全時間の 37 % が `RvSpline`**
(= `log` + 節点二分探索) に落ちていた。原因は

```julia
for j in 1:ns
    pa = ra + h*(j-1);  pb = ra + h*j
    g, f = _dirac_rk4_c(pa, pb, pot_V(pa), pot_V((pa+pb)/2), pot_V(pb), …)
end
```

が **κ ごと** (l_max=42 なら 85 本) に回ること。区間を `n_sub` 分割したときの
評価点は κ に依らないのに、3·n_sub·nch·n 回引いていた。

⇒ 区間ごとに `(2·n_sub+1)` 個の表 `vsub` を**1 回だけ**作る。呼び出し回数は
**約 1/113**。`ns > n_sub` の区間 (障壁中で刻みを増やす) だけは従来どおり直接
引くが、そこも `pot_V(pb)` = 次の刻みの `pot_V(pa)` の重複を消して 3→2 にした。

⚠ ビット同一の根拠は「同じ Float64 引数で同じ関数を呼ぶ」ことだけ。
`(pa+pb)/2` を `ra + h*(j−0.5)` に書き換えると最下位ビットが変わる。

### 2.2 Q₊ 側の R(Q) を ε ノードあたり 1 回に — **1.21 倍**

`src/l4_angular.jl` `precompute_RaT`。監査書 (`docs/notes/speedup_audit_2026-08-05.json`)
の P1-5「Qp 側 Ra の ε ごと 1 回化」— **未検証だった項目**。

`angular_integral` の K≠0 経路で

    kp_d     = k_i·cosθ_i                 … i にしか依らない
    Q₊²[i,j] = k_i² + k_f² − 2 k_f·kp_d   … **j にも K にも依らない**

つまり Q₊ 側の PCHIP 評価は格子点 i ごとに 1 回で足りるのに、
**K ノード 161 × φ ノード 48 = 7728 回**引き直していた。

### 2.3 Legendre 漸化を 8 点インターリーブ — **1.15 倍**

`Pl[λ+1] = ((2λ−1)cPl[λ] − (λ−1)Pl[λ−1])/λ` は 1 点の中では直列依存で、
実測 **~16 サイクル/反復 = ちょうど `vdivsd` のレイテンシ**。格子点どうしは
独立なので、`P_BLK = 8` 点を同じループで回して除算をパイプラインに重ねる。
各点の演算列は不変。

### 2.4 ε ループを `:greedy` + 降順 (LPT) に — **1.25 倍**

`src/l5_channel.jl` `compute_NK`。監査書 P4-1、**未検証だった項目**。

既定の `Threads.@threads` (`:dynamic`) は ne=96 を nthreads 個の**連続チャンク**に
割る。ε とともに l_max が伸びる本問では最後のチャンクだけが重く、他スレッドが
遊んでいた。`:greedy` で 1 ノードずつ引かせ、**降順**にして重い方から配る。

⚠ ie ごとに互いに素なスライスへ書くだけなので、値はスケジューリングに依存しない。

### 2.5 ★ R 積分の q レーン SIMD (E5) を Dirac 版へ移植 — **1.21 倍**

`src/l3_radial.jl` `RlTable(cont::DiracContinuumSet, …)`。

**これが最大の取りこぼしだった**。260805Cl に E5 (q を 8 本まとめて SIMD
レーンに載せ、r 方向の逐次加算連鎖を 8 本の独立連鎖に畳む) を入れたとき、
Dirac 版は「比較・検証用で出荷しない」前提だったので素の逐次和のまま残り、
v4 で出荷経路になった後もそのままだった。docstring にも
「⚠ 求積は素直な逐次和 (非相対論版の SIMD/タイル最適化は入れていない)」と
書いてあった。

移植後のプロファイルでは RlTable の累算が消え、**球ベッセル評価が最大項**になる。

### 2.6 その他 (中立〜微小、いずれもビット同一)

| 変更 | 効果 | 備考 |
|---|---|---|
| RK4 の除算を 16 → 6 に畳む | ~1 % | 中点の商を k2/k3 で 4 回計算していた |
| `gw` を (i, κ′) 順に転置 | 中立 | タイルが L2 に収まるので効かない。連続性のため残す |
| P2-2 (gw の厳密ゼロ前置部を飛ばす) | 中立 | E5 後は累算が軽く、効果が出ない |
| `BLAS.set_num_threads(1)` | フリートで有効 | 8×16 = 128 本の遊休スレッドが消える。**値が動かないことは実測**|

---

## 3. 到達点

1 行あたり [s]、HIGH + 161 点、`-t 4`:

| ケース | 開始時 | 現在 | 倍率 |
|---|---|---|---|
| Fe K 200 kV | 31.6 | 7.9 | 4.0 |
| Sn K 100 kV | 14.2 | 5.0 | 2.8 |
| Au L3 300 kV | 22.6 | 6.0 | 3.8 |
| Zn M1 100 kV | 25.3 | 6.2 | 4.1 |
| Au M5 200 kV | 29.8 | 6.5 | 4.6 |
| **平均** | **24.7** | **6.3** | **3.9** |

v3 (SRC) 経路も同じ変更の恩恵を受ける (平均 9.2 → 5.1 s/行)。

**フリート実測**: 8 レーン × 4 スレッド (9950X、16C/32T) で **43.7 行/分**
⇒ 14796 行を **約 5.6 時間**。

---

## 4. 現在のプロファイル (Fe K 200 kV、HIGH、`-t 1`)

| 区分 | 割合 |
|---|---|
| `RlTable` (Dirac) | **46 %** — うち球ベッセル `_jl8_miller!` が 33 % |
| `angular_integral` | 29 % |
| `DiracContinuumSet` (RK4) | 24 % |

---

## 5. ここで止めた理由と、次の一手

**球ベッセル (33 %) はビット同一の床に達している**。Miller 下方漸化の
`c/X[j]*jc[j] − jp[j]` は除算がスループット律速で、`1/X` を先に作る形
(`c*(1/X)`) は**丸めが変わる**ので採れない。

残る大きな一手は監査書の **P3-1 (l_need による λ・r 事前打ち切り)** で、
これは**ビット同一を壊す**:

- Miller の開始次数 `M = lmax + 20 + ⌈√(40(lmax+1))⌉` は lmax=42 で 104。
  格納するのは λ ≤ 42 だけなので **62 ステップ (60 %) が捨てられる助走**
- 連続波は r^{l+1} が e^{−60} になる半径から種を蒔くので、**小さい r では
  高い λ を持つチャネルが 1 本も寄与しない** (実測: l=42 の種は格子の 92 % 右)。
  そこで lmax を落とせば M も落ち、平均で 2 倍前後になる見込み
- ただし M を変えると j_λ の値が ~1e-13 相対で動く。受け入れゲートを
  bitwise から **|ΔF/F| < 1e-10** に張り替える判断とセット (監査書 §1 グループ B)

⇒ **全体で 1.2 倍前後の見込み**。5.6 時間が 4.7 時間になる程度なので、
**ビット同一という検証手段を手放す価値はないと判断した**。将来 v5 で求積を
変える回があれば、そこで一緒に入れるのが筋。

### 測って否定した案 (再提案しないこと)

- **有意性フィルタで捨てる部分波を先読みして作らない** (監査書の × を実測で確認)。
  実測すると **l' の 95〜99 % が生き残る** (Fe K / Au L3 / Au M5 / C K で確認)。
  低 ε ノードでだけ 57〜86 % に落ちるが、そこは元々安い。**取り分が無い**
- **`gw` の転置**と **P2-2 のゼロ前置スキップ**は、E5 の後では中立
  (タイルが L2 に収まり、累算そのものが軽いため)

---

## 6. 副産物として分かったこと

### 6.1 ⚠ 軽元素 K の高 ε で `l_cap` が効いている

C (Z=6) K 殻の最高 ε ノードで **l_max = 128 = `l_cap` に張り付き、しかも
l′=128 が有意**だった (`sig_thresh` を通る)。つまり**部分波和が打ち切られている**。

監査書 P1-9 が指摘していた「監査ケース (26,K,200)/(79,L3,300) では l_max が
42/25 にしかならず、`l_cap 128→160` の監査行が何も測っていない」の実例。

- 実害の見積り: 打ち切りは最高 ε ノード側に集中し、ε 積分の重みは小さい
- **v4 の出荷を止める理由にはしない**が、**次の求積監査では軽元素 K × 高 E0 を
  ケースに足すこと**。`diag` に `l_cap` 張り付きフラグを出すのが本筋
- ★ **2026-08-19 追記: 実測した → §8**。張り付きは C/N/O K × E₀ ≥ 300 keV で本物
  (C K 400 keV は 96 ノード中 27 本)、**ノード単位では K=0 が 4 % 動く**が、ε 積分後の
  F(s) は**相対 ≤ 8.3e-05・絶対 ≤ 1.8e-07** (量子化の半歩 5e-07 未満)。
  「重みは小さい」の見積りは裏が取れた。道具 = `tools/lcap_saturation_probe.jl`

### 6.2 ⚠ v3 は現行コードでビット再現できない (相対 2.8e-08)

`--v3` で Z=6 K を HIGH 再生成し、出荷済み v3 と比べた:

    22 行すべてでビット一致せず、最悪の相対差 = 2.84e-08

原因は今回の高速化ではない (**5 チャネル `===` は差分ゼロ**)。v3 出荷 (2026-08-05)
後に原子層 (SCF・KLI 配線・`SCFAtom` のフィールド追加) が変わったため。

- **物理的な実害は無い** (求積の収束 ~1e-6、モデル精度 ~1e-3 の遥か下)
- ただし「v3 はビット再現できる」と書いてはいけない。**再現できるのは 3e-8 まで**
- v4 は生成時点の HEAD で固定されるので、この問題は世代ごとにリセットされる

---

## 7. 検証 (全て通過)

```
julia +1.11 -t 4 tools/bitident_snapshot.jl --v4 <file>   # 7 チャネル、差分ゼロ
julia +1.11 -t 4 tools/bitident_snapshot.jl      <file>   # v3 処方 5 チャネル、差分ゼロ
julia +1.11 -t 4 src/ionization.jl selftest               # ALL PASS (50 s)
julia +1.11 -t 4 src/ionization.jl refcheck               # WORST 9.044e-08 (基準値と一致)
julia +1.11 -t 1 tools/verify_simd_bessel.jl              # 288 ケース ALL BIT-IDENTICAL
julia +1.11 -t 1 tools/verify_e5_qlane.jl                 #  75 ケース ALL BIT-IDENTICAL
julia +1.11 -t 1 tools/verify_e5_qlane_dirac.jl           #  75 ケース ALL BIT-IDENTICAL ★新規
julia +1.11 -t 1 tools/verify_angular_pack.jl             # 61440 要素 ALL BIT-IDENTICAL ★新規
```

**★ 新規の 2 本は CI に入れた** (`.github/workflows/ci.yml` の bit-identity ジョブ)。
スナップショットは基準ファイルを要するので CI に置けないが、この 2 本は
**オラクル (移植前のコード・温存した `legendre_sum!`) を同じファイルに持つ**ので
単体で完結する。

- `verify_e5_qlane_dirac.jl` — Dirac 版 RlTable の E5 移植と P2-2。
  合成 `DiracContinuumSet` で n_q 端数・δ 域・Miller/上方混在境界・
  **κ ごとに違う種蒔き位置** (= ゼロ前置部の長さ) を踏む
- `verify_angular_pack.jl` — Q₊ の巻き上げ・チャネル詰め直し・Legendre
  インターリーブの 3 つを、温存した `legendre_sum!` に対して同時に検査。
  非相対論 / κ 分解 / 重元素 / d 殻 (l_init=2) の 4 経路

⚠ **v4 のビット同一基準は `--v4` の 7 チャネル**。従来の 5 チャネルは v3 処方
なので、v4 の出荷経路 (κ 分解 Dirac・M 殻の l_init=2) を 1 行も踏まない。
M1 (3s、節 2) と M5 (3d、l_init=2) を足してあるのはそのため。

---

## 8. l_cap 張り付きの実測 (2026-08-19)

§6.1 の宿題 — 軽元素 K × 高 E₀ で部分波上限 `l_cap` に張り付くか、張り付くと F(s) が
どれだけ動くか — を**出荷処方 (`prepare_channel(…; dirac_continuum=true)` = v4/v5)・
出荷求積 (`HIGH_SETTINGS`、l_cap = 128) で実測した**。
道具 = **`tools/lcap_saturation_probe.jl`** (読み取り専用、src 不変)。
`eps_setup` を `eps_worker` と同じ呼び方で叩き、K=0 は `tools/beta_spike.jl` の
`partial_angular` (β = π = 全角度、β = 30 mrad)、K>0 は `angular_integral` で
s = 0, 0.5, 1, 2, 4, 8, 12, 16 Å⁻¹ の 8 点。対象 = C/N/O/Al K × E₀ = 300/400 keV の 8 行
(ε ノード 96 本ずつ全部)。実行 = `julia +1.11 --project=. -t 4 tools/lcap_saturation_probe.jl`
(4 スレッドで 254 s。`--quick` で C K 400 keV の 1 行のみ、`--json` で生データ)。

- ⚠ **出荷の上限は `HIGH_SETTINGS.l_cap = 128`** (`gen_production.jl` の `run_channel` 既定)。
  `PROD_SETTINGS.l_cap = 96` は `audit` の `o_prod` 比較用で出荷経路ではない
  (式から求まる張り付き本数だけ併記した)
- ⚠ 張り付きの判定は **`eps_setup` の 5 番目の戻り値 (l_max そのもの)** で取った。
  `rl.lam_max` は多重極 λ であって l_max ではない (`window_quadrature_2026-08-19.md` §2.6 の罠)。
  式 `l_max = min(l_cap, max(6, min(l_kin, l_barrier)))` (`l5_channel.jl:85`) で再計算した値と
  **8 行 × 96 ノードすべて一致**
- 負の対照: 張り付いていないノード (l_max < l_cap) を l_cap = 256 で再計算すると
  **8 行とも bit 同一** ⇒ l_cap は l_max 経由でしか効かず、張り付きノードだけ再計算して
  差し替えれば ε 積分後の値が正確に出る

### 8.1 張り付きは本物 (C/N/O K)。Al K は張り付かない

| Z | E₀ [keV] | 張り付き / 96 | ε_onset [keV] | l_kin 最大 (最高 ε ノード) | 帯の重み Σ_sat wₑ·row / N(0) | 同 s = 16 |
|---|---|---|---|---|---|---|
| C 6 | 300 | 18 | 162.9 | 181 | 1.90e-04 | 1.45e-02 |
| C 6 | 400 | **27** | 159.3 | **215** | 2.38e-04 | 1.35e-02 |
| N 7 | 300 | 14 | 214.2 | 157 | 1.30e-04 | 9.31e-03 |
| N 7 | 400 | 19 | 207.1 | 186 | 1.91e-04 | 1.18e-02 |
| O 8 | 300 | 11 | 256.2 | 139 | 7.50e-05 | 4.62e-03 |
| O 8 | 400 | 15 | 266.0 | 164 | 1.39e-04 | 8.18e-03 |
| Al 13 | 300 / 400 | **0 / 0** | — | 90 / 106 | — | — |

- 張り付いた全ノードで **l′ = 128 が有意** (`sig_thresh` を通り、生存最高部分波 = 128)。
  §6.1 の観測どおり部分波和は打ち切られている
- 張り付きを決めるのは `l_kin = ⌈κ·min(r_core, 6/Z)⌉ + 12` (`l_barrier` は 600〜1000 で
  効かない)。**E₀ に依らず ε だけで決まる**ので、ε_onset (C で ≈ 160 keV) を超える E₀ の
  行はすべて最高 ε 側に張り付きノードを持つ (E₀ が高いほど本数が増える)。
  軽いほど 6/Z が大きく κ がそのまま効く。Al (6/13 = 0.46) は 400 keV でも l_kin = 106 止まり。
  **出荷 K 殻の最軽元素は C** なので、**C K 400 keV が出荷データの最悪ケース**
- PROD (l_cap = 96) なら 400 keV で C 33 / N 31 / O 28 / **Al 11** 本が張り付く。
  `audit` の PROD vs HIGH 比較は軽元素 K でこれを含んでいた
- 本プローブは K 殻のみ。L/M 殻は未測 (6/Z が小さく l_kin が伸びないので張り付かないはずだが、
  測っていない)

### 8.2 ノード単位では 4 % 動くが、ε 積分後の F(s) は相対 ≤ 8.3e-05・絶対 ≤ 1.8e-07

張り付いたノードだけ l_cap = 160 / 192 / 256 で再計算し、差し替えて ε 積分した
(l_cap = 256 は 8 行すべてで張り付き 0 = 打ち切り無しの基準):

| Z | E₀ | max\|Δ\| ノード K=0 β=π | 同 β = 30 mrad | max\|ΔF/F\| (s) | max\|ΔF\| 絶対 (s) | \|ΔN(0)/N(0)\| (= Δσ/σ) |
|---|---|---|---|---|---|---|
| C 6 | 300 | 8.56e-03 | 8.30e-03 | 3.69e-05 (16) | 8.8e-08 (0.5) | 1.9e-07 |
| C 6 | 400 | **4.18e-02** | **4.12e-02** | **8.26e-05 (12)** | **1.8e-07 (0.5)** | 5.6e-07 |
| N 7 | 300 | 2.30e-03 | 2.25e-03 | 5.57e-06 (16) | 2.2e-08 (0.5) | 4.7e-08 |
| N 7 | 400 | 1.40e-02 | 1.39e-02 | 2.48e-05 (12) | 6.8e-08 (0.5) | 2.1e-07 |
| O 8 | 300 | 4.42e-04 | 4.37e-04 | 1.01e-06 (12) | 2.5e-09 (0.5) | 5.5e-09 |
| O 8 | 400 | 4.40e-03 | 4.41e-03 | 4.97e-06 (12) | 2.4e-08 (1.0) | 6.9e-08 |

- **l_cap = 160 では足りない** (C 400 で 16/27、N 400 で 12/19 がまだ張り付く)。192 で N/O は
  全解放、C 400 は l_kin = 215 まで要る。ただし F(s) は 192 と 256 でほぼ同じ
  (C 400 の s = 12: 7.95e-05 vs 8.26e-05)
- ノード単位の変化は最高 ε (ε → ε_max、k_f → 0) に集中し、そこの重み wₑ·row/N(0) は
  1e-10〜1e-5。s = 16 ではノード単位で **13 倍**動く点 (C 400 の node 92、ε = 396.5 keV) も
  あるが、その重みは 1.8e-06。s = 16 への寄与が最大の張り付きノード (node 81、ε = 247 keV、
  重み 1.7e-03) は +4.6e-03 しか動かない
- **s ≤ 2 では相対 ≤ 6.3e-06** (C 400 の s = 2)、s ≤ 1 で ≤ 1.2e-06。観測量が感じるのは
  s < 2 Å⁻¹ だけ (`observable_propagation_2026-08-13.md`) なので、ALCHEMI 側への伝播は 1e-05 未満
- **絶対値では全行・全 s で量子化の半歩 5e-07 未満** (`format_and_sampling_2026-08-12.md`)。
  出荷 `.bin` では最下位ビットがいくつか動く程度
- σ (= N(0)) への効きは ≤ 5.6e-07 — 誤差の階層 (`next_chat_2026-08-20.md`) の ppw 1.54e-06 より下

### 8.3 読み

- §6.1 の「打ち切りは最高 ε ノード側に集中し、ε 積分の重みは小さい」は**実測で裏が取れた**。
  **v4/v5 の出荷データは直さない** (最悪 C K 400 keV で相対 8.3e-05・絶対 1.8e-07)
- ⚠ ただし **相対 8e-05 は収束監査の K ~7e-06 より 1 桁大きい** — 監査の「l_cap 128→160」行が
  何も測っていなかった (P1-9) ことの帰結で、l_cap は軽元素 K の高 s で**監査済みのつまみの中で
  最大の誤差源**になっている (ε 刻み・角度・n_q より上)。
  ⇒ **次にテーブルを再生成する世代では l_cap を 256 にする** (張り付きノードの再計算は
  C K 400 keV の 27 本で 13 s = 行あたり数 % のコスト増)。160 では足りないので「128→160」の
  監査行はそのままでは使えない
- `diag` への l_cap 張り付きフラグ (§6.1 の「本筋」) は src 変更なのでまだ入れていない。
  入れるなら `eps_worker` の戻り値 `l_max == l_cap` を数えるだけで済む

---
