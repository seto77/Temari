# 窓 σ(β,Δ) の求積が 5 桁悪かった — 原因の究明 (260819Cl)

## 0. 一行で

**全格子認証 (14,796 行、16 時間 44 分) で、窓の求積誤差が事前見積り 3.6e-08 に対し
最悪 8.648e-03 だった**。原因は **d 始状態の遠心障壁が作る delayed maximum** で、
現行の **√ε 上 単一 Gauss–Legendre 16 点**が帯域不足になる。⇒ **次数を上げるだけで直る**。

⚠ **求積の族を変えても複合にしても直らない**。等比パネル 4×6 (30 点) は 9.00e-03 で
GL16 とほぼ同じ。**単一 GL32 (32 点) の 4.87e-04 が、認証オラクルの複合 4×12 (48 点)
1.14e-03 より良い**。点数ではなく**次数**が効く。

⚠⚠⚠ **2026-08-19 追記 — 上の 2 段落は `Δ₁ = 0` (√ε 分岐) かつ幅 ≤ 1000 eV の窓でしか
成り立たない。§7 が本節と §3 を上書きする**。認証が測った `Δ₁ > 0` の窓は素の ε 上の GL
だった (§7.1) し、契約は幅を ε_max ≈ 4e5 eV まで許す (§7.2)。広い窓では **√ε 単一 GL128 が
2.35e-03 まで悪化**し、**等比パネル (√ε 上 16×16、256 点) が 5.2e-09 で最良**になる (§7.4a)。
現在の推奨は「固定 GL128」ではなく **√ε (または `ε = ε_max sin²θ`) 統一 + 等比 16×16
パネル** (§7.6。⚠ 作者判断待ち)。⚠ §2.7 の「床の正体」も §2.7a で撤回済 (振幅 ~1.5e-10、
機構未特定)。

## 1. 認証が出した数字

```
認証済み 14,796 行 / 窓 236,728 本
窓の求積 (既定 GL16 vs 複合オラクル 4×12):
  中央値 3.174e-06 / p90 1.890e-04 / p99 4.873e-03 / 最悪 8.648e-03
```

⚠⚠ **契約 §5A の草案 3.6e-08 はスパイクの代表標本だった**。全格子では成り立たない。撤回する。

⚠ **8.648e-03 は真値に対する上界ではない** — オラクル (複合 4×12) 自身が
高精度基準から 1.14e-03 ずれる。契約には「**GL16 と複合 4×12 の observed discrepancy**」
と書く。「誤差」と書いてはいけない。

## 2. 測定の連鎖 (7 つ)

### 2.1 第 3 の方法で「本物か」を確かめた

`tools/window_oracle_probe.jl` の tanh-sinh (√ε 置換を使わない独立な族) を Xe M4 に当てた:

| 窓 | 既定−複合 | 既定−tanh-sinh | 比 |
|---|---|---|---|
| [0,1000] eV | 8.65e-03 | 9.78e-03 | 1.13 |
| [10,1010] eV | 2.89e-03 | 2.89e-03 | 1.00 |

⇒ **本物**。オラクルの作り物ではない。

### 2.2 角度との合成床ではない

各 ε ノードの角度積分を**継ぎ目分割 npt=12** (高精度) に差し替えても、数字が変わらない:

| 規則 | 角度=既定 | 角度=継ぎ目分割 |
|---|---|---|
| GL16 | 9.78e-03 | 9.78e-03 |
| GL32 | 4.87e-04 | 4.87e-04 |
| GL64 | 7.97e-07 | 7.96e-07 |
| GL128 | — | 2.12e-09 |
| GL256 | — | 7.42e-10 |

⇒ **窓と角度は独立**。⚠ ただし GL128/256 は同族 GL384 が基準 = **自己収束**
([[self-convergence-underestimates]])。独立基準 (等比パネル) と一致しているのは GL16/32/64 の 3 行。

### 2.3 ★ 誤差を Chebyshev 係数から**再構成**した — 3 桁一致

GL n 点は 2n−1 次まで厳密なので、誤差は

    E_n = | Σ_{k≥2n} c_k δ_{k,n} | / | I | ,   δ_{k,n} = I[T_k] − Q_n[T_k]

⚠ **c_k はヤコビアン込みの実被積分関数** g(x) = 2x·(dσ/dε)(x²) から取る。
⚠ δ は純粋な数値で物理を要さない。

| 条件 | l | 予測 GL16 | 実測 GL16 | 予測 GL32 | 実測 | 予測 GL64 | 実測 |
|---|---|---|---|---|---|---|---|
| Z=54 M4 @400 keV | 2 | 9.78e-03 | 9.78e-03 | 4.87e-04 | 4.87e-04 | 7.97e-07 | 7.96e-07 |
| Z=53 M2 @200 keV | 1 | 9.28e-05 | 9.28e-05 | 1.23e-06 | — | 4.88e-10 | — |
| Z=26 K @200 keV | 0 | 6.41e-08 | 6.63e-08 | 1.92e-08 | — | 1.07e-09 | — |

⇒ **相関ではなく再構成**。原因の帰属はこれで確定した。

⚠ **Chebyshev–Lobatto の点数は 128 では足りない** — GL64 の予測が N=128 で 7.02e-07、
N=256 で 7.99e-07、N=512 で 7.97e-07 (**N=128 は 12 % ずれる** = alias)。
c_{2n} を見るには **N ≥ 4n** を取ること。

### 2.4 ★ なぜ l=2 だけ係数が減衰しないのか — dσ/dε に山がある

規格化した dσ/dε (窓 [0,1000] eV、β=0.3 mrad):

| 条件 | l | ε=0.05 | ε=2 | ε=10 | ε=35 | ε=75 | ε=1000 | 最大の位置 |
|---|---|---|---|---|---|---|---|---|
| Z=26 K @200 keV | 0 | 1.000 | 0.998 | 0.998 | 0.994 | 0.970 | 0.496 | ε=0 (単調減少) |
| Z=53 M2 @200 keV | 1 | 1.000 | 0.994 | 0.989 | 0.911 | 0.782 | 0.047 | ε=0 (単調減少) |
| Z=54 M4 @400 keV | 2 | 0.019 | 0.074 | 1.000 | 0.371 | 0.228 | 0.003 | **ε ≈ 10 eV** |
| Z=79 M5 @200 keV | 2 | 0.029 | 0.043 | 0.163 | 0.731 | 1.000 | 0.159 | **ε ≈ 75 eV** |

⇒ **d 始状態 (l=2) では双極子の終状態が f 波で、遠心障壁が閾値直上を抑える。**
その結果、断面積の最大が**窓の内側**に来る (delayed maximum)。
山が窓に対して相対的に低い位置にあるほど鋭く、必要次数が上がる —
Xe M4 は山が窓の 1/100 の位置で **k > 129**、Au M5 は 1/13 で **k > 41**。

⚠⚠ **「l=2 だけが難しい」と一般化してはいけない**。全格子の最悪は
**l=0 で 1.09e-03、l=1 で 1.95e-03**。内部に山が無くても、肩・曲率反転・Cooper 型の極小・
チャネルの交代で高次係数は大きくなる。⇒ **言えるのは「全格子最悪の d 殻群についての主要因」まで**。

⚠ **Wigner の閾値則 (σ ~ ε^(l′+1/2)) は原因ではない**。ε = x² と置くと
f(x²)·2x ~ x^(2l′+2) で**どの整数 l′ でも整数冪 = 解析的**になる。
f 波 (l′=3) でも x⁸ で、GL16 は 31 次まで厳密。**閾値の冪そのものは 1 % 誤差を作れない**
(この誤りは codex に指摘されて撤回した)。

### 2.5 層別 — 難しさは 2 つの量と相関する

| l | l′ | 行数 | 中央値 | 最悪 |
|---|---|---|---|---|
| 0 | 1 | 4,974 | 8.31e-07 | 1.09e-03 |
| 1 | 2 | 7,032 | 3.42e-06 | 1.95e-03 |
| 2 | 3 | 2,790 | **8.39e-05** | **8.65e-03** |

幅/閾値 (= 1000 eV / E_th) 別の最悪 (l 固定で r = 0.37–0.52、log-log 傾き 0.74–0.92):

| l | <0.2 | 0.2–1 | 1–5 | >5 |
|---|---|---|---|---|
| 0 | 2.05e-07 | 1.24e-06 | 4.99e-06 | 4.16e-05 |
| 2 | — | 2.47e-05 | **6.45e-04** | 1.61e-04 |

⚠ **これは相関であって決定則ではない**。幅を 1000 eV に固定したまま比を振ると、
比を変えている実体は E_th なので **Z・殻・l・遠心障壁・共鳴位置が同時に動く**。
⚠ **>5 で下がる理由は未説明** (チャネル集団の偏りが第一候補)。
⇒ 本質的な無次元量は幅/E_th ではなく **山の位置 ε_p / 窓幅 W**、あるいは
√ε 座標での山の幅であるはず。**同一 (Z, 殻, E₀, β) で幅を振る試験が要る**。

### 2.6 ⚠ 離散分岐は無関係だった (私の誤りを負のテストが暴いた)

`eps_setup` は ε ごとに `l_max` と生存部分波を決め直す。実際 127 区間中 **14 箇所**で
`lam_max` (8→15)・生存波 (16→52) が切り替わる。
⇒ 「折れ目が床を作っている」と考えたが、**間違いだった**。

`l_cap` を下げれば `l_max = min(l_cap, max(6, min(l_kin, l_barrier)))` で強制できる:

| l_cap | rl.lam_max | 生存波 | f | 相対差 |
|---|---|---|---|---|
| 96 | 9 | 28 | 4.981369036e-08 | 0 (基準) |
| 8 | 9 | 28 | 4.981369036e-08 | 0 |
| 6 | 8 | 24 | 4.981369036e-08 | **2.66e-16** |
| 4 | 6 | 16 | (別値) | 1.10e-07 |

⚠⚠ **最初の検査 (l_cap=8) は効いていなかった** — `rl.lam_max` は**多重極 λ** であって
l_max ではない (λ ≤ l_b + l′ なので l_max=8 でも λ=9 になる)。
**負のテストを打たなければ「段差は消えなかった」と誤って報告していた** ([[prove-the-check-can-fail]])。

⇒ 正しく効かせた l_cap=6 で見ると、**切替の前後で f は 2.66e-16 しか動かない**。
切替点 4 箇所すべてで ≤ 2e-16。⇒ **部分波の打ち切りは折れ目を作っていない**。

⇒ ★ 先に報告した「段差 1.62e-09」は**外挿ノイズ**だった。腕 d で両側から 2f₁−f₂ を取ると
評価の粗さが約 6 倍に増幅される。

### 2.7 ★ 床の正体 = 連続状態 ODE の格子が ε ごとに組み直される

同じ ε を 5 回評価 → **全部ビット同一** (確率的ノイズは無い)。
ε を ±1 ULP 動かすと f は 6.6e-16、±512 ULP で 8.1e-14 (増幅率 ~1.4 = 素直)。

ところが**刻み幅を掃くと下げ止まる** (Z=54 M4、ε≈25 eV、41 点の 4 階差 RMS/f):

| 相対刻み h | 4 階差 RMS/f |
|---|---|
| 1e-04 | 1.92e-09 |
| 1e-05 | 1.90e-09 |
| 1e-06 | 3.03e-09 |
| 1e-07 | 2.50e-09 |
| 1e-08 | 2.16e-09 |
| 1e-09 | 2.88e-09 |
| 1e-10 | **1.71e-09** |

⇒ **6 桁にわたって平ら**。⚠⚠ **ここから先の帰属は 2 回とも誤りだった。次の 2.7a を読むこと。**

### 2.7a ⚠⚠⚠ 訂正 — 帰属を 2 回間違えた (2026-08-19 追記)

**取り下げる主張 (2 つとも誤り):**

| 私が書いたこと | 何が誤りか |
|---|---|
| 「振幅は **~2e-09**」 | ⚠ **4 階差 RMS で測った値**。局所 3 次フィットの残差で測り直すと **~1.5e-10** (13 倍小さい) |
| 「原因は**連続状態 ODE の格子が ε ごとに組み直される**こと」 | ⚠⚠ **多エージェント監査 (2026-08-19) が反証**。`eps_setup` の ε 依存の離散判断は**最も速いもので 3 eV に 1 回**しか切り替わらない。h = 1e-10 相対 (= ε=25 eV で 2.5e-09 eV) まで平らな信号を作るには**切替密度が 7 桁足りない** |

**生値を印字して分かったこと** (Z=54 M4 @400 keV、ε≈25 eV、h=1e-10、11 点):

```text
j=-5: +4.878e-10   ← 直線に乗る
j=-4: -1.192e-10   ← 1 点だけ外れる (直線なら +3.90e-10)
j=-3 … j=+5        ← 完全に直線 (刻み −9.75e-11)
```

⇒ 4 階差 RMS 7.9e-10 は**この 1 点**が作っていた。⚠ ただし **301 点で局所 3 次フィットの
残差を取ると中央値 1.3–1.5e-10 が刻み 1e-04〜1e-10 で平ら**なので、
「孤立した異常値」という次の推測も外れである (広帯域に見える)。

★ **角度規則には依らない** — 継ぎ目分割 (ε でパネル集合が動く) と既定 log-x GL (固定) で
**同じ j に同じ大きさ**で出る ⇒ `eps_setup` の中。

⚠ **機構は未特定**。有力候補は **球ベッセル Miller 規格化のガード窓**
(`src/l0_numerics.jl:319-332`)。窓幅 `|x−nπ| < 1e-8·x` の中では精度が **5e-10** まで落ちると
ソースに実測記録があり、観測した 1.5e-10 と桁が合う。`x = Q·r` は ε とともに動くので
**ε の擬似ランダム関数**になる。⚠ **この件で 2 回外しているので断定しない。**

**結論への影響:** 振幅が 2e-09 → 1.5e-10 に下がっても **GL128 の判断は変わらない**
(いずれも `ppw` 感度 1e-06 のはるか下)。⚠ ただし
「2 経路が互いに頭打ちになる床 ~1e-08 の正体を特定した」という主張は**撤回**する。

⚠⚠ **これは窓だけの話ではない**。出荷の F(s) も同じ `eps_setup` を通る
([[measurement-floor-is-shipping-floor]])。**F 側で実測してから書くこと。**

## 3. 対処 — 固定 GL128

7 条件へ広げた最悪:

| 規則 | 点数 | 最悪 (7 条件) |
|---|---|---|
| 現行 √ε 単一 GL16 | 16 | **9.78e-03** |
| √ε GL32 | 32 | 4.87e-04 |
| √ε GL64 | 64 | 7.97e-07 |
| √ε GL128 | 128 | **1.69e-08** |
| 認証オラクル √ε 複合 4×12 | 48 | 1.14e-03 |
| 等比パネル 4×6 | 30 | 9.00e-03 |
| 等比パネル 8×8 | 72 | 3.69e-05 |

★ **推奨 = 固定 GL128** (codex と一致):

- **GL64 は危険** — 最悪 7.97e-07 で 1e-6 ゲートに対し余裕 1.25 倍しかない。
  7 条件だけの標本なので全格子では悪化する
- **条件で n を変えない** — 契約が任意の Δ₁,Δ₂ を受け付けるので、経験式より固定の方が
  説明・再現・認証が明瞭
- **等比パネルもピーク分割も採らない** — 被積分関数は (床の上では) 滑らかなので、
  分割は高次近似能力を落とす。実測でも複合 48 点 < 単一 32 点
- 将来コストが問題になれば **Gauss–Patterson / Gauss–Kronrod / 入れ子 tanh-sinh** へ。
  ⚠ 自己一致だけを停止条件にせず、独立オラクルで安全係数を較正すること

⚠ **角度の規則を継ぎ目分割へ変えるなら、その経路込みで窓を測り直す。**
旧経路で得た床を新経路へ持ち越せない。

## 4. 契約 §5A の書き方

**現行 GL16 について** (2 段で書く):

> 旧草案の 3.6e-08 は代表標本に限られ、全出荷格子では成り立たなかったため撤回した。
> 出荷格子 14,796 行・236,728 窓において、現行の √ε 単一 GL16 と √ε 複合 GL4×12 の
> observed discrepancy は 中央値 3.17e-06 / p99 4.87e-03 / **最大 8.648e-03**。
> 独立な tanh-sinh で Xe M4 [0,1000] eV を測ると GL16 との差は 9.78e-03 で、
> 1 % 級の誤差が実在することを確認した。
> ⚠ **複合 4×12 自身が 1.14e-03 ずれるので、8.648e-03 は真値に対する上界ではない。**

**GL128 へ替えるなら**:

- ⚠ **再認証が要る** (16 時間 44 分)。規則が変わり、GL128 の誤差は c_256 以降で決まるため
- ⚠ **全格子 observed_max を契約に残すなら全格子でやる**。層別重点標本にするなら
  主張を「**層別標本上の** observed_max」へ弱める
- ⚠ **オラクルが床に当たる可能性がある** — Xe M4 の |c_256| = 4.98e-18、|c_512| = 5.92e-18 と
  既に平ら。この場合に言えるのは「**GL128 は GL256 および独立求積と X 以内で一致し、
  高次係数は約 Y の数値評価床に達している。それ以下の真値誤差は分離不能**」まで。
  「**真値に対して ≤ 床**」とは言えない (測っているのは異なる数値規則間の識別可能差の上限)
- JSONL には行最悪だけでなく **幅・開始位置・β・l 別**の値を保存する
  (現行は行最悪とその位置しか持たないので、236,728 窓を計算していても幅別分布を復元できない)
- 尾の窓 (σ ≈ 0) は相対でなく **atol** で判定する

## 5. 潰し残し

- **同一 (Z, 殻, E₀, β) で幅を振る** — ε_p/W 仮説の検定と、>5 で下がる理由
- **F(s) 側の床の実測** — 2e-09 が出荷値へどう伝播するか
- roughness の出所の特定 (`CONT_PPW` / `CONT_DT_LOG` を振って振幅が動くか)
- 全格子で c_32 を測って**高リスク行を順位づけ**し、新認証の重点標本を決める
  (⚠ **GL128 認証の代替にはならない** — c_256 以降が要るうえ、1 つの [0,1000] eV 上の
  展開から別の開始位置・幅を認証できない)
- GL128 の実時間・メモリ・16 レーン負荷

## 6. 道具

すべて既存。**src は 1 行も変えていない**。

- `tools/window_oracle_probe.jl` — 第 3 の方法 (tanh-sinh)
- `tools/nq_nx_probe.jl` の `knot_split_angular` — 角度を高精度にして窓を分離する
- 本書の Chebyshev 再構成・roughness 掃引・l_cap 強制は scratchpad の使い捨てスクリプトで実施

## 参考文献

- Cooper, J. W. (1962). Photoionization from outer atomic subshells. *Phys. Rev.* **128**, 681.
- Fano, U., & Cooper, J. W. (1968). Spectral distribution of atomic oscillator strengths.
  *Rev. Mod. Phys.* **40**, 441.
- Trefethen, L. N. (2008). Is Gauss Quadrature Better than Clenshaw–Curtis? *SIAM Rev.* **50**, 67.

## 7. ★★★ 監査で判明した、より大きな穴 (2026-08-19 追記)

作者の疑問「GL128 で本当に十分か。GL512 も検討してほしい」を受けて、多エージェント監査
(55 エージェント、48 所見 → 22 が敵対的検証を生存) を回した。**blocking が 4 件**出て、
**そのうち 2 件が本書 §3 の推奨を覆した。**

### 7.1 ⚠⚠⚠ 本書 §2 の解析は**分岐を 1 つしか見ていなかった**

`tools/beta_spike.jl:206` の `window_sigma`:

```julia
if d1_eV == 0.0
    eps = e2 .* x .^ 2          # √ε 正則化
else
    eps = e1 .+ (e2 - e1) .* x  # ★★ 素の GL on ε
end
```

⇒ **Δ₁ > 0 の窓は √ε ではなく素の ε 上の GL である。**
本書 §2 の Chebyshev 再構成も「√ε 上で解析的」も **Δ₁ = 0 の分岐**の話だった。
⚠ 認証が測った Δ₁ は **{0, 10, 100, 1000} eV だけ**で、契約 §3 は**任意の Δ₁ > 0** を受け付ける。

**実測** (Z=54 M4 @400 keV、幅 1000 eV 固定、β=0.3 mrad、基準 = ε 上の tanh-sinh):

| Δ₁ [eV] | ε GL16 | ε GL128 | ε GL512 | √ε GL16 | **√ε GL128** |
|---|---|---|---|---|---|
| 1000 | 2.78e-08 | 5.10e-09 | 2.89e-09 | 1.71e-08 | 4.14e-09 |
| 30 | **2.79e-05** | 2.29e-08 | 2.26e-08 | 1.92e-08 | 2.27e-08 |
| 10 | **2.89e-03** | 1.57e-08 | 1.46e-08 | 3.71e-05 | 1.43e-08 |
| 1 | **2.99e-02** | 9.06e-08 | 1.45e-08 | **7.97e-03** | 1.24e-08 |
| 0.1 | **5.24e-02** | 1.85e-07 | 1.68e-08 | **1.64e-02** | **1.76e-08** |
| 0.01 | **5.43e-02** | 2.27e-07 | 1.69e-08 | **1.36e-02** | **1.51e-08** |

⇒ ★ **素の ε は Δ₁ → 0 で GL16 が 6 桁悪化する。**
⇒ ⚠ **座標を √ε に替えるだけでは足りない** (√ε GL16 も 1.36e-02)。
⇒ ★★ **√ε × GL128 が全 Δ₁ で 1.2e-08〜2.3e-08 と平ら。**

⚠ **codex の指摘**: 契約は Δ₁ に下限を置かないので、素の ε では**どんな固定次数でも
一様な保証にならない** (物理的な分岐点 ε=0 が区間のすぐ外に寄り続ける)。
√ε なら Δ₁ → 0 で座標が連続に繋がる。★ **Δ₁ = 0 は現行式をそのまま残せば
既存値は 1 ビットも動かない** (数学的に同じ写像なので、丸め順まで保てる)。

⚠ **私の「立ち上がりは ε^{7/2} 型だから √ε では線形化できない」は半分誤り** (codex)。
`ε = x²` とすれば `ε^{7/2} → x⁷` で**整数冪 = 解析的**になる。
正しくは「**√ε は分岐点を正則化するが、狭い山そのものは平坦化しない**」。

### 7.2 ⚠⚠⚠ 認証は窓幅を 1000 eV で打ち切っていた — 契約は ε_max まで許す

**400 倍の外挿**である。実測 (Z=54 M4 @400 keV、Δ₁ = 0 = **現行の √ε 座標**):

| 幅 [eV] | 幅/ε_max | GL16 | GL64 | **GL128** | **GL512** |
|---|---|---|---|---|---|
| 1,000 | 0.003 | 9.78e-03 | 8.14e-07 | **1.54e-08** | 1.63e-08 |
| 10,000 | 0.025 | 1.56e-02 | 5.53e-04 | 6.49e-07 | 2.29e-08 |
| 100,000 | 0.250 | 1.59e-01 | 8.43e-03 | **1.47e-04** | 1.76e-07 |
| 199,700 | 0.500 | 9.10e-02 | 6.32e-04 | 7.79e-04 | 6.55e-07 |
| 395,300 | **0.990** | 3.68e-01 | 5.35e-03 | **2.35e-03** | **4.93e-07** |

⇒ ★★★ **GL128 は幅とともに 1.54e-08 → 2.35e-03 と 5 桁悪化する。**
**作者の「GL512 も検討してほしい」は正しかった。**

**原因は定量的に説明できる** — Xe M4 の山は ε ≈ 10 eV。幅 395,300 eV を √ε で張ると
山の位置は `√(10/395300) = 0.005` = **区間の最初の 0.5 %**。GL128 の節点でそこに入るのは
2〜3 点だけ。

★ **裏づけ**: 同じ幅でも **Δ₁ = 10 eV (窓が山から始まる) なら √ε GL128 が 1.69e-08** と完璧。
⇒ **難しさを決めるのは幅そのものではなく「窓が山を跨ぐか」である。**

Z=26 K (l=0、山なし) は幅 0.99·ε_max でも **√ε GL128 が 1.13e-08** で問題ない。
⇒ **§2.4 の層別 (l=2 が難しい) がここでも効く。**

### 7.3 ★ 同一条件での誤差階層 (codex の要求)

これまでは**つまみごとに別の標本**で最大を取っていた。同一条件で全部締めてから
1 つずつ出荷値へ戻した (`tools/budget_endtoend.jl`、8 条件、窓 [0,1000] eV、β=0.3 mrad):

| つまみ | 同一条件での寄与 (最悪) |
|---|---|
| **窓 GL512→16 (出荷)** | **9.78e-03** |
| **連続状態 ppw 140→25 (出荷)** | **1.54e-06** |
| Q 表 n_q 2432→240 (出荷) | 1.91e-07 |
| ε 刻み dt_log 2.5e-4→2e-3 (出荷) | 6.29e-09 |
| 角度 継ぎ目分割→log-x n_x=64 (出荷) | 1.28e-09 |
| ★ 窓 GL512→128 (提案) | **4.54e-10** |
| ★ Q 表 2432→1216 (提案) | **4.63e-10** |

⇒ **幅 1000 eV の窓については、提案の 2 つはどちらも ppw 感度の 1/3400。**
⇒ ★ **角度の既定 (log-x n_x=64) は σ(β,Δ) では 1.3e-09 しか効いていない** — β が小さく
角度域が狭いため。⚠ **F(s) (K≠0、全角度) の話ではない。**

### 7.4 ⚠ 参照関数の切替が出荷経路を跨ぐ

`src/l2_continuum.jl:650` / `:321` の `use_bessel = abs(eta) < eta_bessel` は、
**エネルギー規格化の参照を Coulomb 関数 → Riccati–Bessel へ切り替える**。

⚠⚠ `src/l0_numerics.jl:87` のコメント「イオン化経路 (z_asym = 1) では確かに通らない」は
**誤り**。`η = −z_asym(1+ε/c²)/k` から `|η| = ETA_BESSEL = 0.02` を解くと

**ε_c = 1390.4872 Ha = 37.8371 keV**

⇒ **E₀ ≥ 40 keV (出荷の絶対 E₀ ノード 22 個中 21 個) では出荷の ε 積分域が跨ぐ。**
⚠ 段差の大きさと**その上に載る重み**は `tools/eta_bessel_probe.jl` で測る (未実施)。
⚠ **定数を埋め込まない** — 同ツールは `z_asym` と切替条件から毎回解く。

### 7.4a ★★★ 広い窓の直し方 — オラクルが先に壊れていた

⚠⚠ §7.2 の表 (基準 = ε 上の tanh-sinh) には**オラクル自身の誤差が混ざっていた**。
tanh-sinh は**端点**に節点を寄せるので、**区間の 0.0025 % の位置にある山**を解像できない。

⇒ 基準を **等比 32×24 パネル (768 点)** に替えて測り直した
(48×24 = 1152 点との差 **1.0e-09〜1.3e-09** なので基準は健全)。

**Z=54 M4 @400 keV、Δ₁ = 0:**

| 規則 | 点数 | 幅 1e5 eV | 幅 3.953e5 eV (0.99 ε_max) |
|---|---|---|---|
| *tanh-sinh (旧基準そのもの)* | — | **1.84e-07** | **4.79e-07** |
| √ε 単一 GL128 | 128 | 1.47e-04 | 2.35e-03 |
| √ε 単一 GL512 | 512 | 7.60e-09 | 1.36e-08 |
| √ε 単一 GL1024 | 1024 | 1.56e-09 | 2.56e-09 |
| 等比 8×16 | 128 | ⚠ 4.28e-07 | 1.24e-08 |
| **★ 等比 16×16** | **256** | **5.06e-09** | **5.20e-09** |
| 等比 16×24 | 384 | 4.41e-09 | 9.28e-09 |

⇒ ★ **§7.2 で「GL512 が 4.93e-07 で頭打ち」と書いたのは誤り。**
正しくは **GL512 は 1.36e-08**、4.93e-07 は**オラクルの誤差**だった。

⇒ ★★ **等比 16×16 (256 点) が、GL512 (512 点) を半分の点数でわずかに上回る。**
⚠ **8 パネルでは足りない** (幅 1e5 eV で 4.28e-07)。

★★ **角度側と同じ教訓が成り立った** — 難しさが「区間に対して狭い構造」なら**次数ではなく分割**。
⚠ §3 で「等比パネルは効かない」と書いたのは**幅 1000 eV での話**であり、
そこでは山が区間の ~1 % を占めるので分割の利得が出なかった。**両者は整合する。**

⚠⚠ **教訓**: この 5e-07 の「床」は規則ではなく**オラクルの誤差**だった。
[[self-convergence-underestimates]] の逆で、**オラクルが先に壊れる**ことがある。
⇒ **規則どうしを直接突き合わせて基準の健全性を確かめる** ([[oracle-can-break-first]])。

### 7.5 ⚠ まだ潰していない blocking

| | 内容 | 影響 |
|---|---|---|
| 3 | `observed_max` は**同程度の分解能の 2 規則の一致統計**なので、**どちらも解像できない狭い構造は差から消える** | 8.648e-03 は「未知の種類の**下限**」 |
| 4 | 認証の角度の数字 2.811e-05 は **ε = 50 eV の 1 点標本** (全 14,796 行で同じ) | 角度側の認証値の代表性 |
| 6 | K≠0 では `Q₋²(x,φ) = P(x) + C(x)cosφ` なので PCHIP 節点が **(x,φ) 平面の曲線**。1 つの x に沿って φ 方向に **60–345 回**跨ぐ | 素直な一般化は **~400 倍**の費用 |
| 7 | **`n_phi` を単独で振った実験がリポに 1 つも無い** | F(s) の精度主張 |

### 7.6 ⇒ 本書 §3 の推奨を差し替える

**旧**: 「固定 GL128」

**新 (⚠ 作者判断待ち)**:

1. **全窓を `√ε` にする** (`Δ₁ = 0` は現行式のまま = 既存値不変)
2. **幅 ≤ 1000 eV かつ山を跨がない窓なら GL128 で足りる** (ppw 感度の 1/3400)
3. ⚠ **幅を契約の上限まで許すなら GL128 では不足** (幅 1e5 eV で 1.47e-04)。
   **★ 推奨 = √ε 上の等比 16×16 パネル (256 点、最悪 5.2e-09)**。
   単純さを取るなら **√ε 単一 GL512 (512 点、最悪 1.4e-08)** でもよい。
   ⚠ **等比 8 パネルでは足りない**
4. ★ codex 案 **`ε = ε_max sin²θ`** なら `√ε ∝ sinθ`・`√(ε_max−ε) ∝ cosθ` で
   **両端の平方根を同時に正則化**できる (§7.5 の blocking 2 も解ける)
5. **`ETA_BESSEL` の切替点 (ε_c) をパネル境界にする**

⚠⚠ **契約 §5A の窓は凍結しない** (codex と一致)。§7.5 の blocking 3 が残る限り、
`observed_max` は上界どころか「未知の種類の下限」である。

## 8. ★★★ 指示書 §10 の 1–3 を実行した結果 — 規則を決め、本番候補を実装した (2026-08-19 夜・追記)

前段 = `docs/handover/next_chat_2026-08-20.md` §10 (着手順 1–3) と §2.2 (提案)。本節の実測で
§7.6 の「作者判断待ち」の材料が揃ったので、**本番候補を `tools/sigma_beta_delta.jl` に実装した**
(§8.5)。⚠ 規則の採否と再認証 (数日) の起動は作者判断のまま。

### 8.1 参照関数の切替 ε_c — 段差は実在するが小さい (`tools/eta_bessel_probe.jl`)

```text
条件                   ε_max[keV]    段差 |Δf|/f   ε>ε_c の重み     σ への寄与
Z=14 K @200 keV             198.2       3.74e-06       2.63e-06       9.82e-12
Z=26 K @200 keV             192.9       8.42e-06       2.97e-04       2.50e-09
Z=54 M4 @400 keV            399.3       1.19e-05       4.50e-08       5.34e-13
Z=79 M5 @400 keV            397.8       6.22e-06       3.84e-06       2.39e-11
Z=6 K @400 keV              399.7       1.85e-05       1.27e-08       2.36e-13
```

- 切替は **ε_c = 1390.4872 Ha = 37.8371 keV** (切替条件から解いた。定数は埋め込まない)
- `dσ/dε` の相対段差は **4e-06〜2e-05** で実在する。全域 σ への寄与は **≤ 2.5e-09** (重みが小さい)
- ⚠ ただし **ε_c を跨ぐ狭い窓** ([ε_c−100, ε_c+100] eV など) では段差そのものが被積分関数の
  大きさに対して効く (GL n 点で ~段差/n)。⇒ **本番規則では ε_c をパネル境界にする** (安い。§8.5)
- ⚠ `src/l0_numerics.jl:87` のコメント「イオン化経路では通らない」は誤り (src は凍結中なので
  次の全再生成で直す)

### 8.2 n_q の補間誤差の**直接**測定 (`tools/nq_interp_direct.jl`、入れ子格子の中点 = 補間を通さない値)

| n_q | 点ごと最悪 | 積分への影響 (最悪、8 条件) |
|---|---|---|
| 240 | 1.43e-02 | 2.84e-04 |
| 540 | 1.80e-03 | 1.18e-05 |
| 810 | 4.86e-04 | 2.38e-06 |
| **1216** | 1.77e-04 | **4.74e-07** |

最悪は **Z=20 M1 @400 keV (ε=2000 eV)** — `nq_nx_2026-08-19.md` §12 の 5 チャネル標本 (1216 で 5.6e-08)
には無かった条件。⚠ **出荷 F(s) テーブルの設定 n_q = 360 (HIGH) を同じ方法で測ると、積分への影響は
最悪 5.85e-05 (同じ Ca M1 条件、点ごと 5.2e-03)**、他 7 条件は 5e-09〜2.5e-07 (2026-08-19 深夜、scratchpad)。
F(s) = N(K)/N(0) は比なので一部は相殺するはず ⇒ **出荷経路そのもの (`compute_NK`、HIGH_SETTINGS) で
n_q を 360 → 720 → 1440 と振って測った** (codex の助言、2026-08-19 深夜、scratchpad `fs_nq.jl`):

| | Ca M1 @400 keV | Fe K @200 keV |
|---|---|---|
| N(0) の相対差 360 vs 1440 | 7.2e-07 (⚠ 720 vs 1440 は 4.0e-06 — **n_q に対して非単調**) | 4.2e-08 |
| F(s) の**絶対**差 360 vs 1440 (最大) | **9.3e-07** (s = 0.5。量子化の半歩 5e-07 の 1.9 倍) | 3.2e-08 |
| F(s) の相対差 360 vs 1440 (最大) | 1.9e-04 (s = 4、F = 8e-05) | 2.2e-06 (s = 16) |

⇒ 出荷 F(s) への影響は Ca M1 でも絶対 ~1e-06 (ppw 感度の絶対最大 1.3e-06 と同程度)。**今の出荷データは
直さない**。⚠ Ca M1 の N(0) が n_q 720 で 1440 より 4e-06 離れるのは、S(Q) の Cooper 極小の構造に対して
log-Q 節点の落ち方が効いている (非単調) ためで、**「n_q を上げれば単調に良くなる」は M1 では成り立たない**。
F v6 の台帳へ: M 殻 (特に 3s) の n_q、または Q 格子の局所細分 (gen_production の収束監査「n_q 360→540」が
M1 × 高 E₀ を含んでいたかの確認込み)。⚠ **標本を広げたらまた悪化した** (5.6e-08 → 4.7e-07)。§12 の 5.6e-08 は取り下げ、
「8 条件の直接中点標本上の observed_max 4.74e-07」と記録する。1216 は ppw 感度 (1.5e-06) より下なので
本番 n_q = 1216 とする (2432 は単価 2 倍。深いオラクル用)。

### 8.3 ★ 窓の座標と規則 — 第 2 版 (`tools/window_coord_probe.jl --width2`)

§7.4a の教訓 ([[oracle-can-break-first]]) を踏まえ、基準を **√ε 等比 32×24 (768 点)** に替え、基準の
健全性を**別座標の sin²θ 等比 48×16 (768 点)** との差で毎行確認した (全 90 条件で ≤ 2.4e-08、⚠ 無し)。
5 行 (Xe M4 @400 / Au M5 @200 / Fe K @200 / Ca M1 @400 / C K @30) × Δ₁ {0, 0.01, 10} eV ×
幅 {1e3, 1e4, 1e5, 0.5ε_max, 0.99ε_max, **ε_max−Δ₁ (上端ちょうど)**}、β = 0.3 mrad:

| 規則 | 点数 | 全 90 条件の最悪 (基準との差) |
|---|---|---|
| 出荷 GL16 (Δ₁=0 は √ε、Δ₁>0 は素の ε) | 16 | **1.00e+00** (Ca M1、Δ₁=0.01 eV、幅 ≥ 1e5 eV) |
| √ε 単一 GL512 | 512 | 2.19e-08 |
| **√ε 等比 16×16** | 256 | **2.35e-08** |
| √ε 等比 16×24 | 384 | 3.62e-08 |
| sin²θ 単一 GL256 | 256 | **6.89e-05** (Xe M4、幅 0.99ε_max) |
| sin²θ 単一 GL512 | 512 | 2.87e-08 |
| **sin²θ 等比 16×16** | 256 | **3.49e-08** |
| sin²θ 等比 16×24 | 384 | 2.76e-08 |

読み:

- ★ **座標 (√ε vs sin²θ) の差は床 (§8.4) の下で見えない**。効いているのは「単一規則か等比パネルか」。
  単一規則は 512 点でやっと、256 点では壊れる (sin² GL256 が 7e-05)。**等比 16 パネル × GL16 (256 点)
  なら全条件で床**
- **Δ₂ = ε_max ちょうど** (kf → 0 の √ 分岐点が端) でも等比パネルは床 (sin²θ は両端を正則化するので
  理論上の保証が 1 つ増える。実測では √ε と区別できない)
- **旧規則の Δ₁ > 0 分岐 (素の ε) は Ca M1 で相対誤差 1.00** — 積分をまるごと落とす

### 8.4 ★★ 床 ~1e-08 の正体 = `ppw` (連続状態 ODE の離散化)。n_q ではない (`tools/window_floor_probe.jl`)

私の仮説「n_q=240 の Q 表補間誤差が ε の擬似ランダム関数になって床を作る」を 2×2 + 共通 Q 格子で試した
(√ε 等比 16×16 と sin²θ 等比 48×16 の差。β = 0.3 mrad):

| 条件 | 幅 | n_q 240→1216 | ppw 25→100 | 共通 Q 格子 |
|---|---|---|---|---|
| Z=54 M4 @400 keV | 1e3 eV | 9.48e-09 → **9.48e-09** (不変) | 9.48e-09 → **4.28e-10** (22×) | 2.88e-09 / 9.75e-09 (効かない) |
| 同 | 1e5 eV | 3.98e-09 → 3.98e-09 | 3.98e-09 → 2.00e-10 (20×) | 1.33e-08 / 3.54e-09 |
| Z=26 K @200 keV | 1e3 eV | 3.13e-08 → 3.13e-08 | 3.13e-08 → **2.57e-11** (1200×) | 3.16e-08 / 3.06e-08 |
| 同 | 1e5 eV | 1.46e-08 → 1.46e-08 | 1.46e-08 → 4.86e-10 (30×) | 5.08e-08 / 1.54e-08 |

⇒ **仮説は反証された。床は `ppw = 25` の連続状態 ODE 離散化が ε に対して持つ粗い成分が、
規則ごとに違うノードで標本化されて現れるもの** (n_q は無関係)。
⚠ 2 つのスケールを混同しないこと (codex): (i) 求積ノード間隔で見える ~1e-08 の粗い成分 = ppw、
(ii) §2.7a の h→1e-10 で見える ~1.5e-10 の微視的 roughness = **依然として機構未特定**。
§2.7a が撤回した「ODE 格子」説は (ii) についてであり、本節は (i) の帰属。
⚠ これは「ODE の真値誤差が 1e-08」という意味ではない。ppw 感度 1e-06 (§7.3) とは別量。
⚠ 出荷 F(s) も同じ機構の床を持つ ([[measurement-floor-is-shipping-floor]]) — ⚠⚠ ただし**出荷テーブルの
ppw は 25 ではなく 30** (`HIGH_SETTINGS`: ppw 30 / dt_log 1e-3 / l_cap 128 / n_q 360 / n_x 96。
`src/prod_v5_jl/*.json` の `settings` で確認、2026-08-19 深夜)。08-20 §4 と本節の「(出荷)」ラベルは
`PROD_SETTINGS` (σ(β,Δ) のスパイク・認証 v1 の設定) のことで、出荷値ではない。床の大きさは ppw 30 なら
25 よりやや下がる (未測定)。
⇒ **窓規則どうしの比較は ~3e-08 より下を見られない**。認証の合否はその上に置く (§8.6)。

### 8.5 ★ 本番候補の実装 (`tools/sigma_beta_delta.jl`、commit `5d325ab`)

codex との 2 巡の議論 (2026-08-19) を経て次で実装した。⚠ **出荷本番ではなく、凍結 src に依存する
版管理された候補実装・認証対象**:

| | 規則 |
|---|---|
| 窓の座標 | **θ = atan(√ε, √(ε_max−ε))** (ε = ε_max sin²θ、ε_max−ε = ε_max cos²θ は引き算で作らない) |
| 窓の規則 | **θ 上の等比 16 パネル × GL16 (256 点)**。θ₁=0 は最下パネル [0, δθ·θ₂] (δθ = 1e-4。⚠ 切り捨てではなくメッシュ設計値、全域を積む) + 15 等比 |
| ε_c | 開区間 (Δ₁,Δ₂) にあれば**パネル境界に追加** (重複は表現上同一のみ除く。物理的な丸めはしない) |
| 表現限界 | GL 節点が端点に丸まるパネル (ε_c の 1 ULP 隣など) は寄与 0 にして `n_degenerate_panels` に数える (黙って反対枝を積まない) |
| 角度 | PCHIP 継ぎ目 + `bt2 = 0` で分割した GL12、`Q² = dq²·e^x`、環状は [x(β_in), x(β_out)] を直接 |
| Q 表 | n_q = 1216 |
| ppw | 25 (⚠ 出荷 F(s) テーブルは 30。§8.4 末尾の訂正 → 候補 v1 は出荷の連続状態設定 ppw 30 / dt_log 1e-3 / l_cap 128 / sig_thresh 1e-13 に揃える。§8.8) |
| 入力検証 | 契約 §3 (検証 → clip → 空窓・β=0 の 0 返し → 状態構築、の順) |
| 規格化 | own と Bote の**両方を常に返す**。分母 σ_own,total は**同じ規則**で [0, ε_max]・β = π |
| 診断 | `window_indicator` (Legendre モードの尾 k=13..15。⚠ 診断のみ、誤差推定ではない) |
| 和 | パネル順・ノード順の固定順序 (1 スレッドと 6 スレッドでビット同一を実測) |

煙試験: 入力検証 5 種が拒否 / β=0・空窓は 0 / 環状の直接積分と差分が 2e-16 で一致 /
**全域 σ (β=π, [0,ε_max], 横断 off) が出荷 JSON の `sigma_own_nm2` (Fe K @200) と 4.6e-06 で一致**
(出荷経路は eps_nodes + log-x n_x=96 なので、その差は出荷側の既知の求積誤差の大きさ)。

### 8.6 認証 v2 (`tools/certify_sigma_v2.jl`) — 事前登録済、pilot を走行中

正本 = `docs/notes/certification_v2_preregistration_2026-08-19.md` (値を見る前に commit)。
オラクル = √ε 上の等比 24×16 (座標もパネル数も違う、同じ n_q = 窓求積の差を測る) + 角度は閉形式と
tanh-sinh。窓 = Δ₁ {0, 0.01, 10, 1000} × 幅 {10, 1e3, 1e5, ε_max−Δ₁} + ε_c 跨ぎ 2 本 = 最大 18/行、
**1 窓 = 1 JSONL 行**。合否 |P−O| ≤ 1e-7|O| + 1e-9·σ_ref。
**費用: 1 行 ≈ 2,500–3,200 s (1 スレッド) ⇒ deep (全 525ch × E₀ 3 点 ≈ 1,580 行) は 16 レーンで
約 2.9 日** — 作者判断 (codex も「作者不在で起動しない」)。pilot (sentinel 11 行) は走らせた (§8.7)。

### 8.7 pilot の結果 — ★★★ 大 β × 高 ε で不合格、機構 2 つを特定 (正本 = `certification_v2_pilot_2026-08-19.md`)

- **β ≤ 30 mrad は全窓床 (最悪 5.3e-08)**。角度 ~2e-15
- ⚠⚠ **β ≥ 60 mrad × 高 ε へ届く窓 (幅 ≥ 1e5 eV、to_epsmax) で 32/174 窓が不合格、最悪 9.45e-05**
  (Zn M3 @300 keV)。K 殻・L1 は全合格
- 機構 (a): **l_max(ε) の階段** — src の l_kin = ⌈κ·min(r_core, 6/Z)⌉+12 が 1 ずつ増えるたびに大 β の
  dσ/dε が ~1e-06 跳ぶ (l_cap=12 で定数化すると 16 vs 32 パネル差 1.4e-06 → 9.8e-09。ppw / dt / n_q /
  sig_thresh は無関係)。§2.6 の「2.66e-16」は β=0.3 mrad で高 l′ が効かなかっただけ
- 機構 (b) ★★★: **その l_kin は広がった軌道の高 ε で収束していない** — Xe M4 @400 keV、ε=30 keV、β=π で
  l_max 18 (src) → 80 で **+7.3 %** 単調収束 (ε=5 keV −2.7e-04、ε ≤ 1 keV ≤ 1e-08)。Fe K・Au L3・C K は
  収束済 (≤ 4e-05)。原因 = m = min(r_core, 6/Z) の 6/Z が 3d (r_core 2.64) に対して小さすぎる。
  **出荷 σ_own で ~1e-03、F(s) の高 s (M4/M5) で百分率級の可能性 — F 側で測るまで主張しない (F v6 台帳、最優先)**
- 配管: 行単位 flush のため Ca M1 @400 (> 2 h) が watchdog に殺された → 窓ごと flush に修正
