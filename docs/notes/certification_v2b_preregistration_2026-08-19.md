# σ(β,Δ) 本番候補規則 **v2** の認証 — 事前登録 (2026-08-19 深夜)

⚠ **本書は値を見る前に書く** (pilot v1 の結果と、1 窓の煙試験は見た — §0 末尾に明記)。
合否規則・標本・保存項目・主張の範囲をここで固定し、走らせたあとに動かさない
(動かすなら追記で「事後変更」と明記する)。
前段 = `docs/notes/certification_v2_preregistration_2026-08-19.md` (候補 v1 の事前登録。pilot v1 で
**β ≥ 60 mrad × 高 ε の窓 32/174 が不合格**、機構 2 つを特定 — `certification_v2_pilot_2026-08-19.md`)。
**v1 の事前登録は閾値も標本も事後変更しない**。本書は**別版 (v2) の事前登録**である (v1 §7.2 の手順)。

対象 = `tools/sigma_beta_delta.jl` の `SIGMA_RULE_V2` / 台本 = `tools/certify_sigma_v2.jl --rule v2`。
実施記録は本書末尾 §7 に追記する。

## 0. 四行で

1. **v1 から変えたのは 2 点だけ**: (a) **連続状態を出荷 F(s) テーブルと同じ HIGH に揃える**
   (ppw 30 / dt_log 1e-3 / l_cap 128 / sig_thresh 1e-13。v1 は PROD の ppw 25 / dt 2e-3 / l_cap 96 / sig 1e-12 で、
   事前登録 v1 の「ppw = 25 (出荷と同じ)」は誤記だった)、(b) **部分波数 l_max を `min(l_cap, ⌈κ(ε)·r_core⌉ + 12)`
   にする** (src の `l_kin = ⌈κ·min(r_core, 6/Z)⌉ + 12` は 3p/3d の高 ε で収束していない —
   `lkin_truncation_2026-08-19.md`。`:kappa_rc`)。**窓 (θ 上の等比 16×GL16 + ε_c 境界)・角度 (継ぎ目分割 GL12)・
   n_q = 1216・規格化 (own と Bote の両方、分母は同じ規則で [0, ε_max]・β = π) は v1 と同一**
2. **オラクル**: v1 と同じ (√ε 上の等比 24 パネル × GL16 = 384 点、座標もパネル数も違う) だが、
   **ノードの部分波数は本番と同じ `node_rl` を通す** (= 同じ `:kappa_rc`)。⇒ P−O は v1 と同じく
   **窓求積の差**を測る。打ち切り (l_max) の誤差は P−O には**見えない** — それは別の測定
   (`tools/lkin_sweep.jl` の krc12 vs krc32c256) で並記する
3. **合否・標本・β・保存項目は v1 と同一** (rtol 1e-7 + atol 1e-9·σ_ref、sentinel 11 行、deep = 全 525ch × E₀ 3 点、
   β 8 本、窓 ≤ 18/行)。⚠ 事後に変えない
4. **主張の範囲**: 「**この決定論的な層別標本上の** P と O の observed discrepancy」。契約域全体の上界ではない。
   n_q・ppw・参照関数の段差・部分波打ち切り・モデル誤差は**含まない**

⚠ **値を見る前に見たもの (正直に書く)**: (i) pilot v1 の全結果 (v2 を作る理由そのもの)、(ii) 煙試験 1 窓 —
Xe M4 @400 keV、窓 [1000, 101000] eV、β = {10, 30, 200} mrad で v2 の P−O が −7.4e-09 / −6.7e-09 / −4.1e-09
(v1 の同じ窓は β=200 で 16 vs 32 パネル差 1.4e-06、pilot の scaled 3.8e-05 級)。これは「機構 (a) の階段が
:kappa_rc で消えるか」の配管確認であり、合否規則はこの値を見て決めていない (v1 から据え置き)。

## 1. 対象版 (走らせる直前に埋める。⚠ 空欄のまま走らせない)

| 項目 | 値 |
|---|---|
| git commit | **§7.1 に記入** (pilot v2 は codex レビュー反映後の commit で走らせる。本書の初版 = `334c35b`) |
| dirty | 走行前に commit する (`git status --porcelain -uno` が空) |
| Julia | 1.11.9 (`julia +1.11`) |
| `CERT_FP_V2` と内訳 | 台本が起動時に印字。JSONL の各窓にも `cert_fp`。**規則ごとに違う指紋** (`rule` の名前が入る) |
| 規則の文字列 | `win:sin2theta-geo16xGL16-dth0.0001-epsc/ang:knotsplit-GL12-exactx/nq1216/ppw30.0/dt0.001/lcap128/sig1.0e-13/lmax:kappa_rc+12` |
| `model_id` 接尾辞 | `-sigma-candidate-v2` (`rule_version`) |
| ε_c | 切替条件から毎回解く (1390.4872 Ha = 37.8371 keV)。埋め込まない |
| スレッド | レーン 1〜2 スレッド。和はパネル順・ノード順の固定順序 (スレッド数非依存) |

## 2. 標本 (固定 — v1 §2 と同一)

- **deep**: 出荷格子の全 525 チャネル × E₀ 3 点 {最小, 中央添字 (偶数個なら下側), 最大} + sentinel 行
- **sentinel 行 (pilot)**: Xe M4 @400/@170、Au M5 @200、Ca M1 @400、C K @400/@30、Fe K @200、Zn M3 @300、
  Rn M5 @300、Ag L1 @200、I M2 @200 (11 行)。
  ⚠ **Ca (20) M1 は出荷格子の外** (M1 は Z = 30–86。`all_channels`) — n_q 補間の最悪条件として入れた
  ストレス行で、F(s) テーブルにはない。結果は「格子外の参考」として層別する (事後に外さない)
- **窓**: Δ₁ {0, 0.01, 10, 1000} eV × 幅 {10, 1e3, 1e5, ε_max−Δ₁} + ε_c 跨ぎ [ε_c ∓ 100]、[ε_c ∓ 0.01] eV
  (≤ 18/行、契約外は `out_of_domain=true` で記録)
- **β**: {0.3, 1, 3, 10, 30, 60, 100, 200} mrad
- **角度**: 行に 2 点 (ε = 50 eV、0.05 ε_max)、閉形式 (縦) と tanh-sinh (横断)。観測のみ、ゲートにしない

## 3. 合否規則 (固定 — v1 §3 と同一)

    |P − O| ≤ rtol·|O| + atol·σ_ref,   rtol = 1e-7,  atol = 1e-9,  σ_ref = 同じ行の [0, ε_max] の O (β ごと)

- 分母は |O|。記録は P, O, diff, rel, scaled をすべて残す。集計の正式な量は scaled の最悪・中央値・p99
- rtol = 1e-7 の根拠は v1 と同じ (規則どうしの差は連続状態 ODE の離散化で ~1e-08〜3e-08 に頭打ち。
  ⚠ その測定は ppw 25。**ppw 30 の床は未測定**で、25 より下がると見込むが、床が下がっても閾値は下げない)
- ⚠ **不合格が出ても規則を書き換えて合格にしない**。改訂は v3 として別に事前登録する

### 3.1 v2 で特に見るもの (観測。合否規則には足さない)

| 問い | 見方 |
|---|---|
| 機構 (a) の階段の段は閾値の下に落ちたか | ⚠ 階段そのものは :kappa_rc でも残る (⌈κ·r_core⌉ が 1 増えるたびに部分波空間が変わり、P と O はノードが違うので相殺しない)。期待は「足される波が κ·r_core の 12 波外側なので 1 段が床の下」。見方 = β ≥ 60 mrad × 幅 ≥ 1e5 eV / to_epsmax の窓の scaled が床 (≤ 3e-08 級) に落ちているか。pilot v1 では 1e-05〜1e-04。⚠ まだ見えるなら v3 の直し方は「⌈κ·r_core⌉ の切替 ε (閉形式) をパネル境界に足す」— v2 に事後で入れない |
| 機構 (b) の打ち切りは P と O の両方にどう残るか | **P−O には出ない** (同じ l_max)。`l_max_max` が l_cap=128 に張り付く窓の数を数える (張り付きは打ち切りが cap で決まっている印)。⚠⚠ **cap に張り付いたノードを含む窓は、P−O が合格しても cap-128 の打ち切りバイアスの大きさを何も制約しない** (ε とともに増える bias であって階段ではない)。その窓は別の層として報告し、打ち切りの監査 (§5) を評価するまで v2 の精度主張に含めない。該当しやすい行 = Ca M1 (r_core 9.79)、Zn M3 (7.12)、As M4 (8.9) |
| 費用 | 1 行の所要 (v1 の 2,500〜4,450 s に対し煙試験では 1 窓 3.3 倍)。deep の見積りを更新する |
| 床 | β ≤ 30 mrad の scaled の分布が v1 (中央値 1e-09 級、最悪 5.3e-08) からどう動くか (ppw 30 の効果) |

## 4. 保存項目

v1 §4 と同一 + `rule_version` (`v1`/`v2`) + `l_max_policy` + `rule_config` (SigmaRule の全フィールド。
`rule` の短い文字列は互換用で `transverse` と v1 既定の連続状態設定を省く) + `l_max_min`/`l_max_max`
(窓の中で使った l_max の範囲)。1 窓 = 1 JSONL 行、**窓ごとに flush** (pilot v1 の配管欠陥の修正)。
⚠ `state_sha` は束縛軌道 (u_b, r_b) だけのハッシュで、イオン場や Dirac 束縛状態の完全な同一性ではない。
⚠ 集計 (`--summary`) は指紋が混在していると拒否する (`--fp` で 1 種に絞る)。済み判定は (指紋, 行) ごと。

## 5. 事前に分かっている限界 (測る前に書く)

- P−O は**窓求積の差**。n_q の補間バイアス (1216 で 4.74e-07、8 条件)・ppw 30 の離散化バイアス・
  **部分波打ち切り (l_cap = 128 に張り付く高 ε ノード)** は見えない。打ち切りは `tools/lkin_sweep.jl`
  (出荷 F(s) 経路の写し、HIGH 設定、s は LK_S の 10 点 {0, 0.25, 0.5, 1, 2, 4, 6, 8, 12, 16} Å⁻¹ —
  ⚠ 「s ≤ 2 の最大」はこの標本点上の最大であって、出荷 s 格子 321 点や連続の最大ではない) で別に測り、
  契約に並記する。⚠ krc32c256 − krc12 は cap (128 → 256) と margin (+12 → +32) を同時に動かすので両者を
  分けられない — 分けるには (+12, cap256) と (+20, cap256) を追加で回す (`--conv-policies`)。
  **これは感度 (aggressive reference との増分) であって、誤差上界でも収束の証明でもない**
- ★ **打ち切りの収束判定 (事前登録)**: krc12 に対する aggressive reference (krc32c256) の差が
  **|ΔN(0)/N(0)| ≤ 1e-04 かつ |ΔF| ≤ 5e-06 (s ≤ 2、LK_S 上。量子化の半歩 5e-07 の 10 倍)** なら
  「krc12 は出荷の量子化水準で収束」、超えた行は「cap/margin 律速」の層として別に数える (事後に閾値を動かさない)
- 認証台本は `sigma_window_v1` を直接呼ぶので、**公開入口 `sigma_beta_delta()` の規格化 (own/Bote の両返し・
  分母 [0, ε_max]・β = π) と `model_id` は認証の対象外** (煙試験と B8 fixture で別に検査)
- オラクルも同じ `node_rl` を通るので、参照関数の段差そのもの・Miller ガード窓は差から消える
- `indicator` は診断値。偽陰性 (scaled 大 × indicator 小) の件数を数えるだけ
- 標本は決定論的な層別標本。**全格子の observed_max は主張しない**
- ⚠ v2 は v1 より 1 行あたり ~3 倍重い。pilot の Ca M1 @400 は 1 スレッドで 6 時間超になりうる
  (行の途中で止めると、その行は最初から)

## 6. 費用の見積り (pilot で実測してから埋める)

| | 見積り | 根拠 |
|---|---|---|
| pilot (11 行、9 レーン) | 最長レーン 3〜7 時間 (2 スレッド) | 煙試験の 3.3 倍 × v1 の実測 |
| deep (≈ 1,580 行、16 レーン × 1 スレッド) | **v1 の 3.4〜4 日 × ~3 = 10〜12 日** (⚠ 作者判断。費用削減案 = 重複窓の一度評価・σ_ref の再利用・無効窓の事前除外・スレッド 2 本化) | pilot で更新 |

## 7. 実施記録 (走らせたあとに追記)

### 7.1 pilot (v2) — (起動時に記入)

- 起動 **2026-08-19 23:14–23:18** (9 レーン × 2 スレッド、`CERT_RULE=v2 STAGGER=30 bash tools/run_cert_v2_fleet.sh pilot 9 2 cert_v2b_pilot`)。
  commit **`184a65c`** (dirty 0)、Julia 1.11.9、指紋 **`b447496bf5c31a45`**
  (fp.rule = `…/ppw30.0/dt0.001/lcap128/sig1.0e-13/lmax:kappa_rc+12`)。出力 = `../cert_v2b_pilot_lane*.jsonl` (リポ外)
- 並走: `tools/lkin_sweep.jl` (3 レーン × 6 スレッド、22:53 起動、HEAD 334c35b の版 — ⚠ E₀ ≤ 35 keV の行は s=16 が
  運動学的上限の外で例外 → `b04ccfe` で修正、完走後に当該行だけ再開)
- 結果は `docs/notes/certification_v2_pilot_2026-08-19.md` の続き (§6 以降) に書く
