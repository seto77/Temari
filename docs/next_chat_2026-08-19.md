# 次チャットへの指示書 (2026-08-18 夜)

**⭐ 冒頭でこれを読む。**前段の正本は `docs/next_phase_2026-08-18.md` (方向)、
本書は**その上での現在地と、次に何をどの順でやるか**。

⚠ 本書には**実測**と**作者決定**と**私 (Claude) の解釈**が混ざる。区別して書いた。

## 0. 五行で

1. **看板は B (`σ(β,Δ)` と k-factor) を最有力のまま、確定させていない**。上位概念として
   **J = GOS から実験幾何へ写す EELS response engine** が出た (`docs/candidate_j_2026-08-18.md`)
2. ★★ **A (Bethe 尾根) は blocking な前段から外した**。代わりのゲート =
   **Zhang の GOS を同条件で積分した σ 比**が動き、**0.83–1.11** が出ている
3. **契約は草案 v2 まで書けた** (`docs/sigma_beta_delta_contract_2026-08-18.md`)。
   **作者判断 4 件は決着済**。⚠ **凍結はまだ**
4. ★ **次の一手 = 全格子認証の実行** (§1)。ハーネスは完成・試走済み、**推定 41 時間**
5. ⚠ **src は 2026-08-18 のセッションで 1 行も変えていない** (`git diff ff50269..HEAD -- src/` が空)

## 1. ★ 最初にやること — 全格子認証を回し始める

**ハーネスは完成している** (`tools/certify_sigma.jl`、4 行で試走済み)。長いので**先に起動**し、
走っている間に §2 以降を進める。

```bash
cd /c/Users/seto/source/repos/Temari
nohup julia +1.11 --project=. -t auto tools/certify_sigma.jl \
      cert_sigma_v1.jsonl > cert_sigma_v1.log 2>&1 &
```

- **規模**: 525 チャネル × 全 E₀ = **14,796 行** × 窓 16 種 × β 8 本
- **推定 41 時間** (実測 7.4 s/行 (C K) 〜 12.7 s/行 (Au M5)、32 スレッド)。
  ⚠ **SCF 未キャッシュのチャネルは 13–29 s 余分**にかかる (525 チャネル分で最大 3 時間)
- **再開**: 同じコマンドを再実行するだけ (JSONL に既にある行は読み飛ばす)
- **生死の見方**: `cert_sigma_v1.jsonl` の **mtime**。⚠ **wedged はプロセスが残る**ので
  「プロセスがいる = 生きている」ではない (`src/IMPORT.md` の既知の運用上の注意)
- **集計**: `julia +1.11 --project=. tools/certify_sigma.jl cert_sigma_v1.jsonl --summary`

⚠⚠ **Windows Julia の GC クラッシュは健在** (v4 生成中に 5 回、うち 2 回は wedged)。
41 時間なら **必ず遭遇する前提** で、`tools/lane_watchdog.sh` を当てるか、
**数時間おきに mtime を見て落ちていたら同じコマンドで再開**する。
⚠ **完走 ≠ 健全** — 終わったら必ず `--summary` を読む。

### 1.1 集計が出たら契約の §5A を書き換える

今の §5A は**代表標本の観測最大値**。全格子の結果 (中央値 / p90 / p99 / 最悪、
および悪い行の並び) で置き換える。⚠ **`≤` と書かない** — `observed_max` のまま。

### 1.2 外部ゲートも全元素へ広げる

`refs/data/database/` に **108 元素を展開済み** (505 MB、zip はローカルにあった。
ダウンロードは不要だった)。⇒ `tools/dump_for_zhang_sigma.jl` の `ZSPEC` を
**全チャネル (先方の DB にある端すべて)** へ広げて回す。

- GOS 面は **E₀ 非依存**なのでチャネルあたり 1 回 (0.3–0.6 s)。525 面で ~5 分
- Python 側の σ 比は 4 チャネル × 12 条件で **1.1 s** なので、全体でも数分
- ⇒ **外部ゲートは内部認証よりずっと安い。先に片づけてよい**

## 2. 決まっていること (蒸し返さない)

### 2.1 作者決定 (2026-08-18)

| | 決定 |
|---|---|
| 看板 | **B に確定しない**。要所で codex と「B と並ぶ看板 / 新しい看板」を検討し続ける |
| 有限 α | **v1 では限定しないが、パラメータとしては受け付けない** (契約 §2.3) |
| 環状開口 | **直接区間積分を正とする** (「外−内」では出さない) |
| 上限超過窓 | **既定は拒否**。`clip=true` のときだけ切り詰め、requested と effective の両方を返す |
| 既定の規格化 | **`bote`** (両方常に返す) |
| k-factor | **A と B に同じ E₀・開口・窓を強制**。分母の床は**相対値** `σ_A < 1e-10 × σ_total_own` |
| `β > π` | **拒否** |
| 認証の広さ | **出荷格子全部 + 窓 16 本 + 外部ゲートは 108 元素全部** (計算時間より保証範囲を優先) |
| 長時間実行 | **watchdog + 行チェックポイント** |

### 2.2 測って決着したこと

- **部分角は求積区間の制限で出せる**。β=π は既存 `angular_integral` と**全 ε ノードで
  ビット同一** (PROD 72/72、6 通り)
- **β は事実上ただ** (`eps_setup` の 0.005–0.14 %/本)。⚠ **新しい窓はただではない**
- ★★ **n を倍にする自己収束テストは誤差を 26 倍過小評価する** (誤差が非単調)。
  **出荷設定 n_x=96 は n_x=64 より系統的に良くない**。
  ⇒ **収束の主張には必ず「変数を変えた独立オラクル」か「係数つき漸近」を使う**
- **A (尾根) が σ に持つ重み**: 尾根帯は σ の **0.00–5.79 %** (β≤30 mrad なら ≤2.42 %)
- **Zhang との同条件 σ 比 = 0.83–1.11** (端相対の窓)。⚠ **閾値の揃え方で変わる** —
  絶対損失で揃えると Au M5 の [0,50] eV 窓が **2.207** に跳ねる (**M4/M5 の遅れ立ち上がり**)
- A1–A5・軸 8・軸 10 の結果は契約 §7.6–§7.8

## 3. 契約凍結までに残っていること

| | 項目 | 状態 |
|---|---|---|
| 1 | **全格子の内部認証** | §1 で回す。**これが本丸** |
| 2 | **外部ゲートを 108 元素へ** | §1.2。安い |
| 3 | 床 1e-08 の原因 | **未特定**。n_q を 8 倍しても系統的に下がらない。⚠ **深追いしない** — 契約には「両経路が互いに 1e-08 で頭打ちになる。原因未特定」と書けばよい |
| 4 | §5B の経路別許容値 | ⚠ **1 の結果を見てから決める** (作者判断) |

## 4. 次に codex へ相談すべき節目

作者指示: **節目ごとに codex に相談し、完了済み項目も再確認させる**
(`config` に `{"model_reasoning_effort": "medium"}` を必ず渡す)。

1. **全格子認証の集計が出たとき** — §5B の経路別許容値の置き方
2. **契約を凍結する直前** — 「凍結してよいか」を負のテストの観点で
3. **S2b (本実装) の設計** — 既存 K=0 分岐を触らずにどう配線するか

## 5. S2b (本実装) の設計メモ

⚠ **まだ書いていない** — 凍結後に着手する。制約だけ先に:

- **既存の K=0 分岐 (`l4_angular.jl` の `angular_integral`) を一般化しない。**
  部分角用の**別関数**を足し、全角度は既存へ dispatch する。
  現コードは総和順序までビット同一性に依存している
- 実装後に **`tools/bitident_snapshot.jl` の v3 5ch / v4 7ch を両方**走らせて差分ゼロを確認
- `tools/beta_spike.jl` の `partial_angular` / `oracle_angular` / `asymptotic_ratio` /
  `annulus_angular` (`contract_axes_probe.jl`) が**試作の実体**。src へ移すときは
  **Q² の式を `:naive` のまま**にする (ビット同一 closure の担保。式間差は σ まで来ると ≤1.3e-09)
- 契約 §1 の**返却フィールド一覧**をそのまま JSON schema にする

## 6. 2026-08-18 に足したもの (すべて src の外)

| ファイル | 中身 |
|---|---|
| `docs/beta_spike_2026-08-18.md` | S2a スパイクの測定の正本 (G1–G11) |
| `docs/candidate_j_2026-08-18.md` | 看板候補 J の検討。**A を前段から外した根拠**もここ |
| `docs/sigma_beta_delta_contract_2026-08-18.md` | **契約 草案 v2**。作者決定 4 件込み |
| `tools/beta_spike.jl` | 部分角求積の試作 + 独立オラクル + 漸近 |
| `tools/j_surface_probe.jl` | 応答面の格子・容量・**ρ 帯の重み** |
| `tools/sigma_contract_probe.jl` | 契約前の (a)–(d) |
| `tools/contract_axes_probe.jl` | A1–A5 |
| `tools/dump_for_zhang_sigma.jl` + `tools/sigma_vs_zhang.py` | **外部ゲート** |
| `tools/certify_sigma.jl` | **全格子認証のハーネス** |

⚠ `refs/data/database/` に **108 元素を展開済み** (gitignore 済み、505 MB)。

## 7. 罠 (2026-08-18 に実際に踏んだもの)

- ⚠ **重みは対象全体で取る** — 応答面の内挿誤差を**行の最大値**で正規化して
  「面は表せない」と誤読しかけた。実際は EELS が使わない隅だけ ([[count-vs-weight]])
- ⚠ **`write_json` は整形して改行を入れる** — JSONL にならない。自前で 1 行に詰めた
- ⚠ **`json_loads` は存在しない** (実体は `parse_json_file` / `_json_value`)。
  **沈黙する `catch` がそれを隠した** — 読めなかった行を数えるようにした
- ⚠ **Bash heredoc の中の Python で `\n` を含む置換が壊れる** ([[bash-heredoc-backslash-mangling]])。
  複数行の置換は Edit ツールを使う
- ⚠ **和文 md の `**強調。**` は閉じられない** — `tools/md_emphasis_check.py` を毎回かける
