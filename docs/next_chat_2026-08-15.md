# 次チャットへの指示書 (2026-08-14 09:20 作成。フリート完走後に更新予定)

⚠ 本書は「次に何を、どの順で、どう気をつけてやるか」だけを書く。根拠は正本へ:

| 正本 | 内容 |
|---|---|
| **`docs/repr_measurement_2026-08-14.md`** | ★ B_repr/B_repr,e の実測 (84 元素) と設計問題 2 件 |
| **`docs/endpoint_truncation_2026-08-14.md`** | ★ 端点切断スクリーニング (86/86、≤1.2 % B_grid) |
| `docs/grid_certification_l1_run_2026-08-14.md` | L¹ 認証 v2.1 最終 (certified 83 / low_signal 3 / violated 0) |
| `docs/grid_certification_preregistration_v2.1_2026-08-14.md` | v2.1 の凍結規則 (`cddd5d0`) |
| `docs/scattering_factor_dataset_plan_2026-08-10.md` §4.23.8 | T_comp,e の作者決定と保留理由 |
| `docs/next_chat_2026-08-14.md` | 前日の指示書 (§2-1〜§2-4 実施済み、§2-5/2-6 フリート中) |

## 1. 走行中のフリート (2026-08-14 08:05 発火、直列 3 段)

`c:/tmp/temari_endpoints_2026-08-14/launch_ep.sh` が taskscheduler ジョブ
`Temari_endpoints_st1` から発火し、直列に:

1. ~~端点切断 (86 元素)~~ — **完走・報告済み** (08:56、`70bbbba`)
2. **標本 14 元素** → `c:/tmp/temari_sample14_2026-08-14/`
   - τ/100 (`t100_z*.json`、13 元素 + H は事前試験で完了済み)
   - `--prod-all` (`z*.json`、certify_grid 形式)
3. **Tm/Yb の当初規則走** → `c:/tmp/temari_certify_2026-08-11/z069/z070.json`
   (Er は完了済みで skip。最後に v1 の aggregate が走り AGGREGATE_final.txt 更新)

進行は `launch.log` の「終了」行で分かる。**PC 再起動があってもタスク登録は
生き残る** — ジョブを手で再実行するか、`schtasks /Run /TN Temari_endpoints_st1`
で再発火すれば skip 機構が続きから回す。

## 2. フリート完走後にやること (順)

### 2-1. 標本 14 元素の集計

- τ/100: `t100_z*.json` の `u_prime` / `u_prime_over_delta3` / `u_prime_over_B_scf` を
  表にする。**converged=false の元素は「検査不能」— 合否に混ぜない**
  (目的の再定義はツールヘッダに固定済み: 「τ/10 が十分 tight であることを、
  τ/100 が届く元素で確かめる」)。H は実施済み (U′ = B_scf の 0.026)
- --prod-all: 各 JSON の `levels[].U` が stage に依らず平らか
  (C の v1 実測 1.256/1.248/1.274e-09 と同型か) を見る
- 結果は docs/ に短い報告 (endpoint 報告と同じ形式) を書いてコミット

### 2-2. certify_repr を 86 元素で再実行 (Tm/Yb が埋まったら)

    julia tools/certify_repr.jl c:/tmp/temari_certify_2026-08-11 --json c:/tmp/temari_repr_2026-08-14/repr_summary_86.json

`repr_measurement_2026-08-14.md` の「84 元素 (Tm/Yb 欠け)」を 86 に更新。
⚠ 結論 (最悪値) が変わるかもしれない — Tm/Yb は拡がった元素なので f_x の
端区間誤差が Cs 級に出る可能性がある。変わったら報告書の数値を差し替える。

### 2-3. 作者判断待ち 3 点 (計画書 §4.23.8。急がないが忘れない)

1. f_x の収録規約: 左端 clamped (f′(0)=0) + 右端 not-a-knot へ変えるか
   (現行 NAK は Cs/Ba が B_repr 超過。clamped で全元素 0.123× 以下)
2. f_e の出荷生成: δ 形求積 (∫ρ(1−j₀)r²dr 直接) にするか
   (しないと MB 構成床が Au 級 6 元素で B_num,e 超過 → T_comp,e 凍結不能)
3. JSON 十進丸め: 有効数字 11 桁

決まったら T_comp,e = 1e-7 Å (案 A) を凍結し、計画書 §4.23.8 を確定に書き換える。

### 2-4. 残り (端点切断の 2 段目)

事前登録 v1 §8 の**極端例** (原点側 = Au・Rn / 遠方側 = H・Li・Na・Cs) を
**採用格子 dt/16 でも** + **標本の deep** (r₀/100・r_max×2、縮み比を見る)。
一次が小さかったことを理由に**取りやめない** (事前指定を結果で変えない)。
`certify_endpoints.jl --stage 5 --deep` が実装済み (ラダー入り)。
⚠ dt/16 の SCF は重い (Au tight 1 本 ~4000 s)。8 元素 × 5 SCF ≈ 1.5 日級 —
フリート設計してから。

## 3. ⚠ 運用規律 (前日から変わらず + 本日の追加)

- 起動 = タスクスケジューラのトリガー自然発火 + ASCII VBS。nohup と手動 Start は死ぬ
- **未収束解との差は何も測っていない** — endpoints 初走で Yb の r₀/10 が
  9 倍過大に出た (試行 2 で収束させたら 0.0045×)。SCF が絡む差分測定は
  必ず元素単位ラダーで収束させてから
- **certify_* 系のツールはフリート走行中に触らない** (tool_sha が嘘になる)
- 凍結後は走らせるまで 1 文字も変えない。欠陥は記録して次版の事前登録で
- 和文 md の強調は行内で閉じる。検査 = `PYTHONIOENCODING=utf-8 python tools/md_emphasis_check.py <files>`
