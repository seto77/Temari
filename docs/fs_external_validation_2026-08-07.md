# F(s) の外部検証 — 処方の判断材料 (2026-08-07)

*出荷の本体 F(s) を Oxley–Allen 2000 と µSTEM に突き合わせ、
`docs/next_phase_2026-08-07.md` §4 / `frozen_core_and_transverse_2026-08-07.md` §6 /
`kappa_dirac_continuum_2026-08-07.md` の「作者判断待ち」に必要な 1 枚を埋めた記録。*

---

## ⚠ 数値をここに書かない理由

`CONTRIBUTING.md`「Do not add reference data from restricted sources」が
**Oxley–Allen 2000 の表と µSTEM を名指しで禁じ**、「それらから導出した数値も、
コード・テスト・ドキュメント・issue に入れてはならない」と定めている。
乖離のパーセント値は参照から導出した数値なので、**本書には載せない**。

- 参照データの所在: `ReciPro/tools/IonizationGen/`
  (`refs_oa2000.py`, `oa_k_full.json`, `oa_L_full.json`, `mustem_{1s,2s,2p}_full.json`)。
  `refs_oa2000.py` 自身に「**比較検証専用 — 公開リポ/配布物へコピー禁止**」とある
- 比較スクリプトは scratchpad のみ。ReciPro 側のファイルを読むだけで、
  Temari には何も書き出さない
- 既存の物差しは `ReciPro/tools/IonizationGen/compare_heavyL.py`。
  正規化形状 F(s) = f(s)/f(0) を比べる (端エネルギー・絶対値の規約差が落ちる)。
  一致域は **K が s ≤ 1.25、L が s ≤ 0.625**

**数値表が要るなら ReciPro 側で再実行すること。**本書は順位と結論だけを残す。

---

## 測定した処方

出荷 (SRC + 緩和 core-hole + Xα) を基準に、4 つの変種を同じ物差しで測った:
`+KLI` / `+frozen` / `kdirac` / `kdirac+frozen`。
対象は K 殻 7 ケース (Z = 6, 8, 14, 26×2, 29, 47) と L3 殻 4 ケース
(Z = 26, 38, 47, 79)、参照は OA2000 と µSTEM の両方 (L3 は µSTEM 2p のみ)。

---

## 結論 (順位のみ)

### K 殻 — **κ 分解 Dirac が明確に勝つ**

- `kdirac` が **Z = 6 から 29 の全ケースで、OA2000 と µSTEM の両方に対して
  乖離を減らす**。改善幅は軽元素で小さく、中重元素で大きい (Fe では乖離が
  1/4 以下になる)。唯一 Z = 47 だけ僅かに悪化する
- `+frozen` は**明確に悪化させる**。軽元素ほどひどく、C では乖離が 3 倍に育つ。
  引き継ぎ書 §2 の⚠「緩和 core-hole は Oxley–Allen / µSTEM との A/B で
  選ばれた処方で、F(s) を最も動かした要素」という警告が**実測で裏付けられた**
- `+KLI` はほぼ中立 (Z ≥ 8 で僅かに改善、Z = 6 で僅かに悪化)

### L3 殻 — **KLI が明確に勝ち、κ 分解 Dirac は負ける**

- `+KLI` が **4 ケース全部で乖離を減らす**。Fe・Sr・Ag では大幅 (Ag では
  ほぼ参照に張り付く)。K 殻でほぼ中立だったのと対照的
- `kdirac` は Fe・Sr・Ag で悪化し、Au でのみ僅かに改善する
- `+frozen` はほぼ中立〜僅かに悪化

⚠ **K と L で最良の処方が違う。**これは「どれか 1 つを出荷既定にすれば全部良く
なる」という話ではないことを意味する。K は連続状態の相対論 (κ 分解) が効き、
L は束縛側の交換 (KLI) が効く — 波動関数の広がりが違うので筋は通る。

---

## 判断への含意

| | f_x (OFFV1) | GOS (Zhang) | σ_own/σ_Bote | **F(s) (OA/µSTEM)** |
|---|---|---|---|---|
| ① Dirac+KLI | **決定的に改善** | — | 僅かに悪化 | K 中立 / **L3 は明確に改善** |
| ② frozen core | — | Fe 改善・Au 悪化 | 全例改善 | **明確に悪化 (軽元素で顕著)** |
| ③ 横断項 | — | — | **改善** (E0 依存性の形も合う) | 影響なし (K=0 専用) |
| ④ κ分解 Dirac | — | **幅 11.2→4.0 %** | 5 例すべて 1 へ | **K は明確に改善 / L3 は悪化** |

- **② frozen core を出荷既定にするのは支持されない。**GOS は Fe で改善するが、
  F(s) は外部参照から離れる。緩和 core-hole が A/B で選ばれた理由がそのまま生きている
- **① と ④ は殻依存**。K に ④、L に ① が効く。両方入れた `kdirac+KLI` は未測定
  (次の一手)
- **③ は無条件で入れてよい** — F(s) を動かさないのでテーブル再生成も要らない

---

## 次の一手

- **`kdirac` + `KLI` の組み合わせ**を測る。K と L で効く方向が違うので、
  両方入れたときに足し合わさるのか打ち消すのかは測らないと分からない
- Z = 47 で `kdirac` が悪化する理由を見る (K 殻で唯一の逆転)
- L 殻の OA2000 参照は 2s + 2p 合算なので、µSTEM 2p だけが物差しになっている。
  µSTEM 2s と L1 を突き合わせれば L 殻の見立てが 1 本増える
