# dataset v5.0.0 — errata / 出荷後の測定 (2026-08-20)

⚠ 本書は出荷済み dataset v5.0.0 の**隣に置く注記**であって、データにも `MANIFEST.md` にも 1 バイトも
触れていない (MANIFEST は SHA-256 で凍結された出荷宣言なので直接編集しない)。正本の測定記録 =
`docs/notes/lkin_truncation_2026-08-19.md` §6 と `docs/notes/eps_nodes_threshold_2026-08-20.md`。
対処は次世代 (dataset v6) の処方変更 — 生成コードは変更済み (Temari `381e777` → `1dcaf5a`、n1 は
2026-08-20 夜のコミット)。

## 1. 部分波打ち切りの処方感度 (出荷後の測定、2026-08-19〜20)

出荷の部分波数の規則 `l_max = min(l_cap, max(6, min(l_kin, l_barrier)))`、`l_kin = ⌈κ·min(r_core, 6/Z)⌉ + 12`
(l_cap = 128) と、上限 6/Z を外した `min(128, ⌈κ·r_core⌉ + 12)` を、**出荷の F(s) 生成経路のビット一致の写し**
(HIGH 設定。`tools/lkin_truncation_probe.jl` / `tools/lkin_sweep.jl`) で全 525 チャネルについて比べた
(K/L は最大 E₀、M は最小・中央・最大 E₀。M の最大 E₀ は出荷 s 格子 321 点、それ以外は
s = {0, 0.25, 0.5, 1, 2, 4, 6, 8, 12, 16} Å⁻¹ の標本点)。

| 殻 | F の絶対差 max (s ≤ 0.5) | N(0) = σ_own の相対差 max |
|---|---|---|
| K | ≤ 1.0e-07 | ≤ 3.1e-07 |
| L1 / L2 / L3 | 1.6e-04 / 9.2e-05 / 9.4e-05 (Ca–Co @400 keV。標本点上 — 峰は最大 13 % 大きい可能性) | 6.0e-04 / 2.4e-04 / 2.4e-04 |
| M1 | **1.65e-03** (Z = 30–41 @400 keV、峰は s ≈ 0.15–0.30。出荷格子上) | **5.7e-03** |
| M2 / M3 | 1.26e-03 / 1.28e-03 (出荷格子上) | 4.2e-03 / 4.1e-03 |
| M4 / M5 | 6.30e-04 / 6.35e-04 (出荷格子上) | 1.2e-03 / 1.2e-03 |

- KIND = **二処方間の観測された処方感度** (prescription sensitivity)。不確かさの上界でも、真値との距離でもない
- 方向: 第 2 の処方が「試験した、より収束した側」(1 ノードの l_max 掃引は単調。cap 256 / margin +32 の
  aggressive reference との増分は F で ≤ 1.1e-05)。ただし ΔF は s で符号を変えるので F に単一の向きはなく、
  「補正値が分かっている」とは言わない
- E₀ 依存: F の感度は E₀ とともに増え、最悪は一貫して最大 E₀ (400 keV)。30 keV でも M 殻の F で 3〜8e-04
- s ≤ 2 で測った E₀ 補間の項 (8.5e-05) より M 殻では 1 桁大きい。**別の種類の項なので足さない**
- 観測量への伝播は s < 0.5 の重みに依存する (サイト比で ~1e-03 級の見積り。1 配置の伝達係数から)
- 対処 (v6): `l_kin = ⌈κ·r(含有率 0.999)⌉ + 12`、l_cap 256 (要因計画 18 行 × 24 規則で選択、
  aggressive reference に対し ΔF ≤ 4.1e-07)

## 2. ε 求積の閾値側区間の未収束 (出荷後の測定、2026-08-20)

ε 積分 (`eps_nodes`、3 区間) の**閾値側 √ 区間 (n1 = 20 点)** が重 Z で未収束。層 = **Z に対して急峻な
ランプ (Z ≈ 78 で 1e-06 級 → Z = 86 で 1e-04 級) × K 以外の全殻 × 全 E₀**。最悪 = **Rn (Z=86) M5: F の
絶対差 6.0e-05 (s ≤ 2)、N(0) の相対差 2.4e-04** (n1 = 64 参照比。tanh-sinh の独立参照で照合済)。
K 殻は無傷 (Rn K で 2e-08)。

- 機構は総数一定の配分実験で n1 単独に特定 (中央の log 区間 n2 は 40 点で収束済、n3 は無関係)
- 部分波の項 (§1) より 1 桁半小さいが、v6 で部分波を直すと重 Z ではこれが支配的な数値項になる
- 対処 (v6): n1 = 20 → 40 (全行一律)。全副殻で残差 ≤ 3.1e-07、実時間 +10 % 以下

## 3. これらが意味すること

dataset v5.0.0 の数値は、**MANIFEST と `docs/src/*/data.md` が述べる検査をすべて通ったまま**である。
本書が足すのは「出荷後に測った、出荷処方の 2 つの数値的な項の大きさ」であり、M 殻の s ≤ 0.5 と
重 Z (Z ≳ 80) の K 以外の殻を使う利用者は、上の表の大きさを見込んでおくこと。次世代 (v6) は両方を
処方として直して再生成する (DOI は取り直す)。
